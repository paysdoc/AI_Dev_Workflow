/**
 * Dev server janitor — cleans up stale dev server processes.
 *
 * Placeholder module. Full implementation will be added in a future issue
 * (see the devServerLifecycle helper in adws/core/ for the lifecycle API it will use).
 */

/** Scans for stale dev server processes and terminates them. No-op stub. */
export async function runJanitorPass(): Promise<void> {
  // TODO: implement stale-process detection and cleanup
}
