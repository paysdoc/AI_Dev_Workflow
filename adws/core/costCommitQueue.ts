/**
 * Backward-compatible re-export barrel.
 *
 * CostCommitQueue has moved to adws/cost/commitQueue.ts — it belongs with the
 * cost module, not in core.
 */
export { costCommitQueue, CostCommitQueue } from '../cost/commitQueue';
