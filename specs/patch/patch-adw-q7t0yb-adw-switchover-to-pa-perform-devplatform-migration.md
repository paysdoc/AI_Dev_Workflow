# Patch: Perform the `@paysdoc/devplatform` 1.1.0 switchover — rebase, rewrite every import, delete `adws/gitContext/` and `adws/providers/`, empty the guard's exempt set, refresh BDD and docs

## Metadata
adwId: `q7t0yb-adw-switchover-to-pa`
reviewChangeRequest: `Issue #1: The migration was not performed. Verified on HEAD 2deec251 (merge-base 4d25d21c, 18 commits behind origin/dev, clean tree): git diff 4d25d21c..HEAD touches only README.md, .adw/conditional_docs.md, app_docs/feature-oqb76h-gitcontext-base-path-authority.md, the plan spec and nine specs/patch/*.md files — zero code. Acceptance criteria, all unmet: adws/gitContext/ and adws/providers/ exist and 221 relative imports from outside them still resolve into them (the spec's validation grep prints 161 lines, must print nothing); package.json has no @paysdoc/devplatform and node_modules/@paysdoc is absent; the issue #840 forge-name grep lists 16 production files; bun run lint:git-guard prints 'Exempt packages (2)', 'Sanctioned construction sites — 2 permanent, 0 sunset' (adws/providers/forgeProviders.ts still listed) and 'Extraction-readiness scope — 10 entries'; adws/guard/extractionRule.ts plus both its tests exist and 'extraction-readiness' is still a ViolationRule member (adws/guard/violationTypes.ts:13); features/regression/upgrade/feature-729.feature lines 24, 36 and 81 still cite adws/gitContext; app_docs/feature-9gjajh-providers.md still exists and .adw/conditional_docs.md has 14 adws/gitContext or adws/providers references. The gate that excused this in the previous nine reviews is now open: npm view @paysdoc/devplatform versions returns 1.0.0 and 1.1.0 (1.1.0 published 2026-09-22T11:24:14Z, dist-tag latest); the packed 1.1.0 tarball's /providers barrel exports createForgeCredentials, forgeProviders, createGhRepoApi, createGitHubTokenProvider, resolveBootstrapGitIdentity, resolveContextToken, ghAuthToken, isGitHubAppConfigured and getInstallationToken (types GhRepoApi, GitHubAppConfig, AppAuthDeps, ForgeProvidersOptions and ForgeProviderDeps reachable through its export-star chain) and its /git barrel exports GitContext, createLiteralTokenProvider, commitOps, isLeaseRejection, branchOps and consoleLogger; devplatform#11 (L4) is CLOSED, its PR #14 merged at 11:02Z and release run 35722234979 succeeded, so the OIDC publish blocker is also gone. The symbols 1.1.0 still lacks (readLocalRepoInfo, parseGitHubIssue, selectPreferredPR, convertToSshUrl, GitLabApiClient) are no longer imported from adws/providers by any file on origin/dev after #844 (ADW owns readLocalRepoIdentity and convertToSshUrl in adws/core), so after the rebase the published surface covers every ADW consumer. The three doc notes this branch added (the README.md test-listing hunk, the conditional_docs.md rename condition, the living-doc 'No physical npm package … once published' sentence) describe the package as unpublished, which is now false, and two of those files conflict with origin/dev. This run's first patch attempt (11:49Z) was terminated by a rate limit before committing anything. Resolution: Perform the migration. First rebase this branch onto origin/dev (18 commits behind; #844/#845 and #846/#847 changed the consumer inventory). git merge-tree reports exactly two docs-only conflicts, README.md and .adw/conditional_docs.md, where keeping either side is correct. Then execute the spec from Step 2 through Step 13 with @paysdoc/devplatform pinned exactly to '1.1.0' (no caret) in dependencies, following the updated issue #840 body for the launch boundary: adws/core/launchGitContext.ts obtains the token provider and bootstrap git identity from createForgeCredentials({ forge, identity, deps }) with deps.github = { appConfig, pat, alternateIdentityPat } built from adws/core/githubAppAuth.ts's env-read App config (the GitHubAppConfig type is re-exported from @paysdoc/devplatform/providers), dropping its direct imports of resolveContextToken, ghAuthToken, createGitHubTokenProvider, readLocalRepoInfo and resolveBootstrapGitIdentity; test fixtures take createLiteralTokenProvider from @paysdoc/devplatform/git; no production module outside test fixtures imports a forge-named symbol (createGitHub*, createGitLab*, GitLabApiClient, Jira*). Land test/mocks/gitContextFixture.ts, rewrite every import onto the three entry points by resolving specifiers (adws/cost/providers/anthropic/** must stay untouched), delete adws/gitContext/ and adws/providers/, empty EXEMPT_PACKAGES, delete the extraction-readiness rule with its plumbing and tests, trim SANCTIONED_CONSTRUCTION_SITES to adws/core/launchGitContext.ts, add the imported-name guard tests from spec Step 8, repoint feature-729.feature and its steps, retire app_docs/feature-9gjajh-providers.md with its index entry, and replace the three stale 'unpublished' doc notes. Validate with every command in the spec's Validation Commands plus the #840 forge-name grep: both typechecks, lint, unit (expect about 144 files), lint:git-guard printing an empty exempt set and exactly one sanctioned site with no extraction section, lint:docs-index, the @regression suite, the import grep silent, and both directories gone.`

## Issue Summary
**Original Spec:** specs/issue-840-adw-20l8es-adw-switchover-to-pa-sdlc_planner-devplatform-switchover.md
**Issue:** Ten review cycles have produced only docs and patch plans because spec Step 1's gate was closed. It is now open: `@paysdoc/devplatform@1.1.0` is on npm with every symbol ADW's `origin/dev` consumers need. None of the four acceptance criteria are met on this branch: both extracted directories exist and 249 import specifiers across 143 files (on `origin/dev`) still resolve into them; there is no dependency; the guard still exempts two packages, still lists `adws/providers/forgeProviders.ts` as a sanctioned site and still runs the extraction-readiness rule; `feature-729.feature` cites `adws/gitContext`; the providers living doc and its index entry still exist; and the three "unpublished" doc notes this branch added are now false. The branch is also 18 commits behind `origin/dev`, whose #844 and #846 changed the consumer inventory (ADW now owns `readLocalRepoIdentity`/`convertToSshUrl`, and the HITL notifier, issue record and health checks go through the forge ports).
**Solution:** Rebase onto `origin/dev` (two docs-only conflicts, upstream side taken — both files are rewritten later anyway), then execute spec Steps 2–13 against the rebased tree: pin `@paysdoc/devplatform` at exactly `1.1.0`; land `test/mocks/gitContextFixture.ts`; rewrite all 249 specifiers by resolved path onto the three entry points (`adws/cost/providers/anthropic/**` untouched); rework `adws/core/launchGitContext.ts` so `createForgeCredentials({ forge, identity, deps })` supplies the launch `TokenProvider` and bootstrap `GitIdentity` with `deps.github = { appConfig, pat, alternateIdentityPat }` from `adws/core/githubAppAuth.ts`'s new env-read `readGitHubAppConfig()`; delete `adws/gitContext/` and `adws/providers/`; empty `EXEMPT_PACKAGES`, delete the extraction-readiness rule with its plumbing, workflow wording and tests, trim `SANCTIONED_CONSTRUCTION_SITES` to the launch boundary and prove the name-based rules still fire on library-imported names; repoint `feature-729.feature` and its steps; retire `app_docs/feature-9gjajh-providers.md` and its index entry, rewrite the `oqb76h` living doc and entry, refresh README/`adws/README.md`/`.adw/project.md`/`UBIQUITOUS_LANGUAGE.md`, and replace the three stale notes; validate with every spec command plus the #840 forge-name grep; restate the operator gate (runbook 7.4) in PR #843's body.

### Verified at patch-plan time (2026-09-22, after the tenth review)
- **npm:** `npm view @paysdoc/devplatform versions dist-tags.latest` → `['1.0.0', '1.1.0']`, latest `1.1.0`. Packed 1.1.0 is ESM-only (`"type": "module"`, `engines.node >=20`; ADW's `package.json` is `"type": "module"` too) with the closed `exports` map `.`, `./providers`, `./git`, `./package.json`. Runtime keys — root: `BOARD_COLUMNS BoardStatus Platform validateRepoIdentifier` plus every port/domain type of `providers/types.d.ts` (`RepoIdentifier Issue IssueComment IssueSummary IssueListEntry IssueTracker CodeHost BoardManager PullRequest PullRequestRecord PullRequestSummary PullRequestResult CreatePROptions ForgeActionResult ReviewComment BoundProviders RepoContext …`); `/providers`: `createForgeCredentials forgeProviders createGhRepoApi createGitHubTokenProvider resolveBootstrapGitIdentity resolveContextToken ghAuthToken isGitHubAppConfigured getInstallationToken CODE_HOST_FORGES ISSUE_TRACKER_FORGES isCodeHostForge isIssueTrackerForge UnknownForgeError validateWorkingDirectory parseOwnerRepoFromUrl createGitHubIssueTracker createGitHubCodeHost createGitHubBoardManager createGitLabCodeHost createGitLabBoardManager createJiraIssueTracker createJiraBoardManager GitHubCodeHost GitHubIssueTracker GitLabCodeHost JiraApiClient JiraIssueTracker` (+ mappers) and the types `GhRepoApi GitHubAppConfig AppAuthDeps ForgeSelection CodeHostForge IssueTrackerForge ForgeProvidersOptions ForgeProviderDeps GitHubForgeDeps ForgeCredentialsOptions ForgeCredentials ForgeCredentialDeps GitHubCredentialDeps GitLabConfig JiraConfig JiraAuth RawPR GitHubIssue GitHubLabel GitHubComment PRDetails PRListItem PRReviewComment`; `/git`: `GitContext createLiteralTokenProvider consoleLogger commitOps isLeaseRejection branchOps claimOps worktreeCreateOps worktreeRemoveOps worktreeQueryOps worktreeProbeOps worktreeResetOps killProcessesInDirectory readOriginRemoteUrl readCurrentBranch readEnvGitIdentity readGitConfigIdentity getTargetRepoWorkspacePath isRepoCloned cloneRepo ensureRepoWorkspace` and the types `GitIdentity GitContextOptions ExecFn GitContextDeps FsDeps ExecWorkingDirectory ExecOptions TokenProvider CredentialPurpose CredentialRequest Logger LogLevel LogSinceOptions GitConfigIdentityDeps`. `createLiteralTokenProvider(token, alternateIdentityPat?)` is exported from `/git` only. Not exported by any barrel: `readLocalRepoInfo`, `parseGitHubIssue`, `selectPreferredPR`, `convertToSshUrl`, `GitLabApiClient` — and no file on `origin/dev` imports them from `adws/providers`.
- **`createForgeCredentials`** (`dist/providers/forgeCredentials.js`): `validateRepoIdentifier(identity)`, refuses a non-code-host forge by name, then for `codeHost === 'github'` returns `createGitHubTokenProvider({ pat, alternateIdentityPat, isAppConfigured: () => appConfigured, mintInstallationToken: (o, r) => getInstallationToken(appConfig, o, r, github.appAuth), ghAuthToken })` with `appConfigured = appConfig !== null && isGitHubAppConfigured(appConfig)` decided **at construction**, plus `gitIdentity = resolveBootstrapGitIdentity({ env, exec, appConfig })` resolved **eagerly** (env `GIT_AUTHOR_*`, then `git config`). `deps.github: { appConfig: GitHubAppConfig | null; pat?; alternateIdentityPat?; ghAuthToken?; env?; exec?; appAuth? }`; `deps.env`/`deps.exec` are the forge-neutral bootstrap-identity seams (`GitConfigIdentityDeps`). Same resolution order as ADW's current `createLaunchTokenProvider` (App token → PAT → `gh auth token`; alternate-identity requests short-circuit to `alternateIdentityPat`).
- **Branch vs `origin/dev`:** `origin/dev` = `1ec1295c`; HEAD `2deec251` is 18 behind / 10 ahead of it, merge-base `4d25d21c`, clean tree. `git merge-tree --write-tree origin/dev HEAD` → exactly two `CONFLICT (content)`: `README.md` and `.adw/conditional_docs.md`. The branch's README `MAX_FAILURES` hunk already exists on `origin/dev` (2 matches), so taking the upstream side loses nothing.
- **Consumer inventory on `origin/dev`** (`git grep -nE "(from|mock|import|require)\(?[[:space:]]*['\"](\.\.?/)+(adws/)?(gitContext|providers)(/[^'\"]*)?['\"]" -- '*.ts' '*.tsx'`, excluding the two directories and `providers/anthropic`): **143 files, 249 specifier lines** — `adws/core` 25, `adws/triggers` 40, `adws/phases` 27, `adws/forge` 10, `adws/agents` 6, `adws/__tests__` 6, `adws/vcs` 5, `adws/promotion` 2, `adws/proof` 1, `adws/adwClearComments.tsx`, `adws/adwMerge.tsx`, `adws/adwUpgrade.tsx`, `adws/healthCheck.tsx`, `adws/healthCheckChecks.ts`, `adws/guard/__tests__/extractionRule.test.ts` (deleted anyway), `features/per-issue` 13, `features/regression` 2. 75 are production files. Two non-`from` specifiers: `vi.mock('../../gitContext', …)` at `adws/core/__tests__/targetRepoManager.test.ts:17` and the inline type `import('../providers/types').CodeHost` at `adws/triggers/trigger_webhook.ts:94`. Four `providers/anthropic` specifiers exist and must survive.
- **Launch boundary on `origin/dev`:** already reads identity via `readLocalRepoIdentity` (#844); adapter imports left: `resolveBootstrapGitIdentity`, `ghAuthToken`, `resolveContextToken`, `createGitHubTokenProvider`, `forgeProviders`(+types), `Platform`/`BoundProviders`/`RepoIdentifier`, `GitContext`. `resolveLaunchToken`/`resolveLaunchGitIdentity` have no caller except the `adws/core/index.ts:204` re-export; `createLaunchTokenProvider` is called only by `launchGitContext.test.ts` §7b (lines 274–288), which relies on `vi.mock('../githubAppAuth', { isGitHubAppConfigured: () => true, getInstallationToken: () => 'app-token' })`. `forgeWiring.ts:22`, `adws/core/index.ts:162`, `forgeWiring.test.ts:13` and `workflowInit.test.ts:50` also import/mock `isGitHubAppConfigured`/`getInstallationToken` from `githubAppAuth` — those exports stay.
- **#840 forge-name grep on `origin/dev`** (`git grep -lE "createGitHub|createGitLab|GitLabApiClient|JiraApiClient|JiraIssueTracker" -- adws ':!**/__tests__/**'`, outside the two directories): exactly `adws/core/launchGitContext.ts` (imports `createGitHubTokenProvider`) and `adws/guard/constructionRule.ts` (its `PROVIDER_CONSTRUCTORS` string set *names* the factories the rule forbids — it imports nothing).
- **Unit-test count:** vitest's include matches 172 files on `origin/dev`; 36 are under the two directories and 2 are the extraction-rule tests → **134 files expected after this patch**. The review's "about 144" is the spec's pre-#844 estimate (#844 deleted `adws/vcs/__tests__/commitOperations.test.ts` and `fetchAndResetToRemote.test.ts`).
- **Source-reading tests that break with the directories:** `adws/triggers/__tests__/devServerJanitor.test.ts:681–699` (two tests read `adws/gitContext/processCleanup.ts` as text); `adws/__tests__/checkGitGhGuard.test.ts:736–768` (reads twelve in-tree declaration files as text, ten of them inside the two directories).
- **BDD loading:** `cucumber.js` imports `features/per-issue/step_definitions/**/*.ts` on every run, so the `@regression` run loads the 13 per-issue step files — their imports must be rewritten or the suite fails at load. No `features/regression/**` file asserts on the guard's stdout banners. Per-issue step files that shell out to the guard: `feature-797/810/816/844.steps.ts` (feature-816 asserts `[extraction-readiness]` output — see Notes).
- **Docs index:** 45 entries, band `[25, 60]`; `app_docs/feature-9gjajh-providers.md` owns only `adws/providers/**` (entry at `.adw/conditional_docs.md:259–277` on `origin/dev`); the `oqb76h` entry (`413–455`) owns `adws/gitContext/**` plus 21 live ADW files.
- **Push:** the pipeline pushes with `git push --force-with-lease --force-if-includes -u origin <branch>` (`commitOps.pushBranch`), so a rebased branch pushes; the branch reflog keeps the old remote tip, satisfying `--force-if-includes`.
- **Tooling note:** the repo's pre-tool-use hook blocks any shell command whose text contains a recursive-delete pattern, so the directory deletion in Step 6 must be issued as `git rm -r …` in its own command, and scratch directories under `/tmp` are left in place rather than removed.

## Files to Modify
Use these files to implement the patch:

**Dependency**
- `package.json` — `"@paysdoc/devplatform": "1.1.0"` under `dependencies` (exact, no caret); `bun.lock` regenerated.

**New**
- `test/mocks/gitContextFixture.ts` — verbatim copy of `adws/providers/github/__tests__/gitContextFixture.ts` (74 lines) with its two imports repointed at `@paysdoc/devplatform/git`.

**Deleted wholesale**
- `adws/gitContext/**`, `adws/providers/**` (`git rm -r`).
- `adws/guard/extractionRule.ts`, `adws/guard/__tests__/extractionRule.test.ts`, `adws/guard/__tests__/extractionRule.integration.test.ts`.
- `app_docs/feature-9gjajh-providers.md`.

**The launch boundary and its wiring** (all in `adws/core/`)
- `launchGitContext.ts` — `createForgeCredentials` boundary (Step 5); docblock rewritten.
- `githubAppAuth.ts` — gains exported `readGitHubAppConfig()`; imports from `@paysdoc/devplatform/providers`.
- `index.ts:204` — drop the `resolveLaunchToken`/`resolveLaunchGitIdentity` re-exports.
- `forgeWiring.ts`, `providerConfig.ts`, `workspaceBinding.ts`, `localRepoIdentity.ts`, `targetRepoManager.ts` (+ `sshCloneUrl.ts`, `workspaceTrust.ts`, `issueRecord.ts` comment repoints) — imports per the Step 4 mapping; the two "deep imports only … cycle (#792)" comments deleted.
- `__tests__/launchGitContext.test.ts` — imports, `githubAppAuth` mock, §7b rework (Step 5).
- `__tests__/targetRepoManager.test.ts:17` — `vi.mock('@paysdoc/devplatform/git', …)`.

**Every other consumer** — the 143-file inventory above (Step 4), including the four top-level `.tsx` orchestrators, `adws/healthCheckChecks.ts`, `adws/vcs/worktreeCleanup.ts` (an `export … from`), `adws/triggers/trigger_webhook.ts:94` (inline `import()` type), and the 15 `features/**/step_definitions/*.ts` files (`.ts`-suffixed deep paths become bare specifiers).
- `adws/triggers/__tests__/devServerJanitor.test.ts` — delete the two source-reading tests (lines 681–699).

**Guard**
- `adws/checkGitGhGuard.ts`, `adws/guard/constructionRule.ts`, `adws/guard/identityRule.ts`, `adws/guard/guardReport.ts`, `adws/guard/violationTypes.ts`, `.github/workflows/git-cli-guard.yml`, `adws/__tests__/checkGitGhGuard.test.ts`.

**BDD**
- `features/regression/upgrade/feature-729.feature` (lines 24, 36, 81), `features/regression/step_definitions/feature-729.steps.ts:19`, `features/regression/step_definitions/pythonFixtureE2ESteps.ts:34`, and the 13 `features/per-issue/step_definitions/*.ts` importers (`gitContextSharedWorld.ts`, `feature-794/796/797/810/817/819/820/821/823/823-assembly/823-workspace/844.steps.ts`).

**Docs**
- `.adw/conditional_docs.md`, `app_docs/feature-oqb76h-gitcontext-base-path-authority.md`, `app_docs/feature-9gjajh-github-api.md`, `app_docs/feature-9gjajh-types.md`, `README.md`, `adws/README.md`, `.adw/project.md`, `UBIQUITOUS_LANGUAGE.md`.

**Do NOT touch:** `adws/cost/providers/anthropic/**` and the four `providers/anthropic` specifiers; `specs/runbooks/gitcontext-extraction.md`; the per-issue `features/per-issue/*.feature` narrative files (see Notes); `SANCTIONED_CONSTRUCTION_SITES`' remaining entry, `CWD_DERIVED_IDENTITY_FNS`, `CONTEXT_CONSTRUCTOR_NAMES`, `PROVIDER_CONSTRUCTORS`, `CONTEXT_CONSTRUCTORS`, `GIT_CONTEXT_CLASS_NAME` (the retired names are reintroduction guards, PRD story 24).

## Implementation Steps
IMPORTANT: Execute every step in order, top to bottom.

### Step 1: Rebase onto `origin/dev`, taking the upstream side of the two docs-only conflicts
- `git fetch origin dev` and confirm the pre-conditions before rewriting history: `git status --short` empty; `git merge-base HEAD origin/dev` = `4d25d21c`; `git merge-tree --write-tree origin/dev HEAD 2>&1 | grep -c '^CONFLICT'` = `2`, naming only `README.md` and `.adw/conditional_docs.md`. If a third path conflicts, stop and resolve it by hand instead of relying on the strategy option below.
- `git rebase origin/dev -X ours` (in a rebase, `-X ours` favours the upstream `origin/dev` side of a conflicting hunk — correct here: the branch's README test-listing hunk describes files that are deleted in Step 6, its `MAX_FAILURES` line already exists upstream, and its `conditional_docs.md` rename condition is replaced in Step 9). The ten branch commits (spec, docs, nine patch plans) replay on top of `1ec1295c`.
- Confirm: `git diff --stat origin/dev` lists only `specs/**` files plus `README.md`, `.adw/conditional_docs.md` and `app_docs/feature-oqb76h-gitcontext-base-path-authority.md`; `git grep -c "No physical npm package" -- app_docs/feature-oqb76h-gitcontext-base-path-authority.md` prints `1` (the stale sentence survives the rebase and is replaced in Step 9); `bunx tsc --noEmit` and `bun run test:unit` are green on the rebased, still-unmigrated tree (172 test files).

### Step 2: Add the exact-pinned dependency and prove the three entry points resolve
- `bun add --exact @paysdoc/devplatform@1.1.0`; confirm `package.json` `dependencies` reads `"@paysdoc/devplatform": "1.1.0"` (no caret; it must not land in `devDependencies` — orchestrators import it at runtime) and `bun.lock` changed.
- Verify under the runtime ADW uses:
  ```
  bunx tsx -e "import('@paysdoc/devplatform').then(m=>console.log('root', Object.keys(m).length))"
  bunx tsx -e "import('@paysdoc/devplatform/providers').then(m=>console.log('providers', 'createForgeCredentials' in m, 'forgeProviders' in m))"
  bunx tsx -e "import('@paysdoc/devplatform/git').then(m=>console.log('git', 'GitContext' in m, 'createLiteralTokenProvider' in m, 'commitOps' in m))"
  ```
  Expect `root 4`, `providers true true`, `git true true true`. `tsconfig.json` and `adws/tsconfig.json` already use `moduleResolution: "bundler"`; no config change.

### Step 3: Land the ADW-local test fixture
- Create `test/mocks/gitContextFixture.ts` from `git show origin/dev:adws/providers/github/__tests__/gitContextFixture.ts`, replacing its three import lines with `import { GitContext, createLiteralTokenProvider } from '@paysdoc/devplatform/git';` and `import type { GitContextOptions, ExecFn, Logger, LogLevel } from '@paysdoc/devplatform/git';`. Keep `FRAMEWORK_ROOT`, `TARGET_REPOS_DIR`, `validOptions`, `SpyCall`, `makeSpyExec`, `makeCtx`, `CapturedLog`, `makeCapturingLogger` verbatim. Rewrite the docblock: ADW-owned since the switchover; the exec-fake fixture for tests that need a real `GitContext` over a spy `exec`; lives under `test/` because that directory is in the guard's `EXEMPT_DIR_NAMES`, so the `new GitContext` inside it is never walked by the construction rule.
- It has no importer on `origin/dev` today (#844 removed both former consumers) — land it anyway, as the issue names it as the replacement for the deleted fixture; do not refactor the hand-rolled `new GitContext(...)` fixtures in `forgeWiring.test.ts`, `worktreeSetup.test.ts`, `autoMergeHandler.test.ts` etc. onto it (out of scope).

### Step 4: Rewrite every specifier that resolves into the two directories (143 files / 249 lines)
- Build the file list from the rebased tree with the inventory grep in "Verified" above (it also catches `vi.mock(`, `import(` and `export … from`). Rewrite by **resolved path**, not by text — resolve each specifier relative to its file, strip a trailing `.ts` / `/index`, and map:

| Resolved path | New specifier |
|---|---|
| `adws/gitContext`, `adws/gitContext/*` (`types`, `gitContext`, `consoleLogger`, `bootstrapIdentity`, `commitOps`, `branchOps`, `worktreeResetOps`, `processCleanup`, `repoWorkspace`) | `@paysdoc/devplatform/git` |
| `adws/providers/types` | `@paysdoc/devplatform` |
| `adws/providers`, `adws/providers/forgeProviders`, `adws/providers/workspaceValidation`, `adws/providers/github/**` (incl. `domain/pullRequest` for `RawPR`, `githubTokenProvider` for `createGitHubTokenProvider`), `adws/providers/gitlab/**`, `adws/providers/jira/**` | `@paysdoc/devplatform/providers` |
| `adws/providers/github/githubTokenProvider` **when the imported name is `createLiteralTokenProvider`** (`forgeWiring.test.ts`, `upgradeClaim.integration.test.ts`, `worktreeSetup.test.ts`, `autoMergeHandler.test.ts`, `takeoverHandler.test.ts`, `webhookRepoResolver.test.ts`, `pushBranch.integration.test.ts`, `gitContextSharedWorld.ts`, `feature-797/810/819.steps.ts`) | `@paysdoc/devplatform/git` |
| anything under `adws/cost/providers/**` | **unchanged** |

- Rules: drop `.ts` suffixes (the step files use `'../../../adws/providers/types.ts'`-style paths); preserve `import type` vs value imports exactly (`Platform` and `BoardStatus` are enum values; `RepoIdentifier`, `BoundProviders`, `IssueTracker`, `CodeHost`, `GitIdentity`, `TokenProvider`, `ExecFn`, `FsDeps`, `GitContextOptions`, `LogSinceOptions`, `ForgeProvidersOptions`, `ForgeProviderDeps`, `GitHubForgeDeps`, `RawPR`, `CreatePROptions`, `PullRequestSummary` … are types); merge the duplicate lines a file ends up with (e.g. `launchGitContext.test.ts`, `forgeWiring.test.ts`, `feature-796/797/819.steps.ts`), keeping a separate `import type` line where the file already had one; `adws/vcs/worktreeCleanup.ts:8` becomes `export { killProcessesInDirectory } from '@paysdoc/devplatform/git';`; `adws/triggers/trigger_webhook.ts:94` becomes `import('@paysdoc/devplatform').CodeHost`; `adws/core/__tests__/targetRepoManager.test.ts:17` becomes `vi.mock('@paysdoc/devplatform/git', () => ({ … }))`.
- Delete the "Deep imports only — never the `../providers` barrel … import cycle (#792)" comment in `launchGitContext.ts` and the matching sentence in `forgeWiring.ts`'s docblock; repoint the stale path mentions in `localRepoIdentity.ts`'s docblock (`adws/providers/github/githubIdentity.ts`, `adws/providers/workspaceValidation.ts` → the library), `sshCloneUrl.ts:3`, `workspaceTrust.ts:13`, `issueRecord.ts:6`.
- Check: `git grep -nE "from ['\"][^'\"]*(gitContext|adws/providers)" -- '*.ts' '*.tsx'` and the inventory grep must print nothing outside `adws/gitContext/` and `adws/providers/` (which still exist until Step 6); `git diff --stat -- adws/cost` must be empty; `bunx tsc --noEmit` is expected to fail only in `launchGitContext.ts`/`launchGitContext.test.ts` until Step 5 lands.

### Step 5: Repoint the launch boundary at `createForgeCredentials` and expose the env-read App config
- `adws/core/githubAppAuth.ts`: import `isGitHubAppConfigured`/`getInstallationToken` (aliased) and `type GitHubAppConfig` from `@paysdoc/devplatform/providers`; rename the private `readAppConfig` to an **exported** `readGitHubAppConfig(): GitHubAppConfig` (same three `process.env` reads, fresh on every call); keep `isGitHubAppConfigured()` and `getInstallationToken(owner, repo)` as thin wrappers over it (still used by `forgeWiring.ts`, `adws/core/index.ts:162` and the three mocks). Docblock: the one place ADW reads `GITHUB_APP_ID`/`GITHUB_APP_SLUG`/`GITHUB_APP_PRIVATE_KEY_PATH`; the launch boundary hands the result to `createForgeCredentials`; the library never reads the host environment (PRD story 9). Drop the "moved verbatim from `adws/github/…`" history.
- `adws/core/launchGitContext.ts` — imports become `GitContext` (+ `type GitIdentity, TokenProvider`) from `@paysdoc/devplatform/git`; `Platform` (+ `type BoundProviders, RepoIdentifier`) from `@paysdoc/devplatform`; `createForgeCredentials, forgeProviders` (+ `type ForgeCredentials, ForgeCredentialsOptions, ForgeSelection, ForgeProviderDeps, ForgeProvidersOptions`) from `@paysdoc/devplatform/providers`; `readGitHubAppConfig` from `./githubAppAuth`. Remove `resolveBootstrapGitIdentity`, `ghAuthToken`, `resolveContextToken`, `createGitHubTokenProvider`, `isGitHubAppConfigured`, `getInstallationToken` imports. Then:
  - Add `const LAUNCH_CREDENTIAL_FORGE: ForgeSelection = { codeHost: 'github', issueTracker: 'github' };` — ADW's launch credentials are GitHub-keyed by convention, the same convention as `readLocalRepoIdentity`'s `platform: Platform.GitHub`; provider forge selection still comes from `.adw/providers.md` at first `.providers` access (deferred, unchanged).
  - Add `export function launchCredentialsOptions(identity: RepoIdentifier): ForgeCredentialsOptions` returning `{ forge: LAUNCH_CREDENTIAL_FORGE, identity, deps: { github: { appConfig: readGitHubAppConfig(), pat: GITHUB_PAT, alternateIdentityPat: GITHUB_PAT } } }` — the one place ADW's environment reaches the library's credential factory; `alternateIdentityPat = GITHUB_PAT` preserves the #819 parity (`'alternateIdentity'` requests get the PAT, `'default'` requests resolve App token → PAT → `gh auth token`).
  - Add `forgeCredentials?: (options: ForgeCredentialsOptions) => ForgeCredentials;` to `LaunchGitContextDeps` ("mints the launch `TokenProvider` and bootstrap `GitIdentity` for whatever the seams above did not supply; defaults to the library's `createForgeCredentials()`; deliberately a property-access seam, unflagged by the construction rule"). Every other field of `LaunchGitContextDeps` is unchanged (PRD story 29).
  - Add a private `resolveLaunchCredentials(repoId, deps): { tokenProvider: TokenProvider; gitIdentity: GitIdentity }`: `injectedProvider = deps.tokenProvider ?? (deps.resolveToken ? tokenProviderFromResolver(deps.resolveToken) : undefined)`, `injectedIdentity = deps.resolveGitIdentity?.()`; if both are present return them (no environment or `git config` read — this is what every existing `baseDeps()` test relies on); otherwise `minted = (deps.forgeCredentials ?? createForgeCredentials)(launchCredentialsOptions(repoId))` and return `{ tokenProvider: injectedProvider ?? minted.tokenProvider, gitIdentity: injectedIdentity ?? minted.gitIdentity }`. Guard clauses first, nesting ≤ 2.
  - In `buildLaunchBoundary`: resolve `{ owner, repo }` and `repoId` first (identity still read exactly once), then `const { tokenProvider, gitIdentity } = resolveLaunchCredentials(repoId, deps);`, then `new GitContext({ owner, repo, selfHost, tokenProvider, gitIdentity, frameworkRepoRoot, targetReposDir }, { logger: log })`. The lazy, memoised provider mint and `forgeProviders`/`forgeDeps` seams are untouched.
  - Delete `resolveLaunchToken`, `resolveLaunchGitIdentity` and `createLaunchTokenProvider` (dead once the factory owns resolution) and drop the first two from `adws/core/index.ts:204` (`export { buildLaunchGitContext, buildLaunchBoundary } from './launchGitContext';`).
  - Rewrite the module docblock as: ADW's wiring layer over `@paysdoc/devplatform` — the one place identity is resolved, credentials are minted (`createForgeCredentials`) and the one `GitContext` plus its bound provider triple are constructed; the only sanctioned construction site. No "lives in adws/core (not adws/gitContext)", no "#700 absorbed" history.
- `adws/core/__tests__/launchGitContext.test.ts`: replace the `githubAppAuth` mock with a hoisted `mockReadGitHubAppConfig = vi.fn(() => ({}))` exposed as `readGitHubAppConfig`; replace §7b with (1) **options threading** — `mockReadGitHubAppConfig.mockReturnValue({ appId: '1', appSlug: 'bot', privateKeyPath: '/k' })`, build with `baseDeps({ resolveToken: undefined, resolveGitIdentity: undefined, forgeCredentials: recorder })` where `recorder` records its argument and returns `{ tokenProvider: createLiteralTokenProvider(TEST_TOKEN), gitIdentity: TEST_IDENTITY }`; assert exactly one call whose `forge` is `{ codeHost: 'github', issueTracker: 'github' }`, whose `identity` is `{ owner: 'acme', repo: 'webapp', platform: Platform.GitHub }` and whose `deps.github` is `{ appConfig: { appId: '1', appSlug: 'bot', privateKeyPath: '/k' }, pat: 'pat-xyz', alternateIdentityPat: 'pat-xyz' }`; (2) **PAT parity through the real factory** — `mockReadGitHubAppConfig.mockReturnValue({})`, `forgeCredentials: (o) => createForgeCredentials({ ...o, deps: { ...o.deps, env: { GIT_AUTHOR_NAME: 'ADW Test Bot', GIT_AUTHOR_EMAIL: 'bot@test.dev', GIT_COMMITTER_NAME: 'ADW Test Bot', GIT_COMMITTER_EMAIL: 'bot@test.dev' } } })` (the library's forge-neutral bootstrap-identity seam, so no `git config` spawns; confirm the exact variable names against `readEnvGitIdentity` in `node_modules/@paysdoc/devplatform/dist/git/bootstrapIdentity.js`), then `ctx.commandEnv({}, 'alternateIdentity').GH_TOKEN === 'pat-xyz'` and `ctx.commandEnv({}, 'default').GH_TOKEN === 'pat-xyz'` (App unconfigured → PAT); (3) **seams win** — with `baseDeps()` (both seams injected) a throwing `forgeCredentials` is never called. Keep §1–§7 and §8–§14 as they are apart from the import rewrite.

### Step 6: Delete the extracted directories and the tests that read them as text
- `git rm -r adws/gitContext adws/providers` (run it as its own command — see the tooling note above).
- `adws/triggers/__tests__/devServerJanitor.test.ts`: delete the two tests at lines 681–699 (`killProcessesInDirectory in gitContext/processCleanup.ts sends SIGTERM before SIGKILL`, `… sends SIGKILL only to survivors after SIGTERM`) — they read library source that now lives in `paysdoc/devplatform`, where its own suite covers it.
- Re-run the two greps from Step 4 over `*.ts`, `*.tsx` **and** `*.feature` under `features/regression/`: silent. `test ! -d adws/gitContext && test ! -d adws/providers`.

### Step 7: Guard surgery — empty exempt set, no extraction rule, one sanctioned site, name-based rules proven on imported names
- `adws/checkGitGhGuard.ts`: `export const EXEMPT_PACKAGES: ReadonlyArray<{ readonly dir: string; readonly role: string }> = [];` (typed so `isExemptPackage` and the tests' `.map((p) => p.dir)` still compile) with a docblock saying the set is deliberately empty because the git core and the GitHub adapter now live in `@paysdoc/devplatform`, and **nothing may ever be added back**; keep `isExemptPackage()` and `pruneExemptPackages` (now only `EXEMPT_DIR_NAMES` prunes — adjust its comment); delete the `flagFrameworkImports`/`EXTRACTION_SCOPE` import, `printExtractionScope` import, `descendIntoScope`, `collectScopeEntry`, `collectExtractionScopeFiles`, `scanExtractionScope`, the `extraction` half of `main()`, the second PASS line and the extraction remedy; `main()` prints `Exempt packages: none — no in-repo code may shell out to git or gh` when the array is empty (derive from `EXEMPT_PACKAGES.length`, no hard-coded `(2)`); the shell-out remedy becomes "route through `GitContext` (`@paysdoc/devplatform/git`) or the forge ports — no in-repo package is exempt"; docblock: three rules, exempt set empty. Stays under 300 lines.
- Delete `adws/guard/extractionRule.ts` and its two tests; in `adws/guard/guardReport.ts` delete `printExtractionScope` and the `EXTRACTION_SCOPE` import; in `adws/guard/violationTypes.ts` drop `'extraction-readiness'` from `ViolationRule` and `adws/guard/extractionRule.ts` from the docblock ("three independent AST rules").
- `adws/guard/constructionRule.ts`: `SANCTIONED_CONSTRUCTION_SITES` keeps only the `adws/core/launchGitContext.ts` entry (still PERMANENT, no `owner`, so `findStaleSanctionedEntries` is unaffected); rewrite the docblock paragraph at lines 23–27 ("`adws/providers/github/**` … never reach this rule at all … `isExemptPackage`") and the `SANCTIONED_CONSTRUCTION_SITES` docblock (lines 85–95) to state that the flagged callees (`createGitHub*`, `createGitLab*`, `createJira*`, `forgeProviders`, `new GitContext`) are now names **imported from `@paysdoc/devplatform/providers` and `/git`**, matched by identifier text, and that the launch boundary is the only place they may be called (PRD story 24). Keep every name set unchanged. (This file legitimately still contains the `createGitHub…` strings — see Validation.)
- `adws/guard/identityRule.ts`: no logic change; add one docblock sentence recording the known, unchanged limitation that an **aliased** import (`import { GitContext as GC }` / `forgeProviders as fp`) evades both name-based rules.
- `.github/workflows/git-cli-guard.yml`: job name (line 9) → `Fail on direct git/gh shell-outs, cwd-derived identity, or ad-hoc construction`; step name (line 22) → `Run Git/GH CLI guard (3 rules)`.
- `adws/__tests__/checkGitGhGuard.test.ts`:
  - drop the `scanExtractionScope` import and the whole `scanExtractionScope — extraction-readiness rule (#816)` describe (lines 557–735);
  - `EXEMPT_PACKAGES` describe (285–299) → asserts `toHaveLength(0)`; `isExemptPackage` describe (301–333) → false for `adws/gitContext`, `adws/gitContext/appAuth.ts`, `adws/providers/github`, `adws/providers/github/ghCommandRunner.ts` and the existing negative cases;
  - `a third-package gh call site fails the guard` (335–348): replace the "counterpart is exempt" case with two cases from spec Step 8 — `execSync('gh issue view 1')` at `adws/providers/github/bar.ts` and `execSync('git status')` at `adws/gitContext/foo.ts` each yield exactly one `git-gh-shellout` violation from `scanFiles` (the "guard exempt set empty" acceptance criterion in test form);
  - construction describe: the case at line 459 (`scanFiles(['adws/providers/forgeProviders.ts'])` not flagged) becomes "is flagged" (one `unsanctioned-construction` violation); `isSanctionedConstructionSite` describe (515–541): `SANCTIONED_CONSTRUCTION_SITES` has length 1, `adws/providers/forgeProviders.ts` → false;
  - add the two imported-name cases: source `import { forgeProviders } from '@paysdoc/devplatform/providers';\nconst p = forgeProviders({ identity: readLocalRepoIdentity() });\n` at `adws/phases/someNewPhase.ts` → exactly one `cwd-derived-identity` violation; source `import { GitContext } from '@paysdoc/devplatform/git';\nconst ctx = new GitContext(opts);\n` at `adws/phases/someNewPhase.ts` → exactly one `unsanctioned-construction` violation, and the identical source at `adws/core/launchGitContext.ts` → zero;
  - `guarded factory names still exist` (736–768): the table now reads the **published declarations** through the real `fs` from `node_modules/@paysdoc/devplatform/dist/` — `forgeProviders` in `providers/forgeProviders.d.ts` (`/export declare function forgeProviders\(/`), `readLocalRepoInfo` in `providers/github/githubIdentity.d.ts` (`/export declare function readLocalRepoInfo\(/`), the seven `create*` factories in `providers/github/index.d.ts`, `providers/gitlab/index.d.ts`, `providers/jira/index.d.ts` (`/\bcreateGitHubIssueTracker\b/` etc.), `GitContext` in `git/index.d.ts` (`/export \{ GitContext \} from/`); `readLocalRepoIdentity` keeps `adws/core/localRepoIdentity.ts`. Update the failure hint to name `PROVIDER_CONSTRUCTORS`/`CONTEXT_CONSTRUCTORS`/`GIT_CONTEXT_CLASS_NAME`/`CWD_DERIVED_IDENTITY_FNS` only.

### Step 8: Repoint the BDD layer
- `features/regression/upgrade/feature-729.feature`: line 24 `THE FIX (\`adws/gitContext/commitOps.ts\`)` → `THE FIX (\`commitOps\` in \`@paysdoc/devplatform/git\` — \`src/git/commitOps.ts\` in the library repo)`; line 36's `\`adws/gitContext/commitOps.ts\`` → `the library's \`commitOps\` (\`@paysdoc/devplatform/git\`)`; line 81 `under \`adws/gitContext/__tests__/\`` → `in \`paysdoc/devplatform\`'s own suite`. No `Given`/`When`/`Then` phrase changes (frozen regression vocabulary).
- `features/regression/step_definitions/feature-729.steps.ts:19` → `import { commitOps } from '@paysdoc/devplatform/git';`; `pythonFixtureE2ESteps.ts:34` → `import { Platform } from '@paysdoc/devplatform';`. The 13 per-issue step files were rewritten in Step 4 (`feature-796.steps.ts` keeps `GitHubCodeHost`/`GitHubIssueTracker` and `feature-819.steps.ts` keeps `createGitHub*` — test fixtures may import forge-named symbols; both are exported by `/providers`).
- `grep -rn "adws/gitContext\|adws/providers" features/regression/` must print nothing.

### Step 9: Retire, rewrite and refresh the docs (44 entries, band `[25, 60]`)
- `.adw/conditional_docs.md`: delete the `app_docs/feature-9gjajh-providers.md` entry (lines 259–277 plus its trailing blank line); in the `oqb76h` entry remove the `adws/gitContext/**` glob, rewrite the conditions at (origin/dev numbering) 441 ("GitContext base-path resolution … in `adws/gitContext/`" → "… now `@paysdoc/devplatform/git`; the git core and the forge adapters live in `paysdoc/devplatform`"), 444 (four rules → three, no extraction-readiness), 450 (`createLaunchTokenProvider`'s alternate-identity PAT → `launchCredentialsOptions` hands `alternateIdentityPat = GITHUB_PAT` to `createForgeCredentials`), delete 446, 447, 448 and 453 outright, and add `When a \`@paysdoc/devplatform\` symbol is missing or misbehaving — the git core and the forge adapters live in \`paysdoc/devplatform\`; ADW pins an exact version (1.1.0) and Dependabot proposes bumps` plus `When \`buildLaunchBoundary\`'s \`forgeCredentials\` seam or \`launchCredentialsOptions\` (\`adws/core/launchGitContext.ts\`) are relevant — the launch TokenProvider and bootstrap GitIdentity come from the library's \`createForgeCredentials\`, fed ADW's env-read App config from \`readGitHubAppConfig\` (\`adws/core/githubAppAuth.ts\`)`; repoint line 162 (`readLocalRepoInfo` "still lives in `adws/providers/github/githubIdentity.ts`" → library-internal in `paysdoc/devplatform`) and line 301 (`adws/providers/github/domain/` → `@paysdoc/devplatform/providers`); replace this branch's stale rename condition (the one mentioning "renamed to `@paysdoc/devplatform` on 2026-09-10") with one stating the package is published and pinned. Final grep for `adws/gitContext`/`adws/providers` in the file: empty.
- `app_docs/feature-oqb76h-gitcontext-base-path-authority.md`: rewrite the "`adws/gitContext/` — the core package" section (lines 5–19) as "the git core — `@paysdoc/devplatform/git`", pointing at the library repo for internals; drop the `extraction-readiness` rule bullet (57), the `EXTRACTION_SCOPE` bullets (72, 98) and every "four rules"/"two-entry `EXEMPT_PACKAGES`"/"`adws/providers/forgeProviders.ts` is a sanctioned site" statement (53, 70, 71, 80, 93); replace the stale `No physical npm package …` bullet (87) with `**Consumed from \`@paysdoc/devplatform\` (exact pin 1.1.0)** — the git core is \`@paysdoc/devplatform/git\`, the forge ports and domain model are the root entry point, \`forgeProviders()\`/\`createForgeCredentials()\` and the adapters are \`@paysdoc/devplatform/providers\`; the guard's exempt set is empty and the launch boundary is the only sanctioned construction site`; describe `readGitHubAppConfig` at bullet 28.
- `app_docs/feature-9gjajh-github-api.md` lines 9, 11, 19, 40, 41, 43 and `app_docs/feature-9gjajh-types.md` line 19: repoint `adws/providers/…`/`adws/gitContext` mentions at the library entry points; line 41's "exactly two PERMANENT entries" → one. Keep both docs (their `Owns:` globs are live).
- `README.md`: rewrite bullets at lines 17 (GitContext is consumed from `@paysdoc/devplatform/git`), 18 (three rules, empty exempt set, one-entry allowlist, no `extractionRule.ts`), 22 (`forgeProviders()` from `@paysdoc/devplatform/providers`), the tour entry at 137, and the `adws/providers/repoContext.ts` mentions on tree lines 583, 613, 638, 650; delete the `├── gitContext/` block (658–688) and the `├── providers/` block (808–886) — this also removes the branch's stale test-listing hunk — and the `extractionRule*.test.ts`/`extractionRule.ts` tree lines (989, 990, 993); fix 995 (one PERMANENT entry) and 996 ("composes the two rule modules"). Add one sentence near line 17 that the git core and the forge adapters are consumed from `@paysdoc/devplatform` 1.1.0.
- `adws/README.md:690` → "assembles providers through `@paysdoc/devplatform/providers`' `forgeProviders()`"; `.adw/project.md:15` — replace the `adws/providers/**` Relevant-Files entry (and the stale `adws/github/**` one) with `@paysdoc/devplatform` (git core, forge ports, adapters — pinned dependency, not in-tree); `UBIQUITOUS_LANGUAGE.md:75` — `(\`adws/gitContext/\`)` → `(\`@paysdoc/devplatform/git\`)`.
- Note for the operator (PR body, Step 10): `.adw/**` edits change ADW's content hash and trip the upgrade gate for registered target repos on their next workflow — expected and mandated by the issue.

### Step 10: Validate, then restate the operator gate in PR #843
- Run every command under Validation until green; also read the diff for imports left unused by the consolidation and for any docblock that still names a file that no longer exists in this repo (`git grep -n "adws/gitContext\|adws/providers" -- adws app_docs README.md .adw UBIQUITOUS_LANGUAGE.md 'features/regression/'` must be empty).
- PR #843 body (`gh pr edit 843 --body-file …`, keep it a draft, no `Closes`/`Fixes` line — the issue is `hitl`): replace the blocker preamble with the migration summary and spec Step 13's operator gate — runbook step 7.4 is the operator's (run the full suite locally, trigger one live smoke workflow from this branch through PR creation, tag the pre-merge commit `pre-gitcontext-switchover` and push the tag, then approve), the rollback path is runbook step 8 (hand-revert of the merge commit on `dev` and `main`; ADW runs on the library after this lands and cannot repair its own switch), the dependency is pinned at `1.1.0` with Dependabot owning bumps, and the `.adw/**` content-hash note above.

## Validation
Execute every command to validate the patch is complete with zero regressions.

- `bun install` — exit 0; `node -e "const p=require('./package.json');console.log(p.dependencies['@paysdoc/devplatform'])"` prints `1.1.0`; `test -d node_modules/@paysdoc/devplatform`.
- `bun run lint` — exit 0 (catches imports left unused by the rewrite).
- `bunx tsc --noEmit` and `bunx tsc --noEmit -p adws/tsconfig.json` — both exit 0 (every rewritten specifier resolves through the `exports` map; `test/mocks/gitContextFixture.ts` and the `features/**` step files are inside the root include).
- `bun run test:unit` — 0 failures; **134 test files** (172 − 36 − 2; state the number actually printed in the report).
- `bun run lint:git-guard` — prints `Exempt packages: none — no in-repo code may shell out to git or gh`, `Sanctioned construction sites — 1 permanent, 0 sunset:` listing only `adws/core/launchGitContext.ts`, no `Extraction-readiness` section, the `(0 allowlisted)` capstone, and PASS (exit 0).
- `bun run lint:docs-index` — all checks pass: 44 entries, no dangling entry, no orphaned doc, no overlapping `Owns:` glob, count inside `[25, 60]`.
- `NODE_OPTIONS="--import tsx" bunx cucumber-js --tags "@regression"` — 0 failed, no load errors (the per-issue step files import cleanly); pending/undefined counts unchanged from the pre-migration baseline (10 passed / 0 failed / 42 pending / 1 undefined last cycle).
- `git grep -nE "from ['\"][^'\"]*(gitContext|adws/providers)" -- '*.ts' '*.tsx'` — prints nothing; the inventory grep from "Verified" prints nothing; `git grep -c "providers/anthropic" -- '*.ts' '*.tsx'` still reports the four untouched specifiers and `git diff origin/dev --stat -- adws/cost` is empty.
- `test ! -d adws/gitContext && test ! -d adws/providers && echo "directories gone"` — prints `directories gone`.
- `git grep -lE "createGitHub|createGitLab|GitLabApiClient|JiraApiClient|JiraIssueTracker" -- adws ':!**/__tests__/**'` — prints exactly one line, `adws/guard/constructionRule.ts` (the rule's flagged-name string set; it imports nothing). The import form, `git grep -nE "import[^;]*\b(createGitHub|createGitLab|GitLabApiClient|JiraApiClient|JiraIssueTracker)" -- adws ':!**/__tests__/**'`, prints nothing.
- `grep -rn "adws/gitContext\|adws/providers" features/regression/ .adw/conditional_docs.md app_docs/ README.md adws/README.md .adw/project.md UBIQUITOUS_LANGUAGE.md` — prints nothing.

## Patch Scope
**Lines of code to change:** ~330 import/mock specifier lines across 145 files (mechanical), ~120 lines in `launchGitContext.ts`/`githubAppAuth.ts`/their tests, ~200 lines removed from the guard and its tests plus ~60 added (imported-name cases, published-declaration table), 74 new fixture lines, two directories (~130 files, 36 test suites) and three guard files deleted, ~150 doc lines rewritten or removed.
**Risk level:** high — one atomic switch of every git/gh path onto an external package; behaviour is meant to be identical (same resolution order, same seams, same identity threading), and the operator smoke workflow in runbook step 7.4 is the live proof.
**Testing required:** the full Validation list above (both typechecks, lint, 134-file unit suite, guard, docs-index, `@regression`, the three greps, both directories gone) plus a manual read of the `launchGitContext.ts` diff against the issue body's launch-boundary rule.

## Notes
- **Per-issue `.feature` narrative is out of scope.** `features/per-issue/feature-810/812/816/817/818/819/821/823/844/797.feature` mention `adws/gitContext`/`adws/providers` in ~200 narrative lines; they are the frozen records of their issues and are not part of the acceptance criteria (the `@regression` suite is). Their step-definition **imports** are rewritten in Step 4 because cucumber loads them on every run. Scenarios of feature-816 (extraction-readiness output) and feature-819/823 (the two-entry exempt set / two sanctioned sites) now describe deleted behaviour and will read red when the per-issue sweep runs them — record that in the PR body as expected, not as a regression.
- **Behaviour deltas to state, not hide:** `createForgeCredentials` decides "App configured" once at boundary construction (the old `createLaunchTokenProvider` re-read `GITHUB_APP_*` per command); the bootstrap identity is now derived from the same env-read config the token uses (the in-tree resolver read the env itself). Both are the issue body's mandated shape.
- **`adws/guard/constructionRule.ts` is the one legitimate hit** of the #840 forge-name grep: the rule that forbids calling `createGitHubIssueTracker` must spell the name. Do not obfuscate the name set to silence the grep; report the single expected line instead.
- `.adw/coding_guidelines.md` applies throughout: guard clauses first, max nesting ~2, no `any`, JSDoc on public surfaces, remove unused imports, files under 300 lines (`checkGitGhGuard.ts` shrinks; `launchGitContext.ts` stays under the cap).
