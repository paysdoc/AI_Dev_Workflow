# Patch: Gate still closed (third review) — refresh PR #843's stale blocker preamble, hand back the library release sequence, change nothing in-repo

## Metadata
adwId: `i7hs7a-adw-switchover-to-pa`
reviewChangeRequest: `Issue #1: The migration was not performed; none of the spec's acceptance criteria are met. Verified on HEAD 136c288d (three docs-only commits past base 4d25d21c: spec 7188098b, README d5fd43a1, patch plan 136c288d; zero code changes): adws/gitContext/ (18 modules) and adws/providers/ are present and 117 import lines in 70 files outside them still resolve into them; package.json has no @paysdoc/devplatform and node_modules/@paysdoc does not exist; bun run lint:git-guard prints 'Exempt packages (2)', 'Sanctioned construction sites — 2 permanent' and 'Extraction-readiness scope — 10 entries'; adws/guard/extractionRule.ts and both its tests exist; features/regression/upgrade/feature-729.feature lines 24, 36 and 81 still cite adws/gitContext; .adw/conditional_docs.md has 14 extracted-path references and app_docs/feature-9gjajh-providers.md still exists. Root cause is unchanged from the two previous reviews: the spec's Step 1 gate is closed. npm view @paysdoc/devplatform versions returns ["1.0.0"] (published 2026-09-10T13:28Z, nothing since), and the packed 1.0.0 tarball reports MISSING for providers:createForgeCredentials, createGitHubTokenProvider, resolveBootstrapGitIdentity, resolveContextToken, ghAuthToken, isGitHubAppConfigured, getInstallationToken, createGhRepoApi and for git:createLiteralTokenProvider, commitOps, branchOps, isLeaseRejection. On the library side: main is cba596b8 (2026-09-14, PR #12 docs-index sweep), tags are v1.0.0 and v1.1.0; the 2026-09-14 release run 34829911126 succeeded but released nothing ('Found git tag v1.1.0 … There are no relevant changes, so no new version is released'), which means semantic-release already treats the never-published v1.1.0 (run 34503029087 failed at npm publish with 403 OIDC permission denied) as released and will not retry it automatically. devplatform#11 (L4 widening) is OPEN, labelled adw:feature and adw:unverified, has no PR, and its ADW pipeline (k3pj1m-widen-the-public-sur) errored on 2026-09-22T08:42:56Z resuming from pr_creating because specs/issue-11-plan.md is missing in its worktree. Spec Step 1 forbids re-implementing the missing helpers in ADW because that recreates the git/gh shell-out sites the empty exempt set must forbid, so this is not patchable in-repo; the earlier patch cycles already did everything possible on the ADW side (PR #843 is a draft, closing keywords removed, devplatform#11 filed).
Resolution: Operator actions outside this repository, in order: (1) fix the npm trusted-publisher (OIDC) grant for paysdoc/devplatform's release.yml so a publish can succeed; (2) because semantic-release now treats v1.1.0 as released, a new releasable (feat:/fix:) commit on library main is required to cut a version above 1.0.0 — the devplatform#11 widening is the natural one, so restart its stalled pipeline (## Retry on devplatform#11 or open the PR by hand) and merge it, then confirm with `npm view @paysdoc/devplatform versions` and the spec's Step 1 tarball check that the /providers barrel exports createForgeCredentials (plus createGhRepoApi for feature-797 steps) and the /git barrel exports createLiteralTokenProvider, commitOps and branchOps; (3) rebase this branch onto origin/dev (#844 changed the consumer inventory; git merge-tree is conflict-free today); (4) re-run the build from spec Step 2 pinning the exact published version and following the updated #840 body for the launch boundary (createForgeCredentials({ forge, identity, deps }); no production module imports a forge-named symbol); (5) re-review against all acceptance criteria and the spec's Validation Commands. Until (1) and (2) are done, keep PR #843 a draft and do not commit code on this branch.`

## Issue Summary
**Original Spec:** specs/issue-840-adw-20l8es-adw-switchover-to-pa-sdlc_planner-devplatform-switchover.md
**Issue:** The switchover is still unbuilt because spec Step 1's gate is still closed: npm carries only `@paysdoc/devplatform@1.0.0`, whose barrels lack `createForgeCredentials` (`/providers`) and `createLiteralTokenProvider` (`/git`) — the gate the rewritten #840 body names — and the spec bans the only in-repo workaround (re-implementing the helpers in ADW). Two things changed since the second review and both make the prerequisite chain longer than PR #843's body currently says: (a) the library's 2026-09-14 release run found tag v1.1.0 and released nothing, so semantic-release now treats the never-published v1.1.0 as done and the npm OIDC fix alone will publish nothing — a new `feat:`/`fix:` commit on library `main` is required; (b) the devplatform#11 widening pipeline has errored three times resuming from `pr_creating` because its worktree branch has no plan file and no implementation commits, so a bare `## Retry` cannot succeed. On top of that, PR #843's body still carries the **first** cycle's preamble (the second cycle's body refresh was never applied): it names "#11 merged and published" as the only prerequisite, omits the updated #840 gate, and its checklist still says `@paysdoc/devplatform@1.0.0`.
**Solution:** Make no in-repo change (0 source files, 0 code commits — this plan file is the only artifact). Re-verify the gate, then rewrite PR #843's blocker preamble so it states the complete, current prerequisite chain (OIDC grant **and** a new releasable library commit; #11's branch is empty so it needs a fresh run or a hand PR, not `## Retry`; the updated #840 gate and launch-boundary mapping; the conflict-free rebase). Leave the branch, tree and draft flag exactly as found and hand the operator sequence back in the report with the verified library-side facts.

### Verified state at patch-plan time (2026-09-22)
- **npm:** `npm view @paysdoc/devplatform versions --json` → `["1.0.0"]`; `time` shows 1.0.0 published 2026-09-10T13:28Z and nothing since; `latest` → 1.0.0. The packed 1.0.0 tarball reports `MISSING` for all twelve review-listed symbols (`providers:` `createForgeCredentials`, `createGitHubTokenProvider`, `resolveBootstrapGitIdentity`, `resolveContextToken`, `ghAuthToken`, `isGitHubAppConfigured`, `getInstallationToken`, `createGhRepoApi`; `git:` `createLiteralTokenProvider`, `commitOps`, `branchOps`, `isLeaseRejection`). Gate closed.
- **Library `main`:** `cba596b8` (2026-09-14, merge of PR #12 docs-index sweep). Tags: `v1.0.0` = `4634ee68`, `v1.1.0` = `ba168795`. On `main`, `src/providers/index.ts` already re-exports `./forgeCredentials.js` (`createForgeCredentials`) and `src/git/index.ts` already exports `createLiteralTokenProvider` — the updated #840 gate symbols exist in source but not on npm. `commitOps`, `branchOps`, `createGhRepoApi` and the other #11 symbols are **not** in the barrels on `main`.
- **Why nothing publishes:** release run `34503029087` (v1.1.0, 2026-09-10) computed "next release version is 1.1.0" and failed at `npm publish` with `E403 … OIDC permission denied`. Release run `34829911126` (2026-09-14) succeeded but logged `Found git tag v1.1.0 associated with version 1.1.0 on branch main` → `There are no relevant changes, so no new version is released`. semantic-release therefore treats v1.1.0 as released; fixing the trusted-publisher grant will not by itself publish anything. A new releasable (`feat:`/`fix:`) commit on `main` is required to cut the next version (1.2.0 by semantic-release's arithmetic; pin whatever `npm view` actually reports).
- **devplatform#11 (L4 widening):** OPEN, labels `adw:feature` + `adw:unverified`, no PR, no branch on the remote (only `main` and an old `adw-upgrade-…` branch). Its first pipeline (`obxxxx-widen-the-public-sur`) looped through Review Running → Paused → Resuming → "ADW Unverified — No JUnit Report Emitted" on 2026-09-11 and never produced a plan or a build. The operator posted `## continue` at 2026-09-22T08:25Z; pipeline `k3pj1m-widen-the-public-sur` then resumed from `pr_creating` three times (08:26, 08:34, 08:41Z) and errored each time (08:28:17, 08:35:28, 08:42:56Z) with `Cannot read plan file at …/.worktrees/feature-issue-11-widen-public-exports-adw-switchover/specs/issue-11-plan.md: ENOENT`. Verified read-only on this host: that worktree's branch is at `cba596b` = `main`, **zero commits ahead**, working tree `M README.md` only, and `specs/` holds plans for issues 1, 2, 3 and 9 but nothing for 11. Resuming from `pr_creating` re-enters the build phase, which reads the plan file first (`adws/phases/buildPhase.ts`), so every resume of this state fails identically. `## Retry` in this framework only re-arms `merge_blocked` / `human_gated` / `review_failed` and resumes from the recorded phase (`adws/triggers/retryHandler.ts`) — it will reproduce the ENOENT. Only a fresh run (`## Cancel`, which removes the worktree and re-queues the issue) or a hand-written PR can land the widening.
- **PR #843:** OPEN, `isDraft=true`, base `dev`, `mergeStateStatus=CLEAN`, 0 reviews, no auto-merge. Body = the **first** cycle's blockquote preamble (`Prerequisite: paysdoc/devplatform#11 merged **and published**`) + `## Summary` … + `Tracks #840 (not a closing reference — the migration has not been performed).` + checklist whose second line still reads `@paysdoc/devplatform@1.0.0`. The second cycle's planned refresh (`specs/patch/patch-adw-gz7jte-adw-switchover-to-pa-gate-closed-refresh-pr-843-preamble.md`, Step 2) was never applied to the PR.
- **Issue #840:** OPEN, `hitl` + `adw:chore`. Body rewritten since the spec: prerequisites #844 merged (done) and devplatform issue 9 released on npm; Step 1 gate = a version above 1.0.0 with `createForgeCredentials` in `/providers` and `createLiteralTokenProvider` in `/git`; launch boundary via `createForgeCredentials({ forge, identity, deps })`; rule that no production module imports a forge-named symbol; extra acceptance grep `git grep -lE "createGitHub|createGitLab|GitLabApiClient|JiraApiClient|JiraIssueTracker" -- adws ':!**/__tests__/**'` must print nothing.
- **This branch:** clean tree; HEAD `136c288d`; three docs-only commits past base `4d25d21c` (`7188098b` spec, `d5fd43a1` README, `136c288d` previous patch plan); 15 commits behind `origin/dev` (#844 / PR #845 and its follow-ups); `git merge-tree --write-tree origin/dev HEAD` exits 0 — the rebase in the re-run is conflict-free today.
- **Baseline on the untouched branch (2026-09-22):** `bunx tsc --noEmit` exit 0; `bunx tsc --noEmit -p adws/tsconfig.json` exit 0; `bun run test:unit` → 180 test files / 3193 tests, all passed.

## Files to Modify
Use these files to implement the patch:

- **None in this repository.** The spec is a write-once artifact (`app_docs/feature-9gjajh-specs-and-prd.md`: plan files are not updated after the build phase begins) and every in-repo change it describes is gated behind Step 1. Do not add, edit, delete, rebase or push anything on this branch; do not add the dependency; do not touch the two extracted directories, the guard, the feature file or the docs. The only file this cycle produces is this patch plan under `specs/patch/`, which the pipeline commits as docs (as it did for `136c288d`); that is not a code commit.
- GitHub state changed by this patch: `paysdoc/AI_Dev_Workflow` PR #843 — **body only** (draft flag is already set and must stay set).
- Scratch files written under `/tmp/` only: `/tmp/adw-i7hs7a-pr843-body-current.md`, `/tmp/adw-i7hs7a-pr843-body-v3.md`.

## Implementation Steps
IMPORTANT: Execute every step in order, top to bottom.

### Step 1: Re-verify the gate before touching anything
- Run `npm view @paysdoc/devplatform versions --json` and the tarball check. Leave the scratch directory in place under `/tmp` afterwards — the pre-tool-use hook blocks recursive deletes, and the directory is harmless:
  ```
  D=$(mktemp -d) && cd "$D" && npm pack @paysdoc/devplatform@latest --silent >/dev/null && tar xzf paysdoc-devplatform-*.tgz && node -e "(async()=>{const p=await import('$D/package/dist/providers/index.js');const g=await import('$D/package/dist/git/index.js');for(const s of ['createForgeCredentials','createGhRepoApi'])console.log((s in p?'present':'MISSING')+' providers:'+s);for(const s of ['createLiteralTokenProvider','commitOps','branchOps'])console.log((s in g?'present':'MISSING')+' git:'+s)})()"
  ```
- Expected today: versions `["1.0.0"]` and every line `MISSING`. Record the output for the patch report and continue to Step 2.
- **If instead every line prints `present`** (the gate opened between review and patch): STOP this patch without editing the PR. Report that the migration re-run is now due and must start with the rebase onto `origin/dev` and spec Step 2 pinning that exact version. Do not attempt the ~140-file migration inside this patch cycle — it needs the rebase first and is a `hitl` change the operator gates.

### Step 2: Rewrite PR #843's blocker preamble (keep it a draft; keep it non-closing)
- Confirm the draft flag: `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json isDraft --jq .isDraft` must print `true`. Only if it prints `false`, run `gh pr ready --undo 843 -R paysdoc/AI_Dev_Workflow`.
- Fetch the current body: `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json body --jq .body > /tmp/adw-i7hs7a-pr843-body-current.md`.
- Build `/tmp/adw-i7hs7a-pr843-body-v3.md` as: the preamble below, a blank line, then everything in the current body **from the `## Summary` heading onward** (this drops the first cycle's blockquote preamble, which the new one supersedes). In the retained part make exactly one edit — replace the checklist line:
  ```
  - [ ] `@paysdoc/devplatform@1.0.0` added as a dependency
  ```
  with:
  ```
  - [ ] `@paysdoc/devplatform` added as an exact-pinned dependency at the first published version above 1.0.0 that passes the Step 1 gate
  ```
  Keep `Tracks #840 (not a closing reference — the migration has not been performed).` verbatim; do not introduce any `Closes`/`Fixes`/`Resolves`/`Implements` line.
- Preamble (verbatim):

  ```
  > **BLOCKED at spec Step 1 — do not merge.** This PR carries only the plan spec; the migration has not been performed. Three reviews (adw `gz7jte-adw-switchover-to-pa` on 2026-09-10/11, adw `i7hs7a-adw-switchover-to-pa` on 2026-09-22) rejected it for that reason; the third re-confirmed the gate is still closed.
  >
  > **Gate (issue #840, updated body):** `npm view @paysdoc/devplatform versions` must list a version above 1.0.0 whose `/providers` barrel exports `createForgeCredentials` and whose `/git` barrel exports `createLiteralTokenProvider`. As of 2026-09-22 npm has only 1.0.0 (published 2026-09-10) and both symbols are missing from its tarball.
  >
  > **Why nothing publishes — and why the OIDC fix alone is no longer enough:** library tag v1.1.0 (`ba168795`) carries both gate symbols but never reached npm: release run 34503029087 failed at `npm publish` with `403 … OIDC permission denied`. The next release run (34829911126, 2026-09-14) succeeded but released nothing — semantic-release found tag v1.1.0 and reported "no relevant changes", so it now treats v1.1.0 as released and will not retry it. Opening the gate needs (1) the npm trusted-publisher grant for `release.yml` fixed **and** (2) a new `feat:`/`fix:` commit on library `main` to cut a version above 1.0.0.
  >
  > **The natural (2) is paysdoc/devplatform#11** (barrel widening: `commitOps`/`branchOps` from `/git` for the feature-729/feature-844 steps, `createGhRepoApi` from `/providers` for the feature-797 steps). It is OPEN (`adw:feature`, `adw:unverified`), has no PR and no remote branch. Its pipeline (`k3pj1m-widen-the-public-sur`) errored three times on 2026-09-22 resuming from `pr_creating` because its worktree branch sits at `main` with no plan file and no implementation commits — a bare `## Retry` resumes from the same phase and fails the same way. Either `## Cancel` it so a fresh run plans and builds it, or open the PR by hand (barrel re-exports plus the import-graph assertion that `./git` pulls no adapter module).
  >
  > **Re-run sequence once a qualifying version is on npm:** rebase this branch onto `dev` (#844 landed after it was cut; `git merge-tree` is conflict-free as of 2026-09-22), re-run the build from spec Step 2 pinning the exact published version and following the updated #840 launch-boundary mapping (`createForgeCredentials({ forge, identity, deps })`; no production module imports a forge-named symbol), then re-review against all acceptance criteria and the spec's Validation Commands.
  ```
- Apply: `gh pr edit 843 -R paysdoc/AI_Dev_Workflow --body-file /tmp/adw-i7hs7a-pr843-body-v3.md`.
- Do **not** close the PR (the re-run reuses this branch and adwId; ADW resolves the existing PR by branch name). Do **not** post on paysdoc/devplatform#11: `## Retry` cannot succeed against an empty branch, and `## Cancel` kills a process and removes a worktree in another repository — that is the operator's call, recorded in the PR body and the patch report instead.

### Step 3: Leave the branch exactly as found and hand back the operator sequence
- Verify nothing in-repo moved: `git status --short` prints nothing beyond this plan file (until the pipeline commits it); `git log --oneline 4d25d21c..HEAD` prints exactly `136c288d`, `d5fd43a1`, `7188098b` (plus, after the pipeline commits it, the docs-only commit adding this plan); `git diff --stat` is empty. No code commit, no rebase, no push from this patch.
- In the patch report, restate for the operator, in order: (1) fix the npm trusted-publisher (OIDC) grant for `paysdoc/devplatform`'s `release.yml`; (2) land a new releasable commit on library `main` — devplatform#11 is the natural one, but its branch is empty, so `## Cancel` it and let a fresh run plan/build it, or open the widening PR by hand (`feat:` prefix so semantic-release cuts a new minor); then confirm with `npm view @paysdoc/devplatform versions` and the Step 1 tarball check that `/providers` exports `createForgeCredentials` (+ `createGhRepoApi`) and `/git` exports `createLiteralTokenProvider`, `commitOps`, `branchOps`; (3) rebase this branch onto `origin/dev` (conflict-free today); (4) re-run the build from spec Step 2 pinning the exact published version and following the updated #840 body for `createForgeCredentials`; (5) re-review against all acceptance criteria and the spec's Validation Commands. Until (1) and (2) are done, no in-repo patch can satisfy this blocker and PR #843 stays a draft.

## Validation
Execute every command to validate the patch is complete with zero regressions.

- `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json isDraft,body --jq '"isDraft=\(.isDraft)", "preamble-updated=\(.body | contains("no relevant changes") and contains("createForgeCredentials") and contains("devplatform#11") and contains("k3pj1m") and contains("Tracks #840"))", "old-preamble-gone=\(.body | contains("Prerequisite: paysdoc/devplatform#11 merged") | not)", "checklist-updated=\(.body | contains("@paysdoc/devplatform@1.0.0") | not)"'` — expect `isDraft=true`, `preamble-updated=true`, `old-preamble-gone=true`, `checklist-updated=true`; additionally `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json body --jq .body | grep -ciE '^(closes|fixes|resolves|implements) '` must print `0`.
- `git status --short && git log --oneline 4d25d21c..HEAD && git diff --stat` — expect nothing tracked-and-modified, exactly the three docs commits `136c288d`, `d5fd43a1`, `7188098b` (plus the pipeline's commit of this plan file once made), and an empty diff.
- Baseline no-regression on the untouched branch (nothing changed, so these must stay as recorded): `bunx tsc --noEmit`, `bunx tsc --noEmit -p adws/tsconfig.json`, `bun run test:unit` — expect both typechecks exit 0 and 180 test files / 3193 tests green.
- Gate check (documenting, not passing — today it prints `MISSING` lines; the migration re-run starts only when it prints none): the Step 1 tarball command above, plus `npm view @paysdoc/devplatform versions --json` → `["1.0.0"]`.
- Rebase safety for the re-run: `git fetch -q origin dev && git merge-tree --write-tree origin/dev HEAD >/dev/null; echo "merge-tree exit=$?"` — expect `0`.
- The spec's own Validation Commands (lint, both typechecks, unit, `lint:git-guard`, `lint:docs-index`, `@regression`, the two greps, the directory check) are the exit criteria of the **re-run**, not of this patch; they cannot pass until the migration is built on a published library version.

## Patch Scope
**Lines of code to change:** 0 in-repo (≈14 lines of PR body preamble plus one checklist line, both outside the repository; this plan file is the only committed artifact)
**Risk level:** low — one reversible GitHub body edit on an already-draft PR; no code commits, no rebase, no push, no action on the library repository
**Testing required:** the GitHub-state verification above, the unchanged baseline typechecks and unit suite, and the documented gate and rebase-safety checks
