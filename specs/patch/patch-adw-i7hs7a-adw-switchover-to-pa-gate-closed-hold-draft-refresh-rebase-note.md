# Patch: Gate still closed (fifth review) — hold PR #843 as a non-closing draft, refresh its rebase note for the README.md conflict #847 introduced, change nothing in-repo

## Metadata
adwId: `i7hs7a-adw-switchover-to-pa`
reviewChangeRequest: `Issue #1: The migration was not performed; none of the spec's four acceptance criteria are met. Verified on HEAD a6f398a4 (five docs-only commits past merge-base 4d25d21c; git diff origin/dev...HEAD --name-only = README.md + five markdown files under specs/, zero code): adws/gitContext/ and adws/providers/ are present and 212 relative import lines outside them still resolve into them; package.json has no @paysdoc/devplatform and node_modules/@paysdoc does not exist; bun run lint:git-guard prints 'Exempt packages (2)', 'Sanctioned construction sites — 2 permanent, 0 sunset' (adws/providers/forgeProviders.ts still listed) and 'Extraction-readiness scope — 10 entries'; adws/guard/extractionRule.ts and both its tests exist and 'extraction-readiness' is still a ViolationRule member; features/regression/upgrade/feature-729.feature lines 24, 36 and 81 still cite adws/gitContext; .adw/conditional_docs.md has 14 extracted-path references and app_docs/feature-9gjajh-providers.md still exists. Root cause is unchanged from the four previous reviews: the spec's Step 1 gate is closed. As of 2026-09-22, npm view @paysdoc/devplatform versions returns ["1.0.0"] (published 2026-09-10T13:28Z, nothing since; exports map closed to ., ./providers, ./git, ./package.json) and the spec's Step 1 tarball check reports MISSING for all twelve required symbols (providers: createForgeCredentials, createGitHubTokenProvider, resolveBootstrapGitIdentity, resolveContextToken, ghAuthToken, isGitHubAppConfigured, getInstallationToken, createGhRepoApi; git: createLiteralTokenProvider, commitOps, branchOps, isLeaseRejection — only GitContext is present). On the library side, paysdoc/devplatform#11 (L4 widening) is OPEN (adw:feature, adw:unverified, last activity 2026-09-22T08:42:56Z), there are no open PRs and no GitHub releases; tag v1.1.0 exists but was never published (release run 34503029087 failed with 403 OIDC permission denied, and the 2026-09-14 run released nothing because semantic-release now treats v1.1.0 as released). Spec Step 1 forbids re-implementing the missing helpers in ADW because that recreates the git/gh shell-out sites the empty exempt set must forbid, so this is not patchable in-repo. The earlier patch cycles already did everything possible on the ADW side and their state holds: PR #843 is isDraft=true with autoMergeRequest=null, zero closing keywords, and the current date-proof blocker preamble ('Every review of this PR so far …', gate, OIDC/semantic-release explanation, devplatform#11 status, re-run sequence); git merge-tree against origin/dev is conflict-free. The untouched baseline is green: bunx tsc --noEmit exit 0, bunx tsc --noEmit -p adws/tsconfig.json exit 0, bun run lint exit 0, lint:git-guard PASS with pre-migration banners, lint:docs-index 47 entries all checks pass, scenario proof @regression 10 passed / 0 failed / 43 skipped. Resolution: Not patchable in-repo: make no code change, add no dependency, keep PR #843 a non-closing draft. Operator actions outside this repository, in order: (1) fix the npm trusted-publisher (OIDC) grant for paysdoc/devplatform's release.yml so a publish can succeed; (2) land a new releasable feat:/fix: commit on library main so semantic-release cuts a version above 1.0.0 (the OIDC fix alone publishes nothing because v1.1.0 is already treated as released) — paysdoc/devplatform#11 is the natural commit, but its pipeline branch is empty, so post ## Cancel on it for a fresh run or open the widening PR by hand (re-export commitOps/branchOps/isLeaseRejection from /git and createGhRepoApi from /providers, plus the import-graph assertion that ./git pulls no adapter module); then confirm with npm view @paysdoc/devplatform versions and the spec's Step 1 tarball check that /providers exports createForgeCredentials (+ createGhRepoApi) and /git exports createLiteralTokenProvider, commitOps and branchOps; (3) rebase this branch onto origin/dev (#844 changed the consumer inventory; conflict-free today); (4) re-run the build from spec Step 2 pinning the exact published version and following the updated #840 body for the launch boundary (createForgeCredentials({ forge, identity, deps }); no production module imports a forge-named symbol — git grep -lE "createGitHub|createGitLab|GitLabApiClient|JiraApiClient|JiraIssueTracker" -- adws ':!**/__tests__/**' must print nothing); (5) re-review against all acceptance criteria and the spec's Validation Commands. Until (1) and (2) are done, no in-repo patch can satisfy this blocker.`

## Issue Summary
**Original Spec:** specs/issue-840-adw-20l8es-adw-switchover-to-pa-sdlc_planner-devplatform-switchover.md
**Issue:** Fifth consecutive rejection for the same root cause: the switchover cannot be built because spec Step 1's gate is closed. npm still carries only `@paysdoc/devplatform@1.0.0`, whose barrels lack every symbol the migration needs (all twelve review-listed symbols are `MISSING` from the 1.0.0 tarball; only `GitContext` is present). The library cannot publish until the npm trusted-publisher (OIDC) grant is fixed **and** a new `feat:`/`fix:` commit lands on library `main` (semantic-release treats the never-published v1.1.0 as released). The natural commit, devplatform#11, is still stalled with no PR, no branch and no operator directive since its 2026-09-22T08:42:56Z pipeline error. The spec bans the only in-repo workaround (re-implementing the helpers in ADW re-creates the `git`/`gh` shell-out sites the empty exempt set forbids). The reviewer confirmed the fourth cycle's PR #843 state holds (draft, no auto-merge, date-proof preamble, zero closing keywords) and that the untouched baseline is green. **One fact in that handoff has gone stale since the review:** PR #847 merged into `dev` at 2026-09-22T09:13:57Z — 34 seconds after this review's "Review Running" comment and before its "Review Failed" — and its README.md edits collide with this branch's README commit `d5fd43a1`. `git merge-tree` against `origin/dev` now exits 1 with a single, docs-only conflict in `README.md`, GitHub reports `mergeable=CONFLICTING` / `mergeStateStatus=DIRTY`, and the PR body's re-run sentence still says "`git merge-tree` is conflict-free as of 2026-09-22".
**Solution:** Make no in-repo change (0 source files, 0 dependencies, 0 code commits, no merge, no rebase, no push; this plan file is the only committed artifact and the pipeline commits it as docs, as it did `136c288d`, `eee3d00c` and `a6f398a4`). Re-verify the gate, confirm PR #843 is still the non-closing draft the fourth cycle left it as, and make exactly one reversible GitHub-side edit: replace the re-run sentence's stale "conflict-free" parenthetical with an accurate description of the README.md conflict so the operator's step (3) is not a surprise. Do **not** resolve the conflict in this cycle: the resolution assigns the rebase to the operator as the first move of the re-run, the pipeline's auto-merge phase invokes `/resolve_conflict` itself when it eventually merges (`adws/phases/autoMergePhase.ts`, `adws/triggers/autoMergeHandler.ts`), the gate has no ETA so any merge/rebase done now would go stale again with every `dev` commit, and the conflict is trivially resolvable docs text that the re-run's rebase handles in seconds.

### Verified state at patch-plan time (2026-09-22, after the review)
- **npm:** `npm view @paysdoc/devplatform versions time exports --json` → versions `["1.0.0"]`; `1.0.0` published 2026-09-10T13:28:16Z, `modified` 2026-09-10T13:28:16Z, nothing since; exports map closed to `.`, `./providers`, `./git`, `./package.json`. `npm pack @paysdoc/devplatform@latest` yields `paysdoc-devplatform-1.0.0.tgz`; importing `package/dist/providers/index.js` and `package/dist/git/index.js` reports `MISSING` for all twelve review-listed symbols (`providers:` `createForgeCredentials`, `createGhRepoApi`, `createGitHubTokenProvider`, `resolveBootstrapGitIdentity`, `resolveContextToken`, `ghAuthToken`, `isGitHubAppConfigured`, `getInstallationToken`; `git:` `createLiteralTokenProvider`, `commitOps`, `branchOps`, `isLeaseRejection`) and `present` only for `git:GitContext`. Gate closed.
- **Library `paysdoc/devplatform`:** `main` = `cba596b8` (2026-09-14T09:48:31Z, merge of PR #12 docs-index sweep) — unchanged since the third cycle. Remote branches: `main` and one stale `adw-upgrade-…` branch only; no branch for issue 11. Tags `v1.0.0` (`af70575f`), `v1.1.0` (`ba168795`); `gh release list` empty; no open PRs. `release.yml` runs: `34829911126` (2026-09-14, success, released nothing), `34503029087` (2026-09-10, **failure** at `npm publish`, 403 OIDC permission denied), four earlier successes on 2026-09-10. Merged PRs: #12, #10 (`feat: #9 createForgeCredentials`), #8 — nothing after 2026-09-14.
- **devplatform#11 (L4 widening):** OPEN, labels `adw:unverified` + `adw:feature`, `updatedAt` 2026-09-22T08:42:56Z, 483 comments. Last three are all from pipeline `k3pj1m-widen-the-public-sur`: two "ADW Workflow Resuming" (08:41:32Z, 08:41:41Z) and the "ADW Workflow Error" (08:42:56Z). No operator directive (`## Cancel`, `## Retry`, `## continue`) has been posted since. No PR.
- **PR #843 (`paysdoc/AI_Dev_Workflow`):** OPEN, `isDraft=true`, base `dev`, head `chore-issue-840-adw-switchover-devplatform-library`, `autoMergeRequest=null`, `updatedAt` 2026-09-22T09:13:18Z. **`mergeable=CONFLICTING`, `mergeStateStatus=DIRTY`** (the fourth cycle recorded `CLEAN`; see the `origin/dev` item below). Body = the fourth cycle's date-proof preamble: all four body checks from the previous plan's validation are `true` (`v3-preamble-present`, `old-preamble-gone`, `checklist-updated`, `count-sentence-dateproof`), zero `Closes`/`Fixes`/`Resolves`/`Implements` lines. The re-run paragraph still reads "rebase this branch onto `dev` (#844 landed after it was cut; `git merge-tree` is conflict-free as of 2026-09-22)" — true when written, false since 09:13:57Z.
- **Issue #840:** OPEN, `hitl` + `adw:chore`. Last three comments are the pipeline's Review Failed (09:04:58Z), Review Running (09:13:23Z) and Review Failed (09:18:15Z) for this adwId. Issue #844 (the `## Blocked by` prerequisite) is CLOSED via merged PR #845.
- **`origin/dev`:** now `1ec1295c` = merge of PR #847 ("chore: #846 - Auto-trust target repo workspaces in ~/.claude.json at ensureTargetRepoWorkspace", merged 2026-09-22T09:13:57Z). This branch is 18 commits behind / 5 ahead; merge-base `4d25d21c` unchanged. `git merge-tree --write-tree origin/dev HEAD` exits 1 with exactly one `CONFLICT (content)` line, `README.md`; `README.md` is also the only path changed on both sides since the base. The conflict is two hunks in the `adws/` directory-tree listing: (a) under `adws/providers/__tests__/`, both sides add `forgeProviders.deps.test.ts`, `forgeProviders.selection.test.ts` and `forgeProvidersFixture.ts` next to `forgeProviders.test.ts` with different trailing comments (`dev` from #845, this branch from `d5fd43a1`); (b) under `adws/guard/__tests__/`, both sides add `extractionRule.integration.test.ts` in a different order/with a different comment. Either side is a correct README; nothing else conflicts.
- **This branch:** clean tree; HEAD `a6f398a4` = `origin/chore-issue-840-adw-switchover-devplatform-library`; five docs-only commits past base `4d25d21c` (`7188098b` spec, `d5fd43a1` README, `136c288d`, `eee3d00c`, `a6f398a4` patch plans); `git diff --name-only origin/dev...HEAD` = `README.md` + five markdown files under `specs/`, zero code.
- **In-repo acceptance facts (all still pre-migration):** `adws/gitContext/` and `adws/providers/` present; `package.json` has zero `@paysdoc/devplatform` references; `node_modules/@paysdoc` absent; the spec's import grep (`git grep -nE "from ['\"][^'\"]*(gitContext|adws/providers)" -- '*.ts' '*.tsx'`) prints 161 lines (must print nothing after the migration); `features/regression/upgrade/feature-729.feature` cites `adws/gitContext` at lines 24, 36, 81; `.adw/conditional_docs.md` has 14 `adws/gitContext`/`adws/providers` references; `app_docs/feature-9gjajh-providers.md`, `adws/guard/extractionRule.ts`, `extractionRule.test.ts` and `extractionRule.integration.test.ts` all exist; `'extraction-readiness'` is still a `ViolationRule` member (`adws/guard/violationTypes.ts:13`).
- **Baseline on the untouched branch (2026-09-22):** `bunx tsc --noEmit` exit 0; `bunx tsc --noEmit -p adws/tsconfig.json` exit 0; `bun run lint` exit 0; `bun run lint:git-guard` → PASS, 286 files scanned, with the pre-migration banners (`Exempt packages (2)`, `Sanctioned construction sites — 2 permanent, 0 sunset`, `Extraction-readiness scope — 10 entries`); `bun run lint:docs-index` → 47 entries, 45 docs, all checks pass; `bun run test:unit` → 180 test files / 3193 tests, all passed; `NODE_OPTIONS="--import tsx" bunx cucumber-js --tags "@regression"` → 53 scenarios: 10 passed, 0 failed, 42 pending, 1 undefined (357 steps: 228 passed, 86 skipped, 42 pending, 1 undefined) — the same 10 passed / 0 failed / 43 not-run the review recorded. Those guard banners are what the re-run, not this patch, changes.

## Files to Modify
Use these files to implement the patch:

- **None in this repository.** The spec is a write-once artifact (`app_docs/feature-9gjajh-specs-and-prd.md`: plan files are not updated after the build phase begins) and every in-repo change it describes is gated behind Step 1. Do not add, edit or delete any tracked file; do not add the dependency; do not merge `origin/dev` into this branch, rebase, resolve the README.md conflict or push; do not touch the two extracted directories, the guard, the feature file or the docs. This patch plan under `specs/patch/` is the only file this cycle produces; the pipeline commits it as docs, which is not a code commit.
- GitHub state touched by this patch: `paysdoc/AI_Dev_Workflow` PR #843 — **body only, one parenthetical** inside the re-run sentence (the draft flag is already set and must stay set; nothing else in the body changes).
- Scratch files under `/tmp/` only: a `mktemp -d` directory for the tarball check, `/tmp/adw-i7hs7a-pr843-body-v4.md` (fetched) and `/tmp/adw-i7hs7a-pr843-body-v5.md` (edited).

## Implementation Steps
IMPORTANT: Execute every step in order, top to bottom.

### Step 1: Re-verify the gate before touching anything
- Run the version listing and the spec's Step 1 tarball check. Use `mktemp -d` and leave the directory in place afterwards — the pre-tool-use hook blocks recursive deletes, and the directory is harmless:
  ```
  npm view @paysdoc/devplatform versions --json
  D=$(mktemp -d) && cd "$D" && npm pack @paysdoc/devplatform@latest --silent >/dev/null && tar xzf paysdoc-devplatform-*.tgz && node -e "(async()=>{const p=await import('$D/package/dist/providers/index.js');const g=await import('$D/package/dist/git/index.js');for(const s of ['createForgeCredentials','createGhRepoApi'])console.log((s in p?'present':'MISSING')+' providers:'+s);for(const s of ['createLiteralTokenProvider','commitOps','branchOps'])console.log((s in g?'present':'MISSING')+' git:'+s)})()"
  ```
- Expected today: versions `["1.0.0"]` and every line `MISSING`. Record the output for the patch report and continue to Step 2.
- **If instead every line prints `present`** (the gate opened between review and patch): STOP this patch without editing the PR. Report that the migration re-run is now due and must start with the rebase onto `origin/dev` (resolving the README.md conflict described above) and spec Step 2 pinning that exact version. Do not attempt the ~140-file migration inside this patch cycle — it needs the rebase first and is a `hitl` change the operator gates.

### Step 2: Confirm PR #843 is still the fourth cycle's non-closing draft and refresh its rebase note
- Confirm the draft flag and the absence of an auto-merge request: `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json isDraft,autoMergeRequest --jq '"\(.isDraft) \(.autoMergeRequest)"'` must print `true null`. Only if it prints `false …`, run `gh pr ready --undo 843 -R paysdoc/AI_Dev_Workflow`.
- Fetch the current body: `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json body --jq .body > /tmp/adw-i7hs7a-pr843-body-v4.md`.
- Confirm the previous cycles' refresh is still in place: the file must contain `no relevant changes`, `createForgeCredentials`, `devplatform#11`, `k3pj1m`, `Tracks #840` and `Every review of this PR so far`, and must not contain `Prerequisite: paysdoc/devplatform#11 merged`, `@paysdoc/devplatform@1.0.0` or `Three reviews`. **If any of those checks fails** (the body drifted since the fourth cycle), re-apply the full preamble and checklist edit from `specs/patch/patch-adw-i7hs7a-adw-switchover-to-pa-gate-closed-refresh-pr-843-release-handoff.md` Step 2, then the count-sentence replacement from `specs/patch/patch-adw-i7hs7a-adw-switchover-to-pa-gate-closed-verify-only-operator-handoff.md` Step 2, re-fetch, then continue.
- Make the single edit. Write `/tmp/adw-i7hs7a-pr843-body-v5.md` from the v4 file with exactly this parenthetical replaced (it occurs once, in the re-run paragraph of the blockquote); preserve everything else byte-for-byte, including line endings:
  - old (verbatim):
    ```
    (#844 landed after it was cut; `git merge-tree` is conflict-free as of 2026-09-22)
    ```
  - new (verbatim):
    ```
    (#844 and #847 landed after it was cut; as of 2026-09-22 post-#847, `git merge-tree` reports a single docs-only conflict: two hunks in `README.md`'s `adws/` directory-tree listing, where both sides add the same `forgeProviders.*.test.ts`/`forgeProvidersFixture.ts` and `extractionRule*.test.ts` lines with different trailing comments — keep either side, nothing else conflicts)
    ```
  - Suggested mechanism (asserts exactly one occurrence, so a drifted body cannot be silently mangled):
    ```
    python3 - <<'PY'
    old = "(#844 landed after it was cut; `git merge-tree` is conflict-free as of 2026-09-22)"
    new = "(#844 and #847 landed after it was cut; as of 2026-09-22 post-#847, `git merge-tree` reports a single docs-only conflict: two hunks in `README.md`'s `adws/` directory-tree listing, where both sides add the same `forgeProviders.*.test.ts`/`forgeProvidersFixture.ts` and `extractionRule*.test.ts` lines with different trailing comments — keep either side, nothing else conflicts)"
    src = open('/tmp/adw-i7hs7a-pr843-body-v4.md', newline='').read()
    assert src.count(old) == 1, f"expected exactly one occurrence, found {src.count(old)}"
    open('/tmp/adw-i7hs7a-pr843-body-v5.md', 'w', newline='').write(src.replace(old, new))
    PY
    ```
  - If the assertion fails with `found 0` **and** the body already contains `nothing else conflicts`, the edit was applied by an earlier run — skip the `gh pr edit` below and go to Step 3.
- Apply: `gh pr edit 843 -R paysdoc/AI_Dev_Workflow --body-file /tmp/adw-i7hs7a-pr843-body-v5.md`.
- Do **not** close the PR (the re-run reuses this branch and adwId; ADW resolves the existing PR by branch name). Do **not** introduce any `Closes`/`Fixes`/`Resolves`/`Implements` line. Do **not** post on paysdoc/devplatform#11 (`## Retry` cannot succeed against an empty branch, and `## Cancel` kills a process and removes a worktree in another repository — the operator's call) and do **not** post on #840 (the pipeline's Review Failed comment already carries the resolution text; a second copy is noise).

### Step 3: Leave the branch exactly as found — conflict included — and hand back the operator sequence
- Verify nothing in-repo moved: `git status --short` prints nothing beyond this plan file (until the pipeline commits it); `git log --oneline 4d25d21c..HEAD` prints exactly `a6f398a4`, `eee3d00c`, `136c288d`, `d5fd43a1`, `7188098b` (plus, after the pipeline commits it, the docs-only commit adding this plan); `git diff --stat` is empty. No code commit, no dependency, no merge of `origin/dev`, no rebase, no push from this patch.
- Explicitly leave the README.md conflict unresolved. Reasons, for the patch report: the review's resolution assigns the rebase to operator step (3) as the first move of the re-run; the pipeline resolves conflicts itself at merge time (`adws/phases/autoMergePhase.ts` and `adws/triggers/autoMergeHandler.ts` invoke `/resolve_conflict`), so a `DIRTY` draft is not a merge hazard; the gate has no ETA, so a merge or rebase made now goes stale with every further `dev` commit and adds a non-docs merge commit to a branch the review expects to stay docs-only; and the conflict is two hunks of README tree-listing comments that the rebase resolves in seconds by keeping either side.
- In the patch report, restate for the operator, in order: (1) fix the npm trusted-publisher (OIDC) grant for `paysdoc/devplatform`'s `release.yml`; (2) land a new releasable `feat:`/`fix:` commit on library `main` — devplatform#11 is the natural one, but its branch is empty, so post `## Cancel` on it and let a fresh run plan/build it, or open the widening PR by hand (barrel re-exports for `commitOps`/`branchOps`/`isLeaseRejection` in `/git` and `createGhRepoApi` in `/providers`, plus the import-graph assertion that `./git` pulls no adapter module; `feat:` prefix so semantic-release cuts a new minor); then confirm with `npm view @paysdoc/devplatform versions` and the Step 1 tarball check that `/providers` exports `createForgeCredentials` (+ `createGhRepoApi`) and `/git` exports `createLiteralTokenProvider`, `commitOps`, `branchOps`; (3) rebase this branch onto `origin/dev` — **no longer conflict-free**: expect the single docs-only README.md conflict described above (keep either side of both hunks), nothing else; (4) re-run the build from spec Step 2 pinning the exact published version and following the updated #840 body for the launch boundary (`createForgeCredentials({ forge, identity, deps })`; no production module imports a forge-named symbol — the #840 grep must print nothing); (5) re-review against all acceptance criteria and the spec's Validation Commands. Until (1) and (2) are done, no in-repo patch can satisfy this blocker and PR #843 stays a draft.

## Validation
Execute every command to validate the patch is complete with zero regressions.

- PR state: `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json isDraft,autoMergeRequest,mergeStateStatus,body --jq '"isDraft=\(.isDraft)", "autoMerge=\(.autoMergeRequest)", "mergeState=\(.mergeStateStatus)", "preamble-present=\(.body | contains("no relevant changes") and contains("createForgeCredentials") and contains("devplatform#11") and contains("k3pj1m") and contains("Tracks #840") and contains("Every review of this PR so far"))", "old-text-gone=\(.body | (contains("Prerequisite: paysdoc/devplatform#11 merged") or contains("@paysdoc/devplatform@1.0.0") or contains("Three reviews")) | not)", "rebase-note-refreshed=\(.body | contains("nothing else conflicts") and (contains("is conflict-free as of 2026-09-22") | not))"'` — expect `isDraft=true`, `autoMerge=null`, `mergeState=DIRTY` (expected until the operator's rebase; `CLEAN` is acceptable only if the operator has already rebased), and `true` for the three body checks; additionally `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json body --jq .body | grep -ciE '^(closes|fixes|resolves|implements) '` must print `0`.
- In-repo untouched: `git status --short && git log --oneline 4d25d21c..HEAD && git diff --stat` — expect nothing tracked-and-modified, exactly the five docs commits `a6f398a4`, `eee3d00c`, `136c288d`, `d5fd43a1`, `7188098b` (plus the pipeline's commit of this plan file once made), no merge commit, and an empty diff; `test -d adws/gitContext && test -d adws/providers && ! grep -q '@paysdoc/devplatform' package.json && echo "in-repo untouched"` — expect `in-repo untouched`.
- Baseline no-regression on the untouched branch (nothing changed, so these must stay as recorded): `bunx tsc --noEmit`, `bunx tsc --noEmit -p adws/tsconfig.json`, `bun run lint`, `bun run lint:git-guard`, `bun run lint:docs-index`, `bun run test:unit`, `NODE_OPTIONS="--import tsx" bunx cucumber-js --tags "@regression"` — expect both typechecks and lint exit 0, the guard's PASS with its unchanged pre-migration banners (`Exempt packages (2)`; the empty-exempt-set banner is the re-run's exit criterion, not this patch's), docs-index 47 entries all passing, 180 test files / 3193 tests green, and the regression suite at 10 passed / 0 failed with the other 43 scenarios pending or undefined (the unchanged pre-migration state; `feature-729` is repointed by the re-run, not by this patch).
- Gate check (documenting, not passing — today it prints `MISSING` lines; the migration re-run starts only when it prints none): `npm view @paysdoc/devplatform versions --json` → `["1.0.0"]`, plus the Step 1 tarball command.
- Rebase status for the re-run (documenting): `git fetch -q origin dev && git merge-tree --write-tree origin/dev HEAD 2>&1 | grep '^CONFLICT'` — expect exactly one line, naming `README.md`. If `dev` has moved again and other paths appear, record them in the patch report for the operator's step (3); still no merge or rebase from this patch.
- The spec's own Validation Commands (lint, both typechecks, unit, `lint:git-guard` with an empty exempt set, `lint:docs-index`, `@regression`, the two greps, the directory check) and the #840 forge-name grep are the exit criteria of the **re-run**, not of this patch; they cannot pass until the migration is built on a published library version above 1.0.0.

## Patch Scope
**Lines of code to change:** 0 in-repo (one parenthetical in PR #843's body, outside the repository; this plan file is the only committed artifact)
**Risk level:** low — one reversible GitHub body edit on an already-draft PR with an exactly-one-occurrence assertion; no code commits, no dependency, no merge, no rebase, no push, no action on the library repository or on any issue
**Testing required:** the GitHub-state verification above, the unchanged baseline typechecks / lint / guard / docs-index / unit suite / regression suite, and the documented gate and rebase-status checks
