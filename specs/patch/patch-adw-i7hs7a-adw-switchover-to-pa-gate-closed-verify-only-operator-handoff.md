# Patch: Gate still closed (fourth review) — verify PR #843's handoff is current, make its review-count sentence date-proof, change nothing in-repo

## Metadata
adwId: `i7hs7a-adw-switchover-to-pa`
reviewChangeRequest: `Issue #1: The migration was not performed; none of the spec's acceptance criteria are met. Verified on HEAD eee3d00c (four docs-only commits past base 4d25d21c: spec 7188098b, README d5fd43a1, patch plans 136c288d and eee3d00c; the merge-base diff touches only README.md and four markdown specs, zero code): adws/gitContext/ and adws/providers/ are present and 212 import lines in 124 files outside them still resolve into them; package.json has no @paysdoc/devplatform; bun run lint:git-guard prints 'Exempt packages (2)', 'Sanctioned construction sites - 2 permanent' and 'Extraction-readiness scope - 10 entries'; adws/guard/extractionRule.ts and both its tests exist and 'extraction-readiness' is still a ViolationRule member; adws/core/launchGitContext.ts still imports forge-named symbols (the updated #840 grep criterion); features/regression/upgrade/feature-729.feature lines 24, 36 and 81 still cite adws/gitContext; .adw/conditional_docs.md has 14 extracted-path references and app_docs/feature-9gjajh-providers.md still exists. Root cause is unchanged from the three previous reviews: the spec's Step 1 gate is closed. As of 2026-09-22, npm view @paysdoc/devplatform versions lists 1.0.0 only (published 2026-09-10T13:28Z, nothing since) and the packed 1.0.0 tarball reports MISSING for all twelve checked symbols (providers: createForgeCredentials, createGhRepoApi, createGitHubTokenProvider, resolveBootstrapGitIdentity, resolveContextToken, ghAuthToken, isGitHubAppConfigured, getInstallationToken; git: createLiteralTokenProvider, commitOps, branchOps, isLeaseRejection). Library side: tags v1.0.0 and v1.1.0 only, no GitHub releases, no release run since 34829911126 (2026-09-14, released nothing because semantic-release treats the never-published v1.1.0 as released); devplatform#11 (L4 widening) is OPEN with adw:feature + adw:unverified, has no PR, and its last activity is the 2026-09-22T08:42:56Z pipeline error (k3pj1m-widen-the-public-sur, missing specs/issue-11-plan.md). Spec Step 1 forbids re-implementing the missing helpers in ADW because that recreates the git/gh shell-out sites the empty exempt set must forbid, so this is not patchable in-repo. The previous patch cycle (specs/patch/patch-adw-i7hs7a-adw-switchover-to-pa-gate-closed-refresh-pr-843-release-handoff.md) did everything possible on the ADW side and its validation passes: PR #843 isDraft=true, mergeState CLEAN with no auto-merge request, refreshed preamble present (contains 'no relevant changes', createForgeCredentials, devplatform#11, k3pj1m, 'Tracks #840'), old preamble gone, checklist no longer pins 1.0.0, zero closing keywords at line start; git merge-tree against origin/dev exits 0 (branch is 15 commits behind, rebase is conflict-free).
Resolution: Not patchable in-repo: the patch cycle must make no code change, add no dependency, and keep PR #843 a non-closing draft. Operator actions outside this repository, in order: (1) fix the npm trusted-publisher (OIDC) grant for paysdoc/devplatform's release.yml so a publish can succeed; (2) land a new releasable feat:/fix: commit on library main so semantic-release cuts a version above 1.0.0 (v1.1.0 is tagged but unpublished and semantic-release now treats it as released, so the OIDC fix alone publishes nothing); devplatform#11 is the natural commit, but its pipeline branch is empty so it needs '## Cancel' and a fresh run, or a hand-written PR; then confirm with npm view @paysdoc/devplatform versions and the spec's Step 1 tarball check that /providers exports createForgeCredentials (plus createGhRepoApi) and /git exports createLiteralTokenProvider, commitOps and branchOps; (3) rebase this branch onto origin/dev (#844 changed the consumer inventory; conflict-free today); (4) re-run the build from spec Step 2 pinning the exact published version and following the updated #840 body for the launch boundary (createForgeCredentials({ forge, identity, deps }); no production module imports a forge-named symbol); (5) re-review against all acceptance criteria and the spec's Validation Commands. Until (1) and (2) are done, no in-repo patch can satisfy this blocker.`

## Issue Summary
**Original Spec:** specs/issue-840-adw-20l8es-adw-switchover-to-pa-sdlc_planner-devplatform-switchover.md
**Issue:** Fourth consecutive rejection for the same root cause: the switchover cannot be built because spec Step 1's gate is closed. npm still carries only `@paysdoc/devplatform@1.0.0`, whose barrels lack every symbol the migration needs (the updated #840 gate names `createForgeCredentials` in `/providers` and `createLiteralTokenProvider` in `/git`; both are `MISSING` from the 1.0.0 tarball). The library cannot publish until the npm trusted-publisher (OIDC) grant is fixed **and** a new `feat:`/`fix:` commit lands on library `main` (semantic-release treats the never-published v1.1.0 as released). The natural commit, devplatform#11, is stalled: its pipeline worktree has no plan file and no implementation commits, so every resume from `pr_creating` fails identically. The spec bans the only in-repo workaround (re-implementing the helpers in ADW re-creates the `git`/`gh` shell-out sites the empty exempt set forbids). The reviewer confirmed the third cycle's PR #843 refresh is in place and passing its own validation; the only thing in that handoff that has since gone stale is its opening sentence, which counts "Three reviews … the third re-confirmed" — there are now four.
**Solution:** Make no in-repo change (0 source files, 0 dependencies, 0 code commits; this plan file is the only committed artifact and the pipeline commits it as docs, as it did `136c288d` and `eee3d00c`). Re-verify the gate, confirm PR #843 is still the non-closing draft the third cycle left it as, and make exactly one reversible GitHub-side edit: replace the preamble's review-count sentence with a count-free one so the operator-facing status stays accurate through any further gate-closed cycles instead of needing a body rewrite each time. Leave the branch, tree and draft flag exactly as found; hand the operator sequence back in the patch report with the re-verified library-side facts.

### Verified state at patch-plan time (2026-09-22)
- **npm:** `npm view @paysdoc/devplatform versions time --json` → versions `["1.0.0"]`; `1.0.0` published 2026-09-10T13:28:16Z, `modified` 2026-09-10T13:28:16Z, nothing since. `npm pack @paysdoc/devplatform@latest` yields `paysdoc-devplatform-1.0.0.tgz`; importing `package/dist/providers/index.js` and `package/dist/git/index.js` reports `MISSING` for all twelve review-listed symbols (`providers:` `createForgeCredentials`, `createGhRepoApi`, `createGitHubTokenProvider`, `resolveBootstrapGitIdentity`, `resolveContextToken`, `ghAuthToken`, `isGitHubAppConfigured`, `getInstallationToken`; `git:` `createLiteralTokenProvider`, `commitOps`, `branchOps`, `isLeaseRejection`). Gate closed.
- **Library `paysdoc/devplatform`:** `main` = `cba596b8` (2026-09-14T09:48Z, merge of PR #12 docs-index sweep) — unchanged since the third cycle. Remote branches: `main` and one stale `adw-upgrade-…` branch only; no branch for issue 11. Tags `v1.1.0`, `v1.0.0`; `gh release list` empty. `release.yml` runs: `34829911126` (2026-09-14, success, released nothing), `34503029087` (2026-09-10, **failure** at `npm publish`, 403 OIDC permission denied), three earlier successes on 2026-09-10. Merged PRs: #12, #10 (`feat: #9 createForgeCredentials`), #8, #7, #6, #5 — nothing after 2026-09-14.
- **devplatform#11 (L4 widening):** OPEN, labels `adw:feature` + `adw:unverified`, `updatedAt` 2026-09-22T08:42:56Z. Last three comments are all from pipeline `k3pj1m-widen-the-public-sur`: two "Resuming from: pr_creating" (08:41:32Z, 08:41:41Z) and the "ADW Workflow Error … Cannot read plan file at …/feature-issue-11-widen-public-exports-adw-switchover/specs/issue-11-plan.md: ENOENT" (08:42:56Z). No operator directive (`## Cancel`, `## Retry`, `## continue`) has been posted since. No PR.
- **PR #843 (`paysdoc/AI_Dev_Workflow`):** OPEN, `isDraft=true`, base `dev`, head `chore-issue-840-adw-switchover-devplatform-library`, `mergeStateStatus=CLEAN`, `autoMergeRequest=null`. Body = the third cycle's v3 preamble (opens with the bold "BLOCKED at spec Step 1 — do not merge." sentence, then "Three reviews (adw gz7jte-adw-switchover-to-pa on 2026-09-10/11, adw i7hs7a-adw-switchover-to-pa on 2026-09-22) rejected it for that reason; the third re-confirmed the gate is still closed."), the gate paragraph, the "Why nothing publishes" paragraph (contains `no relevant changes`), the devplatform#11 paragraph (contains `k3pj1m`), the re-run sequence, then `## Summary` … `Tracks #840 (not a closing reference — the migration has not been performed).` … checklist whose dependency line reads "added as an exact-pinned dependency at the first published version above 1.0.0 that passes the Step 1 gate". No `Closes`/`Fixes`/`Resolves`/`Implements` line. Only non-bot PR comment is the PR Lens sticky from 2026-09-10.
- **Issue #840:** OPEN, `hitl`. Body (rewritten before this cycle) names prerequisites #844 merged and devplatform issue 9 released on npm; Step 1 gate = a version above 1.0.0 with `createForgeCredentials` in `/providers` and `createLiteralTokenProvider` in `/git`; launch boundary via `createForgeCredentials({ forge, identity, deps })`; rule that no production module imports a forge-named symbol; extra acceptance grep `git grep -lE "createGitHub|createGitLab|GitLabApiClient|JiraApiClient|JiraIssueTracker" -- adws ':!**/__tests__/**'` must print nothing. `## Blocked by #844` — issue #844 is CLOSED (2026-09-10T21:58Z) via PR #845 (MERGED, `309e3662`), so that prerequisite is satisfied. Last three #840 comments are the pipeline's Review Failed (08:45Z), Review Running (08:58Z) and Review Failed (09:04Z) for this adwId.
- **This branch:** clean tree; HEAD `eee3d00c`; four docs-only commits past base `4d25d21c` (`7188098b` spec, `d5fd43a1` README, `136c288d` and `eee3d00c` patch plans); `git diff --name-only origin/dev...HEAD` = `README.md` + four markdown files under `specs/`, zero code; 15 commits behind `origin/dev`; `git merge-tree --write-tree origin/dev HEAD` exits 0 — the re-run's rebase is conflict-free today. `adws/gitContext/` and `adws/providers/` present; `package.json` has zero `@paysdoc/devplatform` references.
- **Baseline on the untouched branch (2026-09-22):** `bunx tsc --noEmit` exit 0; `bunx tsc --noEmit -p adws/tsconfig.json` exit 0; `bun run test:unit` → 180 test files / 3193 tests, all passed; `bun run lint:git-guard` → PASS with the pre-migration banners (`Exempt packages (2)`, `Sanctioned construction sites — 2 permanent, 0 sunset`, `Extraction-readiness scope — 10 entries`) — those banners are what the re-run, not this patch, changes.

## Files to Modify
Use these files to implement the patch:

- **None in this repository.** The spec is a write-once artifact (`app_docs/feature-9gjajh-specs-and-prd.md`: plan files are not updated after the build phase begins) and every in-repo change it describes is gated behind Step 1. Do not add, edit, delete, rebase or push anything on this branch; do not add the dependency; do not touch the two extracted directories, the guard, the feature file or the docs. This patch plan under `specs/patch/` is the only file this cycle produces; the pipeline commits it as docs, which is not a code commit.
- GitHub state touched by this patch: `paysdoc/AI_Dev_Workflow` PR #843 — **body only, one sentence** (draft flag is already set and must stay set; nothing else in the body changes).
- Scratch files under `/tmp/` only: a `mktemp -d` directory for the tarball check, `/tmp/adw-i7hs7a-pr843-body-v3.md` (fetched) and `/tmp/adw-i7hs7a-pr843-body-v4.md` (edited).

## Implementation Steps
IMPORTANT: Execute every step in order, top to bottom.

### Step 1: Re-verify the gate before touching anything
- Run the version listing and the spec's Step 1 tarball check. Use `mktemp -d` and leave the directory in place afterwards — the pre-tool-use hook blocks recursive deletes, and the directory is harmless:
  ```
  npm view @paysdoc/devplatform versions --json
  D=$(mktemp -d) && cd "$D" && npm pack @paysdoc/devplatform@latest --silent >/dev/null && tar xzf paysdoc-devplatform-*.tgz && node -e "(async()=>{const p=await import('$D/package/dist/providers/index.js');const g=await import('$D/package/dist/git/index.js');for(const s of ['createForgeCredentials','createGhRepoApi'])console.log((s in p?'present':'MISSING')+' providers:'+s);for(const s of ['createLiteralTokenProvider','commitOps','branchOps'])console.log((s in g?'present':'MISSING')+' git:'+s)})()"
  ```
- Expected today: versions `["1.0.0"]` and every line `MISSING`. Record the output for the patch report and continue to Step 2.
- **If instead every line prints `present`** (the gate opened between review and patch): STOP this patch without editing the PR. Report that the migration re-run is now due and must start with the rebase onto `origin/dev` and spec Step 2 pinning that exact version. Do not attempt the ~140-file migration inside this patch cycle — it needs the rebase first and is a `hitl` change the operator gates.

### Step 2: Confirm PR #843 is still the third cycle's non-closing draft and date-proof its review-count sentence
- Confirm the draft flag and the absence of an auto-merge request: `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json isDraft,autoMergeRequest --jq '"\(.isDraft) \(.autoMergeRequest)"'` must print `true null`. Only if it prints `false …`, run `gh pr ready --undo 843 -R paysdoc/AI_Dev_Workflow`.
- Fetch the current body: `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json body --jq .body > /tmp/adw-i7hs7a-pr843-body-v3.md`.
- Confirm the third cycle's refresh is still in place: the file must contain `no relevant changes`, `createForgeCredentials`, `devplatform#11`, `k3pj1m` and `Tracks #840`, and must not contain `Prerequisite: paysdoc/devplatform#11 merged` or `@paysdoc/devplatform@1.0.0`. **If any of those checks fails** (the body drifted since the third cycle), re-apply the full v3 preamble and checklist edit exactly as specified in `specs/patch/patch-adw-i7hs7a-adw-switchover-to-pa-gate-closed-refresh-pr-843-release-handoff.md` Step 2 first, re-fetch, then continue.
- Make the single edit. Write `/tmp/adw-i7hs7a-pr843-body-v4.md` from the v3 file with exactly this sentence replaced (it occurs once, in the first blockquote paragraph); preserve everything else byte-for-byte, including line endings:
  - old (verbatim):
    ```
    Three reviews (adw `gz7jte-adw-switchover-to-pa` on 2026-09-10/11, adw `i7hs7a-adw-switchover-to-pa` on 2026-09-22) rejected it for that reason; the third re-confirmed the gate is still closed.
    ```
  - new (verbatim):
    ```
    Every review of this PR so far (adw `gz7jte-adw-switchover-to-pa`, 2026-09-10/11; adw `i7hs7a-adw-switchover-to-pa`, 2026-09-22) has rejected it for that reason, and the latest re-confirmed the gate is still closed.
    ```
  - Suggested mechanism (asserts exactly one occurrence, so a drifted body cannot be silently mangled):
    ```
    python3 - <<'PY'
    old = "Three reviews (adw `gz7jte-adw-switchover-to-pa` on 2026-09-10/11, adw `i7hs7a-adw-switchover-to-pa` on 2026-09-22) rejected it for that reason; the third re-confirmed the gate is still closed."
    new = "Every review of this PR so far (adw `gz7jte-adw-switchover-to-pa`, 2026-09-10/11; adw `i7hs7a-adw-switchover-to-pa`, 2026-09-22) has rejected it for that reason, and the latest re-confirmed the gate is still closed."
    src = open('/tmp/adw-i7hs7a-pr843-body-v3.md', newline='').read()
    assert src.count(old) == 1, f"expected exactly one occurrence, found {src.count(old)}"
    open('/tmp/adw-i7hs7a-pr843-body-v4.md', 'w', newline='').write(src.replace(old, new))
    PY
    ```
  - If the assertion fails with `found 0` **and** the body already contains `Every review of this PR so far`, the edit was applied by an earlier run — skip the `gh pr edit` below and go to Step 3.
- Apply: `gh pr edit 843 -R paysdoc/AI_Dev_Workflow --body-file /tmp/adw-i7hs7a-pr843-body-v4.md`.
- Do **not** close the PR (the re-run reuses this branch and adwId; ADW resolves the existing PR by branch name). Do **not** introduce any `Closes`/`Fixes`/`Resolves`/`Implements` line. Do **not** post on paysdoc/devplatform#11 (`## Retry` cannot succeed against an empty branch, and `## Cancel` kills a process and removes a worktree in another repository — the operator's call) and do **not** post on #840 (the pipeline's Review Failed comment already carries the resolution text; a second copy is noise).

### Step 3: Leave the branch exactly as found and hand back the operator sequence
- Verify nothing in-repo moved: `git status --short` prints nothing beyond this plan file (until the pipeline commits it); `git log --oneline 4d25d21c..HEAD` prints exactly `eee3d00c`, `136c288d`, `d5fd43a1`, `7188098b` (plus, after the pipeline commits it, the docs-only commit adding this plan); `git diff --stat` is empty. No code commit, no dependency, no rebase, no push from this patch.
- In the patch report, restate for the operator, in order: (1) fix the npm trusted-publisher (OIDC) grant for `paysdoc/devplatform`'s `release.yml`; (2) land a new releasable `feat:`/`fix:` commit on library `main` — devplatform#11 is the natural one, but its branch is empty, so post `## Cancel` on it and let a fresh run plan/build it, or open the widening PR by hand (barrel re-exports for `commitOps`/`branchOps`/`isLeaseRejection` in `/git` and `createGhRepoApi` in `/providers`, plus the import-graph assertion that `./git` pulls no adapter module; `feat:` prefix so semantic-release cuts a new minor); then confirm with `npm view @paysdoc/devplatform versions` and the Step 1 tarball check that `/providers` exports `createForgeCredentials` (+ `createGhRepoApi`) and `/git` exports `createLiteralTokenProvider`, `commitOps`, `branchOps`; (3) rebase this branch onto `origin/dev` (conflict-free today); (4) re-run the build from spec Step 2 pinning the exact published version and following the updated #840 body for the launch boundary (`createForgeCredentials({ forge, identity, deps })`; no production module imports a forge-named symbol — the #840 grep must print nothing); (5) re-review against all acceptance criteria and the spec's Validation Commands. Until (1) and (2) are done, no in-repo patch can satisfy this blocker and PR #843 stays a draft.

## Validation
Execute every command to validate the patch is complete with zero regressions.

- PR state: `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json isDraft,autoMergeRequest,mergeStateStatus,body --jq '"isDraft=\(.isDraft)", "autoMerge=\(.autoMergeRequest)", "mergeState=\(.mergeStateStatus)", "v3-preamble-present=\(.body | contains("no relevant changes") and contains("createForgeCredentials") and contains("devplatform#11") and contains("k3pj1m") and contains("Tracks #840"))", "old-preamble-gone=\(.body | contains("Prerequisite: paysdoc/devplatform#11 merged") | not)", "checklist-updated=\(.body | contains("@paysdoc/devplatform@1.0.0") | not)", "count-sentence-dateproof=\(.body | contains("Every review of this PR so far") and (contains("Three reviews") | not))"'` — expect `isDraft=true`, `autoMerge=null`, `mergeState=CLEAN`, and `true` for the four body checks; additionally `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json body --jq .body | grep -ciE '^(closes|fixes|resolves|implements) '` must print `0`.
- In-repo untouched: `git status --short && git log --oneline 4d25d21c..HEAD && git diff --stat` — expect nothing tracked-and-modified, exactly the four docs commits `eee3d00c`, `136c288d`, `d5fd43a1`, `7188098b` (plus the pipeline's commit of this plan file once made), and an empty diff; `test -d adws/gitContext && test -d adws/providers && ! grep -q '@paysdoc/devplatform' package.json && echo "in-repo untouched"` — expect `in-repo untouched`.
- Baseline no-regression on the untouched branch (nothing changed, so these must stay as recorded): `bunx tsc --noEmit`, `bunx tsc --noEmit -p adws/tsconfig.json`, `bun run test:unit`, `bun run lint:git-guard` — expect both typechecks exit 0, 180 test files / 3193 tests green, and the guard's PASS with its unchanged pre-migration banners (`Exempt packages (2)`; the empty-exempt-set banner is the re-run's exit criterion, not this patch's).
- Gate check (documenting, not passing — today it prints `MISSING` lines; the migration re-run starts only when it prints none): `npm view @paysdoc/devplatform versions --json` → `["1.0.0"]`, plus the Step 1 tarball command.
- Rebase safety for the re-run: `git fetch -q origin dev && git merge-tree --write-tree origin/dev HEAD >/dev/null; echo "merge-tree exit=$?"` — expect `0`.
- The spec's own Validation Commands (lint, both typechecks, unit, `lint:git-guard` with an empty exempt set, `lint:docs-index`, `@regression`, the two greps, the directory check) and the #840 forge-name grep are the exit criteria of the **re-run**, not of this patch; they cannot pass until the migration is built on a published library version above 1.0.0.

## Patch Scope
**Lines of code to change:** 0 in-repo (one sentence of PR #843's body, outside the repository; this plan file is the only committed artifact)
**Risk level:** low — one reversible, single-sentence GitHub body edit on an already-draft PR with an exactly-one-occurrence assertion; no code commits, no dependency, no rebase, no push, no action on the library repository or on any issue
**Testing required:** the GitHub-state verification above, the unchanged baseline typechecks / unit suite / guard, and the documented gate and rebase-safety checks
