# Patch: Gate still closed (sixth review) — verify PR #843's handoff is still current, change nothing in-repo and nothing on GitHub

## Metadata
adwId: `i7hs7a-adw-switchover-to-pa`
reviewChangeRequest: `Issue #1: The migration was not performed; none of the spec's four acceptance criteria are met. Verified on HEAD 0f6395ff (= origin/chore-issue-840-adw-switchover-devplatform-library, clean tree; six docs-only commits past merge-base 4d25d21c: spec 7188098b, README d5fd43a1, patch plans 136c288d, eee3d00c, a6f398a4, 0f6395ff; git diff origin/dev...HEAD --name-only = README.md + six markdown files under specs/, zero code). In-repo facts: adws/gitContext/ (19 entries) and adws/providers/ are present and the spec's validation grep git grep -nE "from ['\"][^'\"]*(gitContext|adws/providers)" -- '*.ts' '*.tsx' prints 161 lines (must print nothing); package.json has zero @paysdoc/devplatform references and node_modules/@paysdoc does not exist; bun run lint:git-guard prints 'Exempt packages (2)', 'Sanctioned construction sites — 2 permanent, 0 sunset' (adws/providers/forgeProviders.ts still listed at constructionRule.ts:93) and 'Extraction-readiness scope — 10 entries'; adws/guard/extractionRule.ts and both its tests exist and 'extraction-readiness' is still a ViolationRule member (violationTypes.ts:13); adws/core/launchGitContext.ts still imports forge-named symbols (the updated #840 grep criterion lists it); features/regression/upgrade/feature-729.feature lines 24, 36 and 81 still cite adws/gitContext; .adw/conditional_docs.md has 14 extracted-path references; app_docs/feature-9gjajh-providers.md still exists; lint:docs-index reports the pre-migration 47 entries. Root cause is unchanged from the five previous reviews: the spec's Step 1 gate is closed. As of 2026-09-22, npm view @paysdoc/devplatform versions time --json lists 1.0.0 only (published 2026-09-10T13:28:16Z, modified 2026-09-10T13:28:16Z, nothing since) and the spec's Step 1 tarball check on paysdoc-devplatform-1.0.0.tgz reports MISSING for all twelve required symbols (providers: createForgeCredentials, createGhRepoApi, createGitHubTokenProvider, resolveBootstrapGitIdentity, resolveContextToken, ghAuthToken, isGitHubAppConfigured, getInstallationToken; git: createLiteralTokenProvider, commitOps, branchOps, isLeaseRejection), with only git:GitContext present. Library side (paysdoc/devplatform): main = cba596b8 (2026-09-14T09:48:31Z, merge of PR #12), tags v1.0.0 (4634ee68) and v1.1.0 (ba168795), gh release list empty, zero open PRs; release runs 34829911126 (2026-09-14, success, released nothing because semantic-release treats the never-published v1.1.0 as released) and 34503029087 (2026-09-10, failure at npm publish, 403 OIDC permission denied). devplatform#11 (L4 widening) is OPEN with labels adw:unverified + adw:feature, last updated 2026-09-22T08:42:56Z (its pipeline's ENOENT plan-file error), no PR, no operator directive since. Spec Step 1 forbids re-implementing the missing helpers in ADW because that recreates the git/gh shell-out sites the empty exempt set must forbid, so this is not patchable in-repo. The fifth patch cycle's handoff is verified intact and accurate: PR #843 is OPEN, isDraft=true, autoMergeRequest=null, mergeable=CONFLICTING / mergeStateStatus=DIRTY (git merge-tree --write-tree origin/dev HEAD reports exactly one docs-only CONFLICT (content) in README.md, introduced when PR #847 merged to dev), body has zero Closes/Fixes/Resolves/Implements lines, carries Tracks #840, the date-proof review-count sentence, the OIDC/no relevant changes root cause, the devplatform#11/k3pj1m paragraph, the #844 and #847 landed after it was cut rebase note, and no stale conflict-free as of 2026-09-22, Three reviews, @paysdoc/devplatform@1.0.0 or Prerequisite: paysdoc/devplatform#11 merged text. Baseline on the untouched branch is green: bunx tsc --noEmit and bunx tsc --noEmit -p adws/tsconfig.json both exit 0 with no output; bun run lint exit 0 with zero warnings/errors; lint:git-guard PASS (286 files); lint:docs-index PASS (47 entries, 45 docs); @regression 10 passed / 0 failed / 42 pending / 1 undefined of 53 (the JUnit's 43 <failure/> entries are all pending/undefined, zero assertion failures); @adw-840 and @review-proof both match zero scenarios (the @review-proof tag appears only in a step-definition comment, never in a .feature file). Every further review/patch cycle run before the gate opens will reproduce this same result. Resolution: Not patchable in-repo: the patch cycle must make no code change, add no dependency, not merge/rebase/resolve the README.md conflict, and keep PR #843 a non-closing draft (its body is already current — the only change since the fifth cycle is that a sixth review has re-confirmed the closed gate, which the date-proof sentence already covers). Operator actions outside this repository, in order: (1) fix the npm trusted-publisher (OIDC) grant for paysdoc/devplatform's release.yml so a publish can succeed; (2) land a new releasable feat:/fix: commit on library main so semantic-release cuts a version above 1.0.0 (v1.1.0 is tagged but unpublished and semantic-release now treats it as released, so the OIDC fix alone publishes nothing); devplatform#11 is the natural commit, but its pipeline branch is empty (no plan file, no implementation commits) so it needs ## Cancel and a fresh run, or a hand-written PR with the barrel re-exports plus the import-graph assertion that ./git pulls no adapter module; then confirm with npm view @paysdoc/devplatform versions and the spec's Step 1 tarball check that /providers exports createForgeCredentials (plus createGhRepoApi for feature-797 steps) and /git exports createLiteralTokenProvider, commitOps and branchOps; (3) rebase this branch onto origin/dev (#844 and #847 changed the consumer inventory and README; the single README.md conflict is two docs-only directory-tree hunks where either side is correct); (4) re-run the build from spec Step 2 pinning the exact published version and following the updated #840 body for the launch boundary (createForgeCredentials({ forge, identity, deps }); no production module imports a forge-named symbol; the extra acceptance grep git grep -lE "createGitHub|createGitLab|GitLabApiClient|JiraApiClient|JiraIssueTracker" -- adws ':!**/__tests__/**' must print nothing); (5) re-review against all four acceptance criteria and the spec's Validation Commands. Until (1) and (2) are done, no in-repo patch can satisfy this blocker.`

## Issue Summary
**Original Spec:** specs/issue-840-adw-20l8es-adw-switchover-to-pa-sdlc_planner-devplatform-switchover.md
**Issue:** Sixth consecutive rejection for the same root cause: the switchover cannot be built because spec Step 1's gate is closed. npm still carries only `@paysdoc/devplatform@1.0.0` (published 2026-09-10T13:28:16Z, nothing since), whose barrels lack every symbol the migration needs — the spec's Step 1 tarball check reports `MISSING` for all twelve review-listed symbols and `present` only for `git:GitContext`. The library cannot publish until the npm trusted-publisher (OIDC) grant for its `release.yml` is fixed **and** a new `feat:`/`fix:` commit lands on library `main` (semantic-release treats the never-published tag v1.1.0 as released, so the OIDC fix alone publishes nothing). The natural commit, paysdoc/devplatform#11, is still stalled: OPEN, no PR, no branch, no operator directive since its pipeline's 2026-09-22T08:42:56Z plan-file ENOENT error. The spec bans the only in-repo workaround (re-implementing the helpers in ADW re-creates the `git`/`gh` shell-out sites the empty exempt set forbids). Unlike the third, fourth and fifth cycles, **nothing in the handoff has gone stale**: the reviewer verified — and this plan re-verified live — that PR #843 is still the non-closing draft the fifth cycle left (`isDraft=true`, `autoMergeRequest=null`, zero closing keywords, the date-proof review-count sentence, the OIDC/`no relevant changes` root cause, the devplatform#11/`k3pj1m` paragraph, and the `#844 and #847` rebase note that already describes the single README.md conflict), and that `origin/dev` (`1ec1295c`), library `main` (`cba596b8`) and npm have not moved since the fifth cycle ran. The date-proof sentence ("Every review of this PR so far … has rejected it for that reason, and the latest re-confirmed the gate is still closed") already covers a sixth rejection, so there is no body edit to make.
**Solution:** Change nothing anywhere: 0 source files, 0 dependencies, 0 code commits, no merge, no rebase, no conflict resolution, no push, and — for the first time in this adwId — no `gh pr edit` either. Re-verify the gate, confirm PR #843's handoff is intact with the same checks the fifth cycle's validation used (re-applying the earlier cycles' documented body edits only if a check has become false), leave the README.md conflict for the operator's rebase, and restate the five operator actions. This plan file under `specs/patch/` is the only artifact; the pipeline commits it as docs, as it did `136c288d`, `eee3d00c`, `a6f398a4` and `0f6395ff`.

### Verified state at patch-plan time (2026-09-22, after the sixth review)
- **npm:** `npm view @paysdoc/devplatform versions time --json` → versions `["1.0.0"]`; `created` 2026-09-10T13:28:15.899Z, `1.0.0` 2026-09-10T13:28:16.219Z, `modified` 2026-09-10T13:28:16.795Z, nothing since. `npm pack @paysdoc/devplatform@latest` yields `paysdoc-devplatform-1.0.0.tgz`; importing `package/dist/providers/index.js` and `package/dist/git/index.js` reports `MISSING` for all twelve review-listed symbols (`providers:` `createForgeCredentials`, `createGhRepoApi`, `createGitHubTokenProvider`, `resolveBootstrapGitIdentity`, `resolveContextToken`, `ghAuthToken`, `isGitHubAppConfigured`, `getInstallationToken`; `git:` `createLiteralTokenProvider`, `commitOps`, `branchOps`, `isLeaseRejection`) and `present` only for `git:GitContext`. Gate closed.
- **Library `paysdoc/devplatform`:** `main` = `cba596b8` (2026-09-14T09:48:31Z, merge of PR #12) — unchanged since the third cycle. Tags `v1.1.0` (`ba168795`), `v1.0.0` (`4634ee68`); `gh release list` empty; zero open PRs. `release.yml` runs: `34829911126` (2026-09-14T09:48:34Z, success, released nothing), `34503029087` (2026-09-10T16:35:23Z, **failure** at `npm publish`, 403 OIDC permission denied), `34477324029` (2026-09-10T12:32:45Z, success). Nothing new since the fifth cycle.
- **devplatform#11 (L4 widening):** OPEN, labels `adw:unverified` + `adw:feature`, `updatedAt` 2026-09-22T08:42:56Z, 483 comments. Last three are all from pipeline `k3pj1m-widen-the-public-sur`: "ADW Workflow Resuming" (08:41:32Z, 08:41:41Z) and "ADW Workflow Error" (08:42:56Z). No operator directive (`## Cancel`, `## Retry`, `## continue`) since. No PR.
- **PR #843 (`paysdoc/AI_Dev_Workflow`):** OPEN, `isDraft=true`, base `dev`, head `chore-issue-840-adw-switchover-devplatform-library`, `autoMergeRequest=null`, `updatedAt` 2026-09-22T09:29:23Z — that is the fifth cycle's single body edit; nothing has touched the PR since. `mergeable=CONFLICTING`, `mergeStateStatus=DIRTY` (expected until the operator's rebase). Body checks: `preamble-present=true` (contains `no relevant changes`, `createForgeCredentials`, `devplatform#11`, `k3pj1m`, `Tracks #840`, `Every review of this PR so far`), `old-text-gone=true` (contains none of `Prerequisite: paysdoc/devplatform#11 merged`, `@paysdoc/devplatform@1.0.0`, `Three reviews`, `is conflict-free as of 2026-09-22`), `rebase-note-refreshed=true` (contains `#844 and #847 landed after it was cut` and `nothing else conflicts`); `grep -ciE '^(closes|fixes|resolves|implements) '` over the body prints `0`.
- **Issue #840:** OPEN, `hitl` + `adw:chore`. Last three comments are the pipeline's Review Failed (09:34:23Z, fifth review), Review Running (09:46:31Z) and Review Failed (09:52:18Z, sixth review — its body carries this cycle's blocker text, HEAD `0f6395ff` included).
- **`origin/dev`:** still `1ec1295c` = merge of PR #847 (2026-09-22T09:13:56Z) — unchanged since the fifth cycle. This branch is 18 commits behind / 6 ahead; merge-base `4d25d21c` unchanged. `git merge-tree --write-tree origin/dev HEAD` produces tree `a8b1fa93` with exactly one `CONFLICT (content)` line, `README.md`; `README.md` is also the only path changed on both sides since the base. The two hunks are byte-identical to the fifth cycle's description: (a) merged-tree lines 812–822, under `adws/providers/__tests__/`, both sides add `forgeProviders.deps.test.ts`, `forgeProviders.selection.test.ts` and `forgeProvidersFixture.ts` next to `forgeProviders.test.ts` with different trailing comments (`dev` from #845, this branch from `d5fd43a1`); (b) lines 997–1003, under `adws/guard/__tests__/`, both sides add `extractionRule.integration.test.ts` in a different order/with a different comment. `README.md` at HEAD is unchanged since `d5fd43a1`. Either side is a correct README; nothing else conflicts.
- **This branch:** clean tree; HEAD `0f6395ff` = `origin/chore-issue-840-adw-switchover-devplatform-library`; six docs-only commits past base `4d25d21c` (`7188098b` spec, `d5fd43a1` README, `136c288d`, `eee3d00c`, `a6f398a4`, `0f6395ff` patch plans); `git diff --name-only origin/dev...HEAD` = `README.md` + six markdown files under `specs/`, zero code.
- **In-repo acceptance facts (all still pre-migration, matching the review line by line):** `adws/gitContext/` (19 entries) and `adws/providers/` present; `package.json` has zero `@paysdoc/devplatform` references; `node_modules/@paysdoc` absent; the spec's import grep (`git grep -nE "from ['\"][^'\"]*(gitContext|adws/providers)" -- '*.ts' '*.tsx'`) prints 161 lines (must print nothing after the migration); the #840 forge-name grep (`git grep -lE "createGitHub|createGitLab|GitLabApiClient|JiraApiClient|JiraIssueTracker" -- adws ':!**/__tests__/**'`) prints 16 files, `adws/core/launchGitContext.ts` among them (must print nothing after the re-run); `features/regression/upgrade/feature-729.feature` cites `adws/gitContext` at lines 24, 36, 81; `.adw/conditional_docs.md` has 14 `adws/gitContext`/`adws/providers` references; `app_docs/feature-9gjajh-providers.md`, `adws/guard/extractionRule.ts`, `extractionRule.test.ts` and `extractionRule.integration.test.ts` all exist; `'extraction-readiness'` is still a `ViolationRule` member (`adws/guard/violationTypes.ts:13`); `adws/providers/forgeProviders.ts` is still a sanctioned construction site (`adws/guard/constructionRule.ts:93`).
- **Baseline on the untouched branch (2026-09-22):** `bunx tsc --noEmit` exit 0; `bunx tsc --noEmit -p adws/tsconfig.json` exit 0; `bun run lint` exit 0 (`eslint .` prints nothing); `bun run lint:git-guard` → PASS, 286 files scanned, with the pre-migration banners (`Exempt packages (2)`, `Sanctioned construction sites — 2 permanent, 0 sunset`, `Extraction-readiness scope — 10 entries`); `bun run lint:docs-index` → all checks pass (47 entries, band `[25, 60]`); `bun run test:unit` → 180 test files / 3193 tests, all passed (the two `failed to push some refs to '/tmp/adw-claim-integ-sandbox-…/remote.git'` lines are the upgrade-claim integration sandbox exercising lease rejection — expected output, not failures); `NODE_OPTIONS="--import tsx" bunx cucumber-js --tags "@regression"` → 53 scenarios: 10 passed, 0 failed, 42 pending, 1 undefined (357 steps: 228 passed, 86 skipped, 42 pending, 1 undefined), exactly the review's numbers (the `publishPrProof: failed to upload calculator/calc.png` warning is the proof-upload stub, not a failure). Those guard banners are what the re-run, not this patch, changes.

## Files to Modify
Use these files to implement the patch:

- **None in this repository.** The spec is a write-once artifact (`app_docs/feature-9gjajh-specs-and-prd.md`: plan files are not updated after the build phase begins) and every in-repo change it describes is gated behind Step 1. Do not add, edit or delete any tracked file; do not add the dependency; do not merge `origin/dev` into this branch, rebase, resolve the README.md conflict or push; do not touch the two extracted directories, the guard, the feature file or the docs. This patch plan under `specs/patch/` is the only file this cycle produces; the pipeline commits it as docs, which is not a code commit.
- **No GitHub state is touched this cycle** — the difference from the third, fourth and fifth cycles. PR #843's draft flag and body are already current (verified above), so there is no `gh pr edit`, no `gh pr ready --undo`, no comment on #840, on PR #843 or on paysdoc/devplatform#11, and no label change. The only GitHub write this plan permits is conditional: re-applying an earlier cycle's documented body edit if — and only if — the Step 2 check finds that edit has regressed.
- Scratch files under `/tmp/` only: a `mktemp -d` directory for the tarball check.

## Implementation Steps
IMPORTANT: Execute every step in order, top to bottom.

### Step 1: Re-verify the gate before touching anything
- Run the version listing and the spec's Step 1 tarball check. Use `mktemp -d` and leave the directory in place afterwards — the pre-tool-use hook blocks recursive deletes, and the directory is harmless:
  ```
  npm view @paysdoc/devplatform versions --json
  D=$(mktemp -d) && cd "$D" && npm pack @paysdoc/devplatform@latest --silent >/dev/null && tar xzf paysdoc-devplatform-*.tgz && node -e "(async()=>{const p=await import('$D/package/dist/providers/index.js');const g=await import('$D/package/dist/git/index.js');for(const s of ['createForgeCredentials','createGhRepoApi'])console.log((s in p?'present':'MISSING')+' providers:'+s);for(const s of ['createLiteralTokenProvider','commitOps','branchOps'])console.log((s in g?'present':'MISSING')+' git:'+s)})()"
  ```
- Expected today: versions `["1.0.0"]` and every line `MISSING`. Record the output for the patch report and continue to Step 2.
- **If instead every line prints `present`** (the gate opened between review and patch): STOP this patch without editing anything. Report that the migration re-run is now due and must start with the rebase onto `origin/dev` (resolving the README.md conflict described above) and spec Step 2 pinning that exact version. Do not attempt the ~140-file migration inside this patch cycle — it needs the rebase first and is a `hitl` change the operator gates.

### Step 2: Confirm PR #843's handoff is still intact — read-only unless a check has regressed
- Run the combined check:
  ```
  gh pr view 843 -R paysdoc/AI_Dev_Workflow --json isDraft,autoMergeRequest,mergeStateStatus,updatedAt,body --jq '"isDraft=\(.isDraft)", "autoMerge=\(.autoMergeRequest)", "mergeState=\(.mergeStateStatus)", "updatedAt=\(.updatedAt)", "preamble-present=\(.body | contains("no relevant changes") and contains("createForgeCredentials") and contains("devplatform#11") and contains("k3pj1m") and contains("Tracks #840") and contains("Every review of this PR so far"))", "old-text-gone=\(.body | (contains("Prerequisite: paysdoc/devplatform#11 merged") or contains("@paysdoc/devplatform@1.0.0") or contains("Three reviews") or contains("is conflict-free as of 2026-09-22")) | not)", "rebase-note-refreshed=\(.body | contains("nothing else conflicts") and contains("#844 and #847 landed after it was cut"))"'
  gh pr view 843 -R paysdoc/AI_Dev_Workflow --json body --jq .body | grep -ciE '^(closes|fixes|resolves|implements) '
  ```
- Expected (and verified at plan time): `isDraft=true`, `autoMerge=null`, `mergeState=DIRTY`, `updatedAt=2026-09-22T09:29:23Z`, the three body checks `true`, and `0` closing-keyword lines. **When everything matches, make no `gh pr edit`:** the date-proof sentence already covers the sixth review, and rewriting an accurate body only churns `updatedAt` and the PR timeline.
- Only if `isDraft=false`: `gh pr ready --undo 843 -R paysdoc/AI_Dev_Workflow`.
- Only if `preamble-present` or `old-text-gone` is `false` (the body drifted): re-apply the preamble and checklist edit from `specs/patch/patch-adw-i7hs7a-adw-switchover-to-pa-gate-closed-refresh-pr-843-release-handoff.md` Step 2, then the count-sentence replacement from `specs/patch/patch-adw-i7hs7a-adw-switchover-to-pa-gate-closed-verify-only-operator-handoff.md` Step 2. Only if `rebase-note-refreshed` is `false`: re-apply the single parenthetical replacement from `specs/patch/patch-adw-i7hs7a-adw-switchover-to-pa-gate-closed-hold-draft-refresh-rebase-note.md` Step 2 (it asserts exactly one occurrence). Re-run the combined check afterwards; all three must read `true`.
- Do **not** close the PR (the re-run reuses this branch and adwId; ADW resolves the existing PR by branch name). Do **not** introduce any `Closes`/`Fixes`/`Resolves`/`Implements` line. Do **not** post on paysdoc/devplatform#11 (`## Retry` cannot succeed against an empty branch, and `## Cancel` kills a process and removes a worktree in another repository — the operator's call) and do **not** post on #840 (the pipeline's Review Failed comment at 09:52:18Z already carries this blocker's Issue #1 text, HEAD `0f6395ff` included; the operator sequence lives in PR #843's body and in this plan, so a second copy is noise).

### Step 3: Leave the branch exactly as found — conflict included — and hand back the operator sequence
- Verify nothing in-repo moved: `git status --short` prints nothing beyond this plan file (until the pipeline commits it); `git log --oneline 4d25d21c..HEAD` prints exactly `0f6395ff`, `a6f398a4`, `eee3d00c`, `136c288d`, `d5fd43a1`, `7188098b` (plus, after the pipeline commits it, the docs-only commit adding this plan); `git diff --stat` is empty. No code commit, no dependency, no merge of `origin/dev`, no rebase, no push from this patch.
- Explicitly leave the README.md conflict unresolved. Reasons, for the patch report: the review's resolution assigns the rebase to operator step (3) as the first move of the re-run; the pipeline resolves conflicts itself at merge time (`adws/phases/autoMergePhase.ts` and `adws/triggers/autoMergeHandler.ts` invoke `/resolve_conflict`), so a `DIRTY` draft is not a merge hazard; the gate has no ETA, so a merge or rebase made now goes stale with every further `dev` commit and adds a non-docs merge commit to a branch the review expects to stay docs-only; and the conflict is two hunks of README tree-listing comments that the rebase resolves in seconds by keeping either side.
- In the patch report, restate for the operator, in order: (1) fix the npm trusted-publisher (OIDC) grant for `paysdoc/devplatform`'s `release.yml` so a publish can succeed; (2) land a new releasable `feat:`/`fix:` commit on library `main` so semantic-release cuts a version above 1.0.0 (v1.1.0 is tagged but unpublished and semantic-release now treats it as released, so the OIDC fix alone publishes nothing) — devplatform#11 is the natural commit, but its pipeline branch is empty (no plan file, no implementation commits), so post `## Cancel` on it and let a fresh run plan/build it, or open the widening PR by hand (barrel re-exports for `commitOps`/`branchOps`/`isLeaseRejection` in `/git` and `createGhRepoApi` in `/providers`, plus the import-graph assertion that `./git` pulls no adapter module; `feat:` prefix so semantic-release cuts a new minor); then confirm with `npm view @paysdoc/devplatform versions` and the Step 1 tarball check that `/providers` exports `createForgeCredentials` (plus `createGhRepoApi` for the feature-797 steps) and `/git` exports `createLiteralTokenProvider`, `commitOps` and `branchOps`; (3) rebase this branch onto `origin/dev` (#844 and #847 changed the consumer inventory and README) — expect the single docs-only README.md conflict described above, keep either side of both hunks, nothing else conflicts; (4) re-run the build from spec Step 2 pinning the exact published version and following the updated #840 body for the launch boundary (`createForgeCredentials({ forge, identity, deps })`; no production module imports a forge-named symbol — the #840 forge-name grep must print nothing); (5) re-review against all four acceptance criteria and the spec's Validation Commands. Until (1) and (2) are done, no in-repo patch can satisfy this blocker and PR #843 stays a draft; every further review/patch cycle before then reproduces this result.

## Validation
Execute every command to validate the patch is complete with zero regressions.

- PR state: the Step 2 combined check — expect `isDraft=true`, `autoMerge=null`, `mergeState=DIRTY` (expected until the operator's rebase; `CLEAN` is acceptable only if the operator has already rebased), `updatedAt=2026-09-22T09:29:23Z` (unchanged, proving this cycle wrote nothing to the PR; a later timestamp is acceptable only if Step 2 had to re-apply a regressed edit), and `true` for the three body checks; `gh pr view 843 -R paysdoc/AI_Dev_Workflow --json body --jq .body | grep -ciE '^(closes|fixes|resolves|implements) '` must print `0`.
- In-repo untouched: `git status --short && git log --oneline 4d25d21c..HEAD && git diff --stat` — expect nothing tracked-and-modified, exactly the six docs commits `0f6395ff`, `a6f398a4`, `eee3d00c`, `136c288d`, `d5fd43a1`, `7188098b` (plus the pipeline's commit of this plan file once made), no merge commit, and an empty diff; `test -d adws/gitContext && test -d adws/providers && ! grep -q '@paysdoc/devplatform' package.json && echo "in-repo untouched"` — expect `in-repo untouched`.
- Baseline no-regression on the untouched branch (nothing changed, so these must stay as recorded; run each command on its own so the exit code is visible — this shell is zsh, where `$pipestatus` is lowercase): `bunx tsc --noEmit`, `bunx tsc --noEmit -p adws/tsconfig.json`, `bun run lint`, `bun run lint:git-guard`, `bun run lint:docs-index`, `bun run test:unit`, `NODE_OPTIONS="--import tsx" bunx cucumber-js --tags "@regression"` — expect both typechecks and lint exit 0, the guard's PASS with its unchanged pre-migration banners (`Exempt packages (2)`; the empty-exempt-set banner is the re-run's exit criterion, not this patch's), docs-index 47 entries all passing, 180 test files / 3193 tests green, and the regression suite at 10 passed / 0 failed with the other 43 scenarios pending or undefined (the unchanged pre-migration state; `feature-729` is repointed by the re-run, not by this patch).
- Gate check (documenting, not passing — today it prints `MISSING` lines; the migration re-run starts only when it prints none): `npm view @paysdoc/devplatform versions --json` → `["1.0.0"]`, plus the Step 1 tarball command.
- Rebase status for the re-run (documenting): `git fetch -q origin dev && git merge-tree --write-tree origin/dev HEAD 2>&1 | grep '^CONFLICT'` — expect exactly one line, naming `README.md`. If `dev` has moved again and other paths appear, record them in the patch report for the operator's step (3); still no merge or rebase from this patch.
- The spec's own Validation Commands (`bun install`, lint, both typechecks, unit, `lint:git-guard` with an empty exempt set, `lint:docs-index`, `@regression`, the import grep, the directory check) and the #840 forge-name grep are the exit criteria of the **re-run**, not of this patch; they cannot pass until the migration is built on a published library version above 1.0.0.

## Patch Scope
**Lines of code to change:** 0 in-repo and 0 on GitHub (this plan file is the only committed artifact; the previous cycles' body edits are re-applied only if Step 2 finds one has regressed)
**Risk level:** low — verification only; no code commits, no dependency, no merge, no rebase, no push, no PR edit, no comment, no action on the library repository or on any issue
**Testing required:** the GitHub-state verification above, the unchanged baseline typechecks / lint / guard / docs-index / unit suite / regression suite, and the documented gate and rebase-status checks
