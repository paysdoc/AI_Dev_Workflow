# Patch: Gate still closed (seventh review) — re-verify, change nothing in-repo, nothing on GitHub, restate the six operator steps

## Metadata
adwId: `i7hs7a-adw-switchover-to-pa`
reviewChangeRequest: `Issue #1: The migration was not performed; none of the spec's four acceptance criteria are met. Verified on HEAD 6f55870d (clean tree; seven docs-only commits past merge-base 4d25d21c; git diff 4d25d21c --stat = README.md + seven markdown files under specs/, zero code). In-repo facts: adws/gitContext/ and adws/providers/ are present and the spec's validation grep git grep -nE "from ['\"][^'\"]*(gitContext|adws/providers)" -- '*.ts' '*.tsx' prints 161 lines (must print nothing); package.json has no @paysdoc/devplatform dependency; bun run lint:git-guard prints 'Exempt packages (2)', 'Sanctioned construction sites — 2 permanent, 0 sunset' (adws/providers/forgeProviders.ts still listed at constructionRule.ts:93) and 'Extraction-readiness scope — 10 entries'; adws/guard/extractionRule.ts exists and 'extraction-readiness' is still a ViolationRule member (violationTypes.ts:13); features/regression/upgrade/feature-729.feature cites adws/gitContext at lines 24, 36 and 81; .adw/conditional_docs.md has 14 adws/gitContext or adws/providers references; app_docs/feature-9gjajh-providers.md still exists. The typecheck, lint and regression suite are green only because the tree is the unchanged pre-migration baseline, not because the switchover works. Root cause (external, unchanged since the first review): spec Step 1's hard gate is closed. npm view @paysdoc/devplatform versions --json returns ["1.0.0"] (created 2026-09-10T13:28:15Z, modified 2026-09-10T13:28:16Z, nothing since); that version's exports map is closed and its barrels omit createGitHubTokenProvider, resolveBootstrapGitIdentity, resolveContextToken, ghAuthToken, isGitHubAppConfigured, getInstallationToken, createLiteralTokenProvider, createGhRepoApi, commitOps, branchOps and isLeaseRejection. Spec Step 1 forbids the only in-repo workaround (re-implementing those helpers in ADW re-creates the git/gh shell-out sites the empty exempt set must forbid), and Step 13 says the PR must not merge while Step 1 is blocked. PR #843 is already correctly parked: OPEN, isDraft=true, autoMergeRequest=null, blocker preamble present, 0 closes/fixes/resolves/implements lines, mergeStateStatus=DIRTY (README.md conflict against origin/dev, expected until the operator rebases). Resolution: Not patchable in this repository until the library ships a wider surface; do not add code, do not re-implement the helpers in ADW, and keep PR #843 a non-closing draft. Operator sequence: (1) fix the npm trusted-publisher (OIDC) grant for paysdoc/devplatform's release.yml so npm publish can succeed (release run 34503029087 failed with 403 OIDC permission denied); (2) land a new releasable feat: commit on library main widening the barrels — src/git/index.ts re-exporting commitOps, branchOps, isLeaseRejection and createLiteralTokenProvider, src/providers/github/index.ts re-exporting createGhRepoApi, createForgeCredentials, createGitHubTokenProvider, resolveBootstrapGitIdentity, resolveContextToken, ghAuthToken, isGitHubAppConfigured and getInstallationToken, plus the import-graph assertion that ./git pulls no adapter module (devplatform#11 is the natural vehicle; its pipeline branch is empty, so post ## Cancel and re-run it or open the PR by hand); v1.1.0 is tagged but unpublished and semantic-release treats it as released, so the OIDC fix alone publishes nothing; (3) confirm with npm view @paysdoc/devplatform versions and the spec's Step 1 tarball check that every listed symbol is present; (4) rebase this branch onto origin/dev, resolving the two README.md tree-listing hunks by keeping either side; (5) re-run the build from spec Step 2 pinning the exact published version through Step 13; (6) re-review against all four acceptance criteria and the spec's Validation Commands (empty exempt set, one sanctioned construction site, no extraction-readiness section, import grep silent, both directories gone, docs-index green, feature-729 repointed).`

## Issue Summary
**Original Spec:** specs/issue-840-adw-20l8es-adw-switchover-to-pa-sdlc_planner-devplatform-switchover.md
**Issue:** Seventh consecutive rejection for the same root cause: spec Step 1's gate is closed, so the switchover cannot be built. npm still carries only `@paysdoc/devplatform@1.0.0` (published 2026-09-10T13:28:16Z, nothing since), whose closed `exports` map and barrels omit every symbol the migration needs — the spec's Step 1 tarball check reports `MISSING` for all twelve review-listed symbols and `present` only for `git:GitContext`. The library cannot publish until the npm trusted-publisher (OIDC) grant for its `release.yml` is fixed **and** a new `feat:` commit lands on library `main` (semantic-release treats the never-published tag v1.1.0 as released, so the OIDC fix alone publishes nothing). The natural vehicle, paysdoc/devplatform#11, is still stalled: OPEN, no PR, no branch content, no operator directive since its pipeline's 2026-09-22T08:42:56Z error. The spec's Step 1 bans the only in-repo workaround (re-implementing the helpers in ADW re-creates the `git`/`gh` shell-out sites the empty exempt set forbids) and Step 13 says the PR must not merge while Step 1 is blocked. Nothing has moved since the sixth cycle: npm, library `main`, devplatform#11 and `origin/dev` are byte-for-byte where the sixth plan recorded them, and PR #843 is still the non-closing draft with the intact blocker preamble (the only PR event since is the pipeline's push of `6f55870d`, the sixth plan's docs commit — no body edit). The date-proof sentence in the PR body ("Every review of this PR so far … has rejected it for that reason, and the latest re-confirmed the gate is still closed") already covers a seventh rejection, so there is no body edit to make.
**Solution:** Change nothing anywhere: 0 source files, 0 dependencies, 0 code commits, no merge, no rebase, no conflict resolution, no push, no `gh pr edit`, no comment. Re-verify the gate, confirm PR #843's handoff is intact with the same checks the fifth and sixth cycles used (re-applying an earlier cycle's documented body edit only if a check has become false), leave the README.md conflict for the operator's rebase, and restate the review's six operator steps with the exact barrel lists. This plan file under `specs/patch/` is the only artifact; the pipeline commits it as docs, as it did `136c288d`, `eee3d00c`, `a6f398a4`, `0f6395ff` and `6f55870d`.

### Verified state at patch-plan time (2026-09-22, after the seventh review)
- **npm:** `npm view @paysdoc/devplatform versions time --json` → versions `["1.0.0"]`; `created` 2026-09-10T13:28:15.899Z, `1.0.0` 2026-09-10T13:28:16.219Z, `modified` 2026-09-10T13:28:16.795Z, nothing since. `npm pack @paysdoc/devplatform@latest` yields `paysdoc-devplatform-1.0.0.tgz`; importing `package/dist/providers/index.js` and `package/dist/git/index.js` reports `MISSING` for `providers:` `createForgeCredentials`, `createGhRepoApi`, `createGitHubTokenProvider`, `resolveBootstrapGitIdentity`, `resolveContextToken`, `ghAuthToken`, `isGitHubAppConfigured`, `getInstallationToken` and `git:` `createLiteralTokenProvider`, `commitOps`, `branchOps`, `isLeaseRejection`; `present` only for `git:GitContext`. Gate closed.
- **Library `paysdoc/devplatform`:** `main` = `cba596b8` (2026-09-14T09:48:31Z, merge of PR #12) — unchanged since the third cycle. Tags `v1.1.0` (`ba168795`), `v1.0.0` (`4634ee68`); `gh release list` empty; zero open PRs. `release.yml` runs: `34829911126` (2026-09-14, success, released nothing — "no relevant changes"), `34503029087` (2026-09-10, **failure** at `npm publish`, 403 OIDC permission denied), `34477324029` (2026-09-10, success). Nothing new since the sixth cycle.
- **devplatform#11 (L4 widening):** OPEN, labels `adw:unverified` + `adw:feature`, `updatedAt` 2026-09-22T08:42:56Z, 483 comments; the last three are pipeline `k3pj1m-widen-the-public-sur`'s "ADW Workflow Resuming" (08:41:32Z, 08:41:41Z) and "ADW Workflow Error" (08:42:56Z). No operator directive since, no PR.
- **PR #843 (`paysdoc/AI_Dev_Workflow`):** OPEN, `isDraft=true`, base `dev`, head `chore-issue-840-adw-switchover-devplatform-library`, `autoMergeRequest=null`, `mergeable=CONFLICTING`, `mergeStateStatus=DIRTY` (expected until the operator's rebase). `updatedAt` 2026-09-22T10:01:33Z = the pipeline's push of `6f55870d`; the body is unchanged since the fifth cycle's edit (sha256 of `gh pr view 843 --json body --jq .body` starts `911d484a5f32adbd`). Body checks: `preamble-present=true` (contains `no relevant changes`, `createForgeCredentials`, `devplatform#11`, `k3pj1m`, `Tracks #840`, `Every review of this PR so far`), `old-text-gone=true` (none of `Prerequisite: paysdoc/devplatform#11 merged`, `@paysdoc/devplatform@1.0.0`, `Three reviews`, `is conflict-free as of 2026-09-22`), `rebase-note-refreshed=true` (contains `#844 and #847 landed after it was cut` and `nothing else conflicts`); `grep -ciE '^(closes|fixes|resolves|implements) '` over the body prints `0`.
- **Issue #840:** OPEN, `hitl` + `adw:chore`. Last five comments are the pipeline's Review Failed (09:34:23Z, fifth), Review Running (09:46:31Z), Review Failed (09:52:18Z, sixth), Review Running (10:01:39Z) and Review Failed (10:04:52Z, seventh — carries this cycle's blocker text and cites HEAD `6f55870d`).
- **`origin/dev`:** still `1ec1295c` = merge of PR #847 (2026-09-22T09:13:56Z). This branch is 18 behind / 7 ahead; merge-base `4d25d21c` unchanged. `git merge-tree --write-tree origin/dev HEAD` reports exactly one `CONFLICT (content)` line, `README.md`, and `README.md` is the only path changed on both sides since the base — the same two docs-only directory-tree hunks (`adws/providers/__tests__/` and `adws/guard/__tests__/` listings, identical file names, different trailing comments) the fifth and sixth plans described. Either side is a correct README.
- **This branch:** clean tree; HEAD `6f55870d` = `origin/chore-issue-840-adw-switchover-devplatform-library`; seven docs-only commits past base `4d25d21c` (`7188098b` spec, `d5fd43a1` README, `136c288d`, `eee3d00c`, `a6f398a4`, `0f6395ff`, `6f55870d` patch plans); `git diff 4d25d21c --stat` = `README.md` + seven markdown files under `specs/`, zero code.
- **In-repo acceptance facts (all still pre-migration, matching the review line by line):** `adws/gitContext/` (19 entries) and `adws/providers/` present; `package.json` has zero `@paysdoc/devplatform` references; `node_modules/@paysdoc` absent; the spec's import grep prints 161 lines; the #840 forge-name grep (`git grep -lE "createGitHub|createGitLab|GitLabApiClient|JiraApiClient|JiraIssueTracker" -- adws ':!**/__tests__/**'`) prints 16 files; `'extraction-readiness'` is still a `ViolationRule` member (`adws/guard/violationTypes.ts:13`); `adws/providers/forgeProviders.ts` is still a sanctioned construction site (`adws/guard/constructionRule.ts:93`); `adws/guard/extractionRule.ts`, `extractionRule.test.ts` and `extractionRule.integration.test.ts` exist; `features/regression/upgrade/feature-729.feature` cites `adws/gitContext` at lines 24, 36, 81; `.adw/conditional_docs.md` has 14 `adws/gitContext`/`adws/providers` references; `app_docs/feature-9gjajh-providers.md` exists.
- **Baseline on the untouched branch (run this cycle):** `bunx tsc --noEmit` exit 0; `bunx tsc --noEmit -p adws/tsconfig.json` exit 0; `bun run lint` exit 0 (`eslint .` prints nothing); `bun run lint:git-guard` → PASS, 286 files scanned, with the pre-migration banners (`Exempt packages (2)`, `Sanctioned construction sites — 2 permanent, 0 sunset`, `Extraction-readiness scope — 10 entries`); `bun run lint:docs-index` → all checks pass (47 entries, 45 docs, band `[25, 60]`); `bun run test:unit` → 180 test files / 3193 tests passed (the three `failed to push some refs to '/tmp/adw-claim-integ-sandbox-…/remote.git'` lines are the upgrade-claim integration sandbox exercising lease rejection — expected output, not failures); `NODE_OPTIONS="--import tsx" bunx cucumber-js --tags "@regression"` → 53 scenarios: 10 passed, 0 failed, 42 pending, 1 undefined (357 steps: 228 passed, 86 skipped, 42 pending, 1 undefined) — cucumber's exit code 1 comes from its strict handling of the pending/undefined scenarios, not from any assertion failure; the numbers are exactly the review's. These guard banners and the feature-729 repoint are what the re-run changes, not this patch.

## Files to Modify
Use these files to implement the patch:

- **None in this repository.** The spec is a write-once artifact (plan files are not updated after the build phase begins) and every in-repo change it describes is gated behind Step 1. Do not add, edit or delete any tracked file; do not add the dependency; do not merge `origin/dev` into this branch, rebase, resolve the README.md conflict or push; do not touch the two extracted directories, the guard, the feature file or the docs. This patch plan under `specs/patch/` is the only file this cycle produces; the pipeline commits it as docs, which is not a code commit.
- **No GitHub state is touched this cycle.** PR #843's draft flag and body are already current (verified above), so there is no `gh pr edit`, no `gh pr ready --undo`, no comment on #840, on PR #843 or on paysdoc/devplatform#11, and no label change. The only GitHub write this plan permits is conditional: re-applying an earlier cycle's documented body edit if — and only if — the Step 2 check finds that edit has regressed.
- Scratch files under `/tmp/` only: a `mktemp -d` directory for the tarball check.

## Implementation Steps
IMPORTANT: Execute every step in order, top to bottom.

### Step 1: Re-verify the gate before touching anything
- Run the version listing and the spec's Step 1 tarball check against the review's full symbol list. Use `mktemp -d` and leave the directory in place afterwards (the pre-tool-use hook blocks recursive deletes; the directory is harmless):
  ```
  npm view @paysdoc/devplatform versions --json
  D=$(mktemp -d) && cd "$D" && npm pack @paysdoc/devplatform@latest --silent >/dev/null && tar xzf paysdoc-devplatform-*.tgz && node -e "(async()=>{const p=await import('$D/package/dist/providers/index.js');const g=await import('$D/package/dist/git/index.js');for(const s of ['createForgeCredentials','createGhRepoApi','createGitHubTokenProvider','resolveBootstrapGitIdentity','resolveContextToken','ghAuthToken','isGitHubAppConfigured','getInstallationToken'])console.log((s in p?'present':'MISSING')+' providers:'+s);for(const s of ['createLiteralTokenProvider','commitOps','branchOps','isLeaseRejection'])console.log((s in g?'present':'MISSING')+' git:'+s)})()"
  ```
- Expected today: versions `["1.0.0"]` and every one of the twelve lines `MISSING`. Record the output for the patch report and continue to Step 2.
- **If instead every line prints `present`** (the gate opened between review and patch): STOP this patch without editing anything. Report that the migration re-run is now due and must start with operator step (4), the rebase onto `origin/dev` (resolving the README.md conflict described above), then spec Step 2 pinning that exact version. Do not attempt the ~140-file migration inside this patch cycle — it needs the rebase first and is a `hitl` change the operator gates.

### Step 2: Confirm PR #843's handoff is still intact — read-only unless a check has regressed
- Run the combined check:
  ```
  gh pr view 843 -R paysdoc/AI_Dev_Workflow --json state,isDraft,autoMergeRequest,mergeStateStatus,body --jq '"state=\(.state)", "isDraft=\(.isDraft)", "autoMerge=\(.autoMergeRequest)", "mergeState=\(.mergeStateStatus)", "preamble-present=\(.body | contains("no relevant changes") and contains("createForgeCredentials") and contains("devplatform#11") and contains("k3pj1m") and contains("Tracks #840") and contains("Every review of this PR so far"))", "old-text-gone=\(.body | (contains("Prerequisite: paysdoc/devplatform#11 merged") or contains("@paysdoc/devplatform@1.0.0") or contains("Three reviews") or contains("is conflict-free as of 2026-09-22")) | not)", "rebase-note-refreshed=\(.body | contains("nothing else conflicts") and contains("#844 and #847 landed after it was cut"))"'
  gh pr view 843 -R paysdoc/AI_Dev_Workflow --json body --jq .body | grep -ciE '^(closes|fixes|resolves|implements) '
  gh pr view 843 -R paysdoc/AI_Dev_Workflow --json body --jq .body | shasum -a 256 | cut -c1-16
  ```
- Expected (and verified at plan time): `state=OPEN`, `isDraft=true`, `autoMerge=null`, `mergeState=DIRTY`, the three body checks `true`, `0` closing-keyword lines, and body hash `911d484a5f32adbd`. **When everything matches, make no `gh pr edit`:** the date-proof sentence already covers the seventh review, and rewriting an accurate body only churns the PR timeline. (`updatedAt` is not a useful check any more — it moves with every docs push, including the pipeline's commit of this plan; the body hash is what proves no edit was made.)
- Only if `isDraft=false`: `gh pr ready --undo 843 -R paysdoc/AI_Dev_Workflow`.
- Only if `preamble-present` or `old-text-gone` is `false` (the body drifted): re-apply the preamble and checklist edit from `specs/patch/patch-adw-i7hs7a-adw-switchover-to-pa-gate-closed-refresh-pr-843-release-handoff.md` Step 2, then the count-sentence replacement from `specs/patch/patch-adw-i7hs7a-adw-switchover-to-pa-gate-closed-verify-only-operator-handoff.md` Step 2. Only if `rebase-note-refreshed` is `false`: re-apply the single parenthetical replacement from `specs/patch/patch-adw-i7hs7a-adw-switchover-to-pa-gate-closed-hold-draft-refresh-rebase-note.md` Step 2. Re-run the combined check afterwards; all three must read `true` (the hash will then differ, which is expected and must be noted in the report).
- Do **not** close the PR (the re-run reuses this branch and adwId; ADW resolves the existing PR by branch name). Do **not** introduce any `Closes`/`Fixes`/`Resolves`/`Implements` line. Do **not** post on paysdoc/devplatform#11 (`## Retry` cannot succeed against an empty branch, and `## Cancel` kills a process and removes a worktree in another repository — the operator's call) and do **not** post on #840 (the pipeline's Review Failed comment at 10:04:52Z already carries this blocker's Issue #1 text; the operator sequence lives in PR #843's body and in this plan, so a second copy is noise).

### Step 3: Leave the branch exactly as found — conflict included — and hand back the six operator steps
- Verify nothing in-repo moved: `git status --short` prints nothing beyond this plan file (until the pipeline commits it); `git log --oneline 4d25d21c..HEAD` prints exactly `6f55870d`, `0f6395ff`, `a6f398a4`, `eee3d00c`, `136c288d`, `d5fd43a1`, `7188098b` (plus, after the pipeline commits it, the docs-only commit adding this plan); `git diff --stat` is empty. No code commit, no dependency, no merge of `origin/dev`, no rebase, no push from this patch.
- Explicitly leave the README.md conflict unresolved. Reasons, for the patch report: the review's resolution assigns the rebase to operator step (4) as the first move of the re-run; the pipeline resolves conflicts itself at merge time (`adws/phases/autoMergePhase.ts` and `adws/triggers/autoMergeHandler.ts` invoke `/resolve_conflict`), so a `DIRTY` draft is not a merge hazard; the gate has no ETA, so a merge or rebase made now goes stale with every further `dev` commit and adds a non-docs merge commit to a branch the review expects to stay docs-only; and the conflict is two hunks of README tree-listing comments that the rebase resolves in seconds by keeping either side.
- In the patch report, restate for the operator, in the review's order:
  1. Fix the npm trusted-publisher (OIDC) grant for `paysdoc/devplatform`'s `release.yml` so `npm publish` can succeed (release run `34503029087` failed with 403 OIDC permission denied).
  2. Land a new releasable `feat:` commit on library `main` widening the barrels: `src/git/index.ts` re-exporting `commitOps`, `branchOps`, `isLeaseRejection` and `createLiteralTokenProvider`; `src/providers/github/index.ts` re-exporting `createGhRepoApi`, `createForgeCredentials`, `createGitHubTokenProvider`, `resolveBootstrapGitIdentity`, `resolveContextToken`, `ghAuthToken`, `isGitHubAppConfigured` and `getInstallationToken`; plus the import-graph assertion that `./git` pulls no adapter module. devplatform#11 is the natural vehicle, but its pipeline branch is empty (no plan file, no implementation commits), so post `## Cancel` on it and let a fresh run plan/build it, or open the PR by hand. v1.1.0 is tagged but unpublished and semantic-release treats it as released, so the OIDC fix alone publishes nothing — the new `feat:` commit is what cuts the next minor.
  3. Confirm with `npm view @paysdoc/devplatform versions` and the Step 1 tarball check above that every listed symbol prints `present`.
  4. Rebase this branch onto `origin/dev` (#844 and #847 changed the consumer inventory and README); expect the single docs-only README.md conflict described above, keep either side of both hunks, nothing else conflicts.
  5. Re-run the build from spec Step 2 pinning the exact published version through Step 13, following the updated #840 body for the launch boundary (`createForgeCredentials({ forge, identity, deps })`; no production module imports a forge-named symbol — the #840 forge-name grep must print nothing).
  6. Re-review against all four acceptance criteria and the spec's Validation Commands: empty exempt set, exactly one sanctioned construction site (`adws/core/launchGitContext.ts`), no extraction-readiness section, the import grep silent, both directories gone, `lint:docs-index` green, feature-729 repointed.
- Note for the operator, since each cycle before the gate opens reproduces this result: the orchestrator's review→patch loop is bounded by `MAX_REVIEW_RETRY_ATTEMPTS` (default 3, per the orchestrator header comments in `adws/adwChore.tsx`), so a run stops on its own; every `## Retry` on #840 before steps (1)–(3) are done just re-spends the same three cycles.

## Validation
Execute every command to validate the patch is complete with zero regressions.

- PR state: the Step 2 combined check — expect `state=OPEN`, `isDraft=true`, `autoMerge=null`, `mergeState=DIRTY` (expected until the operator's rebase; `CLEAN` is acceptable only if the operator has already rebased), `true` for the three body checks, `0` closing-keyword lines, and body hash `911d484a5f32adbd` (proving this cycle wrote nothing to the PR body; a different hash is acceptable only if Step 2 had to re-apply a regressed edit, and then the three checks must still be `true`).
- In-repo untouched: `git status --short && git log --oneline 4d25d21c..HEAD && git diff --stat` — expect nothing tracked-and-modified, exactly the seven docs commits `6f55870d`, `0f6395ff`, `a6f398a4`, `eee3d00c`, `136c288d`, `d5fd43a1`, `7188098b` (plus the pipeline's commit of this plan file once made), no merge commit, and an empty diff; `test -d adws/gitContext && test -d adws/providers && ! grep -q '@paysdoc/devplatform' package.json && echo "in-repo untouched"` — expect `in-repo untouched`.
- Baseline no-regression on the untouched branch (nothing changed, so these must stay as recorded; run each command on its own so the exit code is visible — this shell is zsh, where `$pipestatus` is lowercase): `bunx tsc --noEmit`, `bunx tsc --noEmit -p adws/tsconfig.json`, `bun run lint`, `bun run lint:git-guard`, `bun run lint:docs-index`, `bun run test:unit`, `NODE_OPTIONS="--import tsx" bunx cucumber-js --tags "@regression"` — expect both typechecks and lint exit 0, the guard's PASS with its unchanged pre-migration banners (`Exempt packages (2)`; the empty-exempt-set banner is the re-run's exit criterion, not this patch's), docs-index 47 entries all passing, 180 test files / 3193 tests green, and the regression suite at 10 passed / 0 failed with 42 pending and 1 undefined (cucumber exits 1 on those pending/undefined scenarios in strict mode; zero assertion failures is the pass condition here, unchanged from the pre-migration baseline; `feature-729` is repointed by the re-run, not by this patch).
- Gate check (documenting, not passing — today it prints twelve `MISSING` lines; the migration re-run starts only when it prints none): `npm view @paysdoc/devplatform versions --json` → `["1.0.0"]`, plus the Step 1 tarball command.
- Rebase status for the re-run (documenting): `git fetch -q origin dev && git merge-tree --write-tree origin/dev HEAD 2>&1 | grep '^CONFLICT'` — expect exactly one line, naming `README.md`. If `dev` has moved again and other paths appear, record them in the patch report for operator step (4); still no merge or rebase from this patch.
- The spec's own Validation Commands (`bun install`, lint, both typechecks, unit, `lint:git-guard` with an empty exempt set, `lint:docs-index`, `@regression`, the import grep, the directory check) and the #840 forge-name grep are the exit criteria of the **re-run**, not of this patch; they cannot pass until the migration is built on a published library version above 1.0.0.

## Patch Scope
**Lines of code to change:** 0 in-repo and 0 on GitHub (this plan file is the only committed artifact; the previous cycles' body edits are re-applied only if Step 2 finds one has regressed)
**Risk level:** low — verification only; no code commits, no dependency, no merge, no rebase, no push, no PR edit, no comment, no action on the library repository or on any issue
**Testing required:** the GitHub-state verification above, the unchanged baseline typechecks / lint / guard / docs-index / unit suite / regression suite, and the documented gate and rebase-status checks
