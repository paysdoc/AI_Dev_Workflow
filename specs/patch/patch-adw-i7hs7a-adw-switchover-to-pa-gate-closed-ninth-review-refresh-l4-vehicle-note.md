# Patch: Gate still closed (ninth review) — change nothing in-repo, refresh PR #843's devplatform#11 paragraph now that the L4 vehicle is running, restate the six operator steps

## Metadata
adwId: `i7hs7a-adw-switchover-to-pa`
reviewChangeRequest: `Issue #1: The migration was not performed and none of the spec's four acceptance criteria are met. Verified on HEAD 6c33d150 (merge-base 4d25d21c, 18 commits behind origin/dev, clean tree): git diff 4d25d21c..HEAD touches only README.md, the plan spec and eight specs/patch/*.md files, zero code. In-repo facts: adws/gitContext/ and adws/providers/ are present; the spec's validation grep for relative imports into them prints 161 lines (must print nothing); package.json has no @paysdoc/devplatform and no dependency was added; bun run lint:git-guard prints 'Exempt packages (2)', 'Sanctioned construction sites - 2 permanent, 0 sunset' (adws/providers/forgeProviders.ts still listed at constructionRule.ts:93) and 'Extraction-readiness scope - 10 entries'; adws/guard/extractionRule.ts and both its tests exist and 'extraction-readiness' is still a ViolationRule member (violationTypes.ts:13); features/regression/upgrade/feature-729.feature lines 24, 36 and 81 still cite adws/gitContext; app_docs/feature-9gjajh-providers.md still exists and .adw/conditional_docs.md has 14 adws/gitContext or adws/providers references. Root cause, re-verified independently in this review and unchanged since the first review: spec Step 1's gate is closed. npm view @paysdoc/devplatform versions returns only 1.0.0 (published 2026-09-10, nothing since); its exports map is closed to ., ./providers, ./git and ./package.json; the Step 1 tarball check on paysdoc-devplatform-1.0.0.tgz reports MISSING for providers: createForgeCredentials, createGhRepoApi, createGitHubTokenProvider, resolveBootstrapGitIdentity, resolveContextToken, ghAuthToken, isGitHubAppConfigured, getInstallationToken and git: createLiteralTokenProvider, commitOps, branchOps, isLeaseRejection, with only git:GitContext present. The updated issue 840 body's gate (a version above 1.0.0 exporting createForgeCredentials from /providers and createLiteralTokenProvider from /git) is therefore unmet, and that body says 'stop and report; do not open a PR'. On paysdoc/devplatform: tags v1.0.0 and v1.1.0 exist but v1.1.0 was never published (release run 34503029087 failed at npm publish with 403 OIDC permission denied), gh release list is empty, there are zero open PRs, and the L4 widening issue devplatform#11 is OPEN with no PR. Spec Step 1 forbids re-implementing the missing helpers in ADW (it recreates the git/gh shell-out sites the empty exempt set must forbid) and Step 13 forbids merging while Step 1 is blocked. Typecheck (both configs), lint, lint:git-guard, lint:docs-index and the @regression proof (10 passed / 0 failed / 43 skipped; no @adw-840 scenarios exist) are green only because the tree is the unchanged pre-migration baseline. PR 843 is already parked correctly: OPEN, isDraft=true, no closes/fixes/resolves/implements line, blocker preamble present and current, mergeable=CONFLICTING because git merge-tree reports a single docs-only conflict in README.md's directory-tree listing (both sides add the same test-file names with different trailing comments), expected until the operator rebases. Resolution: Not patchable in this repository until the library ships a wider surface: add no code, no dependency and no ADW-side re-implementation of the missing helpers; do not merge, rebase or resolve the README.md conflict; keep PR 843 a non-closing draft (its body is current, no gh pr edit is needed); and do not re-spend review/patch cycles or post ## Retry on issue 840 before the gate opens, since every cycle reproduces this result. Operator sequence outside this repository: (1) fix the npm trusted-publisher (OIDC) grant for paysdoc/devplatform's release.yml so npm publish can succeed; (2) land a new releasable feat: commit on library main widening src/git/index.ts (commitOps, branchOps, isLeaseRejection, createLiteralTokenProvider) and src/providers/github/index.ts (createGhRepoApi, createForgeCredentials, createGitHubTokenProvider, resolveBootstrapGitIdentity, resolveContextToken, ghAuthToken, isGitHubAppConfigured, getInstallationToken) plus the import-graph assertion that ./git pulls no adapter module; devplatform#11 is the vehicle but its pipeline branch is empty, so ## Cancel and re-run it or open the PR by hand (semantic-release treats the unpublished v1.1.0 as released, so the OIDC fix alone publishes nothing); (3) confirm with npm view @paysdoc/devplatform versions and the spec's Step 1 tarball check that every listed symbol prints present; (4) rebase this branch onto origin/dev (issues 844 and 847 landed after it was cut), keeping either side of the two README.md tree-listing hunks, nothing else conflicts; (5) re-run the build from spec Step 2 through Step 13 pinning the exact published version and following the updated issue 840 launch-boundary mapping (createForgeCredentials({ forge, identity, deps }); no production module imports a forge-named symbol); (6) re-review against all four acceptance criteria and the spec's Validation Commands (empty exempt set, exactly one sanctioned construction site at adws/core/launchGitContext.ts, no extraction-readiness section, the import grep silent, both directories gone, lint:docs-index green, feature-729 repointed).`

## Issue Summary
**Original Spec:** specs/issue-840-adw-20l8es-adw-switchover-to-pa-sdlc_planner-devplatform-switchover.md
**Issue:** Ninth consecutive rejection for the same external root cause: spec Step 1's gate is closed, so the switchover cannot be built. npm still carries only `@paysdoc/devplatform@1.0.0` (published 2026-09-10T13:28:16Z, nothing since) with a closed `exports` map (`.`, `./providers`, `./git`, `./package.json`); the spec's Step 1 tarball check reports `MISSING` for all twelve review-listed symbols and `present` only for `git:GitContext`. Nothing has moved on npm, library `main` (`cba596b8`), the release runs (last: `34829911126`, 2026-09-14) or `origin/dev` (`1ec1295c`) since the eighth cycle. Spec Step 1 bans the only in-repo workaround (re-implementing the helpers in ADW re-creates the `git`/`gh` shell-out sites the empty exempt set forbids) and Step 13 says the PR must not merge while Step 1 is blocked, so nothing in this repository can be patched. **One fact has changed since the review's snapshot, and it makes one sentence of the review's resolution and one paragraph of PR #843's body stale:** paysdoc/devplatform#11, the L4 widening vehicle, is no longer a stalled issue with an empty branch. Its old `k3pj1m-widen-the-public-sur` run was cleared and a fresh run, `ifp6cg-widen-the-public-sur`, started at 2026-09-22T10:37:31Z; its branch `feature-issue-11-widen-public-exports-adw-switchover` (tip `458f90cb`) is 15 commits ahead of `main` and already contains the barrel widening; the run reported `Build Completed` at 10:50:00Z and `ADW Unverified — No JUnit Report Emitted` at 10:54:01Z; no PR yet. PR #843's paragraph 7 still says the issue "has no PR and no remote branch" and tells the operator to "`## Cancel` it so a fresh run plans and builds it" — following that today would kill the run that is finally producing the widening. Everything else about PR #843 is intact: OPEN, `isDraft=true`, no closing keyword, the date-proof review-count sentence already covers a ninth rejection, the rebase note is current, and the body hash is still `911d484a5f32adbd`.
**Solution:** Change nothing in this repository: 0 source files, 0 dependencies, 0 code commits, no merge, no rebase, no README.md conflict resolution, no push from this patch. Re-verify the gate, then make exactly one GitHub write — replace PR #843's paragraph 7 (the devplatform#11 paragraph) with the current state of the vehicle so the operator handoff stops recommending a destructive `## Cancel` — and otherwise keep PR #843 the non-closing draft it already is. This is the single, deliberate deviation from the review's literal "no gh pr edit is needed": that sentence rested on "its body is current", which stopped being true when the fresh run started six minutes before the review began; the review's own operator step (2) ("its pipeline branch is empty, so ## Cancel and re-run it") is stale for the same reason and is corrected in the restated sequence below. No comment on #840, on PR #843 or on devplatform#11; no label change; no action on the library repository. This plan file under `specs/patch/` is the only in-repo artifact; the pipeline commits it as docs, as it did the eight earlier plan commits.

### Verified state at patch-plan time (2026-09-22, ~10:55Z, after the ninth review)
- **npm:** `npm view @paysdoc/devplatform versions time exports --json` → versions `["1.0.0"]`; `created` 2026-09-10T13:28:15.899Z, `1.0.0` 2026-09-10T13:28:16.219Z, `modified` 2026-09-10T13:28:16.795Z; `exports` exactly `.`, `./providers`, `./git`, `./package.json`. `npm pack @paysdoc/devplatform@latest` → `paysdoc-devplatform-1.0.0.tgz`; importing `package/dist/providers/index.js` and `package/dist/git/index.js` prints `MISSING` for `providers:` `createForgeCredentials`, `createGhRepoApi`, `createGitHubTokenProvider`, `resolveBootstrapGitIdentity`, `resolveContextToken`, `ghAuthToken`, `isGitHubAppConfigured`, `getInstallationToken` and `git:` `createLiteralTokenProvider`, `commitOps`, `branchOps`, `isLeaseRejection`; `present` only for `git:GitContext`. Gate closed.
- **Library `paysdoc/devplatform` — unchanged parts:** `main` = `cba596b8` (2026-09-14T09:48:31Z, merge of PR #12; 2 commits past tag v1.1.0). Tags `v1.1.0` (`ba168795`), `v1.0.0` (`4634ee68`); `gh release list` empty; zero open PRs. `release.yml` runs: `34829911126` (2026-09-14, success, released nothing — "no relevant changes"), `34503029087` (2026-09-10T16:35:23Z, **failure** at `npm publish`, 403 OIDC permission denied), `34477324029` (the 1.0.0 publish), two earlier successes; nothing new. `main` already exports both #840 gate symbols: `createLiteralTokenProvider` at `src/git/index.ts:44` and `createForgeCredentials` (`src/providers/forgeCredentials.ts:131`, re-exported by `src/providers/index.ts`) — they are in the unpublished v1.1.0, which is why the OIDC fix alone publishes nothing (semantic-release treats v1.1.0 as released) and a new releasable commit is needed. `release.config.js` extends the conventional-commits header pattern with an optional `<agent-name>: ` prefix, so `build-agent: feat: …` commits do count as `feat`.
- **Library — what moved (the material change this cycle):** devplatform#11 ("Widen the public surface for the ADW switchover (L4)") is OPEN, `adw:unverified` + `adw:feature`, `updatedAt` 2026-09-22T10:54:01Z, now 12 comments — the old run's ~480 comments are gone and every remaining comment belongs to the fresh run `ifp6cg-widen-the-public-sur`: Workflow Started 10:37:31Z, Issue Classified 10:39:39Z, Branch Created 10:39:40Z (`feature-issue-11-widen-public-exports-adw-switchover`), Committing Plan 10:39:41Z, Aligning Plan and Scenarios 10:42:56Z, Plan and Scenarios Aligned 10:46:48Z, Running Build 10:46:57Z, Build Progress 10:48:00Z / 10:49:11Z, **Build Completed 10:50:00Z**, Committing Build 10:50:01Z, **ADW Unverified — No JUnit Report Emitted 10:54:01Z**. Branch tip `458f90cb`, 15 ahead / 2 behind `main`: `9e5ab440` plan, `fdc54569` alignment, `de65e20d` "widen the public surface", `44398127` table-driven `smoke:package` key check, `80cbf1d4`/`25d0f039`/`ed5763be`/`57d63533` drive `createGitHubTokenProvider`+`resolveContextToken`, `resolveBootstrapGitIdentity`+`isGitHubAppConfigured`, `getInstallationToken`+`ghAuthToken`, `createGhRepoApi` through the providers entry point, `6d4bc247` `commitOps`/`isLeaseRejection`/`branchOps` through the git entry point, `26f40c42` the four `@adw-11` packaging scenarios, `60189d9e` docs (all 2026-09-10/11), then four operator hygiene commits today (10:31–10:40Z) removing committed `logs/` and updating `.gitignore`. On the branch, `src/git/index.ts` adds `export { commitOps, isLeaseRejection } from './commitOps.js'` and `export { branchOps } from './branchOps.js'`; `src/providers/github/index.ts` adds `createGitHubTokenProvider`, `resolveBootstrapGitIdentity`, `resolveContextToken`, `ghAuthToken`, `isGitHubAppConfigured`, `getInstallationToken`, `createGhRepoApi`. Together with the two gate symbols already on `main`, that is every one of the twelve `MISSING` symbols. `gh pr list -R paysdoc/devplatform --head feature-issue-11-widen-public-exports-adw-switchover --state all` → none (no PR yet).
- **PR #843 (`paysdoc/AI_Dev_Workflow`):** OPEN, `isDraft=true`, base `dev`, `autoMergeRequest=null`, `mergeable=CONFLICTING`, `mergeStateStatus=DIRTY` (expected until the operator's rebase), 9 commits, 1 comment, `updatedAt` 2026-09-22T10:26:29Z = the pipeline's push of `6c33d150`. Body unchanged since the fifth cycle (sha256 prefix `911d484a5f32adbd`): `preamble-present=true`, `old-text-gone=true`, `rebase-note-refreshed=true`, `0` closing-keyword lines. Paragraph 7 (the line beginning `> **The natural (2) is paysdoc/devplatform#11**`) reads "has no PR and no remote branch. Its pipeline (`k3pj1m-widen-the-public-sur`) errored three times … Either `## Cancel` it so a fresh run plans and builds it, or open the PR by hand" — stale as of 10:37:31Z.
- **Issue #840:** OPEN, `hitl` + `adw:chore`, 582 comments, `updatedAt` 2026-09-22T10:47:48Z. The last two are the pipeline's Review Running (10:43:11Z) and Review Failed (10:47:48Z, ninth — carries this cycle's Issue #1 text and cites `HEAD 6c33d150`).
- **`origin/dev`:** still `1ec1295c` (merge of PR #847). This branch is 18 behind / 9 ahead; merge-base `4d25d21c` unchanged. `git merge-tree --write-tree origin/dev HEAD` reports exactly one `CONFLICT (content)` in `README.md`, and `README.md` is the only path changed on both sides since the base — the same two docs-only directory-tree hunks (`adws/providers/__tests__/` and `adws/guard/__tests__/` listings; identical file names, different trailing comments). Either side is a correct README.
- **This branch:** clean tree; HEAD `6c33d150` (2026-09-22T10:26:23Z) = `origin/chore-issue-840-adw-switchover-devplatform-library`; nine docs-only commits past `4d25d21c` (`7188098b` spec, `d5fd43a1` README, `136c288d`, `eee3d00c`, `a6f398a4`, `0f6395ff`, `6f55870d`, `b6f1dc93`, `6c33d150` patch plans); `git diff --stat 4d25d21c..HEAD` = `README.md`, the spec and eight `specs/patch/*.md`, zero code.
- **In-repo acceptance facts (all still pre-migration, matching the review line by line):** `adws/gitContext/` (19 entries) and `adws/providers/` present; `package.json` has 0 `@paysdoc/devplatform` references; `node_modules/@paysdoc` absent; the spec's import grep prints 161 lines; the #840 forge-name grep (`git grep -lE "createGitHub|createGitLab|GitLabApiClient|JiraApiClient|JiraIssueTracker" -- adws ':!**/__tests__/**'`) prints 16 files; `'extraction-readiness'` is still a `ViolationRule` member (`adws/guard/violationTypes.ts:13`); `adws/providers/forgeProviders.ts` is still a sanctioned construction site (`adws/guard/constructionRule.ts:93`); `adws/guard/extractionRule.ts` and its two tests exist; `features/regression/upgrade/feature-729.feature` cites `adws/gitContext` at lines 24, 36, 81; `.adw/conditional_docs.md` has 14 `adws/gitContext`/`adws/providers` references; `app_docs/feature-9gjajh-providers.md` exists. None of `.adw/conditional_docs.md`'s conditions apply to this patch: no code path is touched.
- **Baseline on the untouched branch (run this cycle):** `bunx tsc --noEmit` exit 0; `bunx tsc --noEmit -p adws/tsconfig.json` exit 0; `bun run lint` exit 0 (`eslint .` prints nothing); `bun run lint:git-guard` PASS with the pre-migration banners (`Exempt packages (2)`, `Sanctioned construction sites — 2 permanent, 0 sunset`, `Extraction-readiness scope — 10 entries, widen only (#816), 59 files scanned`); `bun run lint:docs-index` all checks pass (47 entries, 45 docs, band `[25, 60]`); `bun run test:unit` 180 test files / 3193 tests passed, exit 0; `NODE_OPTIONS="--import tsx" bunx cucumber-js --tags "@regression"` 53 scenarios: 10 passed, 0 failed, 42 pending, 1 undefined (357 steps: 228 passed, 86 skipped, 42 pending, 1 undefined); cucumber exits 1 on the pending/undefined scenarios in strict mode, zero assertion failures — identical to the eighth cycle and to the review's numbers (its "43 skipped" is the 42 pending + 1 undefined). The `publishPrProof: failed to upload calculator/calc.png` warning is the proof-publishing step's expected offline behaviour, not a failure.

## Files to Modify
Use these files to implement the patch:

- **None in this repository.** The spec is a write-once artifact and every in-repo change it describes is gated behind Step 1. Do not add, edit or delete any tracked file; do not add the dependency; do not merge `origin/dev` into this branch, rebase, resolve the README.md conflict or push; do not touch the two extracted directories, the guard, the feature file or the docs. This patch plan under `specs/patch/` is the only file this cycle produces; the pipeline commits it as docs, which is not a code commit.
- **One GitHub write:** the body of PR #843 on `paysdoc/AI_Dev_Workflow` — replace its paragraph 7 (the single line beginning `> **The natural (2) is paysdoc/devplatform#11**`) with the refreshed paragraph in Step 2. Nothing else in the body changes; the draft flag, base, title, labels and comments are untouched. No comment on #840, PR #843 or devplatform#11; no `## Cancel`/`## Retry` anywhere; no action on the library repository.
- Scratch files under `/tmp/` only: a `mktemp -d` directory for the tarball check and two temp files for the body edit.

## Implementation Steps
IMPORTANT: Execute every step in order, top to bottom.

### Step 1: Re-verify the gate before touching anything
- Run the version listing and the spec's Step 1 tarball check against the review's full symbol list. Use `mktemp -d` and leave the directory in place afterwards (the pre-tool-use hook blocks recursive deletes; the directory is harmless):
  ```
  npm view @paysdoc/devplatform versions --json
  D=$(mktemp -d) && cd "$D" && npm pack @paysdoc/devplatform@latest --silent >/dev/null && tar xzf paysdoc-devplatform-*.tgz && node -e "(async()=>{const p=await import('$D/package/dist/providers/index.js');const g=await import('$D/package/dist/git/index.js');for(const s of ['createForgeCredentials','createGhRepoApi','createGitHubTokenProvider','resolveBootstrapGitIdentity','resolveContextToken','ghAuthToken','isGitHubAppConfigured','getInstallationToken'])console.log((s in p?'present':'MISSING')+' providers:'+s);for(const s of ['createLiteralTokenProvider','commitOps','branchOps','isLeaseRejection'])console.log((s in g?'present':'MISSING')+' git:'+s)})()"
  ```
- Expected today: versions `["1.0.0"]` and every one of the twelve lines `MISSING`. Record the output for the patch report and continue to Step 2.
- **If instead every line prints `present`** (the gate opened between review and patch — possible now that devplatform#11 is moving): STOP this patch without editing anything in-repo or on GitHub. Report that the migration re-run is now due and must start with operator step (4), the rebase onto `origin/dev` (resolving the README.md conflict described above), then spec Step 2 pinning that exact version. Do not attempt the ~140-file migration inside this patch cycle — it needs the rebase first and is a `hitl` change the operator gates.

### Step 2: Refresh PR #843's devplatform#11 paragraph (one `gh pr edit`), then re-check the handoff
- First confirm the draft and body are as verified above (identical to the eighth plan's check):
  ```
  gh pr view 843 -R paysdoc/AI_Dev_Workflow --json state,isDraft,autoMergeRequest,mergeStateStatus,body --jq '"state=\(.state)", "isDraft=\(.isDraft)", "autoMerge=\(.autoMergeRequest)", "mergeState=\(.mergeStateStatus)", "preamble-present=\(.body | contains("no relevant changes") and contains("createForgeCredentials") and contains("devplatform#11") and contains("k3pj1m") and contains("Tracks #840") and contains("Every review of this PR so far"))", "old-text-gone=\(.body | (contains("Prerequisite: paysdoc/devplatform#11 merged") or contains("@paysdoc/devplatform@1.0.0") or contains("Three reviews") or contains("is conflict-free as of 2026-09-22")) | not)", "rebase-note-refreshed=\(.body | contains("nothing else conflicts") and contains("#844 and #847 landed after it was cut"))", "l4-note-stale=\(.body | contains("has no PR and no remote branch"))"'
  gh pr view 843 -R paysdoc/AI_Dev_Workflow --json body --jq .body | grep -ciE '^(closes|fixes|resolves|implements) '
  ```
  Expected: `state=OPEN`, `isDraft=true`, `autoMerge=null`, `mergeState=DIRTY`, the three legacy checks `true`, `l4-note-stale=true`, and `0` closing-keyword lines. Only if `isDraft=false`: `gh pr ready --undo 843 -R paysdoc/AI_Dev_Workflow`. Only if `preamble-present`, `old-text-gone` or `rebase-note-refreshed` is `false`: re-apply the earlier cycles' documented edits first (`…gate-closed-refresh-pr-843-release-handoff.md` Step 2, `…gate-closed-verify-only-operator-handoff.md` Step 2, `…gate-closed-hold-draft-refresh-rebase-note.md` Step 2). If `l4-note-stale=false`, the paragraph has already been refreshed — skip the edit below and go to Step 3.
- Take a live snapshot of the vehicle so the new paragraph states the phase at edit time, not at plan time:
  ```
  gh issue view 11 -R paysdoc/devplatform --json updatedAt,comments --jq '"updatedAt=\(.updatedAt)", (.comments[-2:][] | "\(.createdAt) | \((.body|split("\n")[0])[0:90])")'
  gh pr list -R paysdoc/devplatform --head feature-issue-11-widen-public-exports-adw-switchover --state all --json number,state,isDraft --jq 'if length==0 then "no PR yet" else .[] | "PR #\(.number) \(.state) draft=\(.isDraft)" end'
  ```
  If the second command names a PR, replace the words `no PR yet` in the paragraph below with `PR #<n> <STATE>`; if the first command's last headline is later than `ADW Unverified — No JUnit Report Emitted (10:54:01Z)`, replace that clause with the newer headline and time. Change nothing else in the paragraph.
- Write the replacement paragraph (one line, kept as a `>` block-quote line so it stays inside the blocker preamble; it retains `devplatform#11` and `k3pj1m` so the fifth-cycle `preamble-present` check keeps passing, and contains no `Closes`/`Fixes`/`Resolves`/`Implements` keyword):
  ```
  cat > /tmp/adw-840-l4-paragraph.md <<'EOF'
  > **The natural (2) is paysdoc/devplatform#11** (barrel widening: `commitOps`/`branchOps` from `/git` for the feature-729/feature-844 steps, `createGhRepoApi` from `/providers` for the feature-797 steps). It is OPEN (`adw:feature`, `adw:unverified`) and, since 2026-09-22T10:37:31Z, **moving again**: the stalled `k3pj1m-widen-the-public-sur` run was cleared and a fresh run (`ifp6cg-widen-the-public-sur`) is driving it on branch `feature-issue-11-widen-public-exports-adw-switchover` (tip `458f90cb`, 15 commits ahead of `main`: `src/git/index.ts` gains `commitOps`, `isLeaseRejection`, `branchOps`; `src/providers/github/index.ts` gains `createGitHubTokenProvider`, `resolveBootstrapGitIdentity`, `resolveContextToken`, `ghAuthToken`, `isGitHubAppConfigured`, `getInstallationToken`, `createGhRepoApi`; plus a table-driven `smoke:package` key check, four `@adw-11` packaging scenarios and docs). Build Completed 10:50:00Z; the pipeline then flagged `ADW Unverified — No JUnit Report Emitted` (10:54:01Z); no PR yet. Do **not** `## Cancel` it while it runs — read `gh issue view 11 -R paysdoc/devplatform` for the current phase; if it errors before `pr_creating`, open the PR from that branch by hand. Library `main` already exports both #840 gate symbols (`createLiteralTokenProvider` from `/git`, `createForgeCredentials` from `/providers`, both in the unpublished v1.1.0) and `release.config.js` parses the agent-prefixed `feat:` commits, so merging this branch cuts the next minor — which reaches npm only after (1), the OIDC fix.
  EOF
  ```
- Apply it as a single-line replacement, asserting exactly one matching paragraph, then push the body back:
  ```
  B=$(mktemp) && gh pr view 843 -R paysdoc/AI_Dev_Workflow --json body --jq .body > "$B"
  node -e 'const fs=require("fs");const [file,para]=process.argv.slice(-2);const lines=fs.readFileSync(file,"utf8").split("\n");const idx=lines.flatMap((l,i)=>l.startsWith("> **The natural (2) is paysdoc/devplatform#11**")?[i]:[]);if(idx.length!==1)throw new Error("expected exactly one devplatform#11 paragraph, found "+idx.length);if(!lines[idx[0]].includes("has no PR and no remote branch"))throw new Error("paragraph already refreshed; do not edit");lines[idx[0]]=fs.readFileSync(para,"utf8").replace(/\n$/,"");fs.writeFileSync(file+".new",lines.join("\n"));console.log("replaced line "+(idx[0]+1));' "$B" /tmp/adw-840-l4-paragraph.md
  gh pr edit 843 -R paysdoc/AI_Dev_Workflow --body-file "$B.new"
  ```
- Re-run the combined check from the top of this step plus the hash: expect `state=OPEN`, `isDraft=true`, `autoMerge=null`, `mergeState=DIRTY`, the three legacy checks `true`, `l4-note-stale=false`, `0` closing-keyword lines, and record the new `shasum -a 256 | cut -c1-16` prefix of the body in the patch report (it will differ from `911d484a5f32adbd`; that difference is the expected proof of this one edit). Also confirm `gh pr view 843 --json body --jq .body | grep -c 'ifp6cg'` prints `1`.
- Do **not** close the PR (the re-run reuses this branch and adwId; ADW resolves the existing PR by branch name). Do **not** introduce any `Closes`/`Fixes`/`Resolves`/`Implements` line. Do **not** post on devplatform#11 (its run is live; `## Cancel` would kill it and `## Retry` is meaningless while it runs) and do **not** post on #840 (the pipeline's Review Failed comment at 10:47:48Z already carries this blocker's Issue #1 text; the operator sequence lives in PR #843's body and in this plan).

### Step 3: Leave the branch exactly as found — conflict included — and hand back the six operator steps
- Verify nothing in-repo moved: `git status --short` prints nothing beyond this plan file (until the pipeline commits it); `git log --oneline 4d25d21c..HEAD` prints exactly `6c33d150`, `b6f1dc93`, `6f55870d`, `0f6395ff`, `a6f398a4`, `eee3d00c`, `136c288d`, `d5fd43a1`, `7188098b` (plus, after the pipeline commits it, the docs-only commit adding this plan); `git diff --stat` is empty. No code commit, no dependency, no merge of `origin/dev`, no rebase, no push from this patch.
- Explicitly leave the README.md conflict unresolved: the review assigns the rebase to operator step (4) as the first move of the re-run and says "do not merge, rebase or resolve the README.md conflict"; the pipeline resolves conflicts itself at merge time (`adws/phases/autoMergePhase.ts` and `adws/triggers/autoMergeHandler.ts` invoke `/resolve_conflict`), so a `DIRTY` draft is not a merge hazard; and the conflict is two hunks of README tree-listing comments that the rebase resolves by keeping either side.
- In the patch report, restate for the operator, in the review's order, with step (2) corrected for the vehicle's new state:
  1. Fix the npm trusted-publisher (OIDC) grant for `paysdoc/devplatform`'s `release.yml` so `npm publish` can succeed (release run `34503029087` failed with 403 OIDC permission denied). Do this **before** devplatform#11's PR merges, or its release run fails the same way and semantic-release will then treat that version as released too.
  2. Land the widening on library `main`. It is already built: devplatform#11's fresh run `ifp6cg-widen-the-public-sur` has branch `feature-issue-11-widen-public-exports-adw-switchover` at `458f90cb` (15 commits: `src/git/index.ts` re-exporting `commitOps`, `isLeaseRejection`, `branchOps`; `src/providers/github/index.ts` re-exporting `createGitHubTokenProvider`, `resolveBootstrapGitIdentity`, `resolveContextToken`, `ghAuthToken`, `isGitHubAppConfigured`, `getInstallationToken`, `createGhRepoApi`; the `smoke:package` key check; the four `@adw-11` packaging scenarios; docs). `main` already exports `createLiteralTokenProvider` from `/git` and `createForgeCredentials` from `/providers`, so the merged result covers all twelve symbols. Do **not** `## Cancel` the run while it is live; let it reach `pr_creating`, then review and merge its PR (it is 2 behind `main` — the docs-index sweep — so expect a trivial rebase). If the run errors before opening a PR, open the PR from that branch by hand rather than re-planning. Confirm the import-graph assertion that `./git` pulls no adapter module is in the merged tests. Because `release.config.js` parses `build-agent: feat:` headers as `feat`, the merge cuts the next minor (semantic-release treats the unpublished v1.1.0 as released, so this new `feat:` content — not the OIDC fix alone — is what produces a version above 1.0.0).
  3. Confirm with `npm view @paysdoc/devplatform versions` and the Step 1 tarball check above that every listed symbol prints `present`.
  4. Rebase this branch onto `origin/dev` (#844 and #847 changed the consumer inventory and README); expect the single docs-only README.md conflict described above, keep either side of both hunks, nothing else conflicts.
  5. Re-run the build from spec Step 2 pinning the exact published version through Step 13, following the updated #840 body for the launch boundary (`createForgeCredentials({ forge, identity, deps })`; no production module imports a forge-named symbol — the #840 forge-name grep must print nothing).
  6. Re-review against all four acceptance criteria and the spec's Validation Commands: empty exempt set, exactly one sanctioned construction site (`adws/core/launchGitContext.ts`), no extraction-readiness section, the import grep silent, both directories gone, `lint:docs-index` green, feature-729 repointed.
- Note for the operator: the orchestrator's review→patch loop is bounded by `MAX_REVIEW_RETRY_ATTEMPTS` (default 3, per the header comment at `adws/adwChore.tsx:26`), so a run stops on its own; every `## Retry` on #840 before steps (1)–(3) are done re-spends the same bounded cycles and lands here again. With the vehicle now moving, the first useful `## Retry` is the one posted after step (3) prints twelve `present` lines.

## Validation
Execute every command to validate the patch is complete with zero regressions.

- PR state: the Step 2 combined check — expect `state=OPEN`, `isDraft=true`, `autoMerge=null`, `mergeState=DIRTY` (expected until the operator's rebase; `CLEAN` is acceptable only if the operator has already rebased), `preamble-present=true`, `old-text-gone=true`, `rebase-note-refreshed=true`, `l4-note-stale=false`, `0` closing-keyword lines, `grep -c 'ifp6cg'` over the body → `1`, and a body hash different from `911d484a5f32adbd` (record it; a hash still equal to `911d484a5f32adbd` means the paragraph edit was not applied and Step 2 must be redone).
- In-repo untouched: `git status --short && git log --oneline 4d25d21c..HEAD && git diff --stat` — expect nothing tracked-and-modified, exactly the nine docs commits `6c33d150`, `b6f1dc93`, `6f55870d`, `0f6395ff`, `a6f398a4`, `eee3d00c`, `136c288d`, `d5fd43a1`, `7188098b` (plus the pipeline's commit of this plan file once made), no merge commit, and an empty diff; `test -d adws/gitContext && test -d adws/providers && ! grep -q '@paysdoc/devplatform' package.json && echo "in-repo untouched"` — expect `in-repo untouched`.
- Baseline no-regression on the untouched branch (nothing changed, so these must stay as recorded; run each on its own so the exit code is visible — this shell is zsh, where `$pipestatus` is lowercase, and vitest's summary carries ANSI colour, so pipe through `sed 's/\x1b\[[0-9;]*m//g'` before grepping): `bunx tsc --noEmit`, `bunx tsc --noEmit -p adws/tsconfig.json`, `bun run lint`, `bun run lint:git-guard`, `bun run lint:docs-index`, `bun run test:unit`, `NODE_OPTIONS="--import tsx" bunx cucumber-js --tags "@regression"` — expect both typechecks and lint exit 0; the guard's PASS with its unchanged pre-migration banners (`Exempt packages (2)`; the empty-exempt-set banner is the re-run's exit criterion, not this patch's); docs-index 47 entries all passing; 180 test files / 3193 tests green; the regression suite at 10 passed / 0 failed with 42 pending and 1 undefined (cucumber exits 1 on those in strict mode; zero assertion failures is the pass condition, unchanged from the pre-migration baseline; `feature-729` is repointed by the re-run, not by this patch).
- Gate check (documenting, not passing — today it prints twelve `MISSING` lines; the migration re-run starts only when it prints none): `npm view @paysdoc/devplatform versions --json` → `["1.0.0"]`, plus the Step 1 tarball command.
- Rebase status for the re-run (documenting): `git fetch -q origin dev && git merge-tree --write-tree origin/dev HEAD 2>&1 | grep '^CONFLICT'` — expect exactly one line, naming `README.md`. If `dev` has moved again and other paths appear, record them in the patch report for operator step (4); still no merge or rebase from this patch.
- The spec's own Validation Commands (`bun install`, lint, both typechecks, unit, `lint:git-guard` with an empty exempt set, `lint:docs-index`, `@regression`, the import grep, the directory check) and the #840 forge-name grep are the exit criteria of the **re-run**, not of this patch; they cannot pass until the migration is built on a published library version above 1.0.0.

## Patch Scope
**Lines of code to change:** 0 in-repo (this plan file is the only committed artifact); 1 paragraph replaced in PR #843's body on GitHub
**Risk level:** low — verification plus one reversible edit to a draft PR's body; no code commits, no dependency, no merge, no rebase, no push, no comment, no label change, no action on the library repository or on any issue
**Testing required:** the GitHub-state verification above (including the four body checks and the `ifp6cg` presence check), the unchanged baseline typechecks / lint / guard / docs-index / unit suite / regression suite, and the documented gate and rebase-status checks
