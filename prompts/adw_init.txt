/adw_init '204' 'mll0og-remove-run-bdd-scena' '{"number":204,"title":"Remove `## Run BDD Scenarios` from commands.md and consolidate into `## Run Scenarios by Tag`","body":"## Summary\n\n`## Run BDD Scenarios` in `.adw/commands.md` is a redundant special case of `## Run Scenarios by Tag`. The scenario writer always tags scenarios with `@adw-{issueNumber}`, so running issue-scoped BDD scenarios can be achieved via `## Run Scenarios by Tag` with tag `adw-{issueNumber}` — no dedicated command is needed.\n\n## Motivation\n\n- `runBddScenarios()` and `runScenariosByTag()` in `bddScenarioRunner.ts` are nearly identical — both spawn a subprocess, skip on `N/A`, and return `BddScenarioResult`. The only difference is the placeholder (`{issueNumber}` vs `{tag}`).\n- The scenario writer (`scenario_writer.md`) always tags scenarios with `@adw-{issueNumber}`, so tag-based filtering already covers the issue-scoped use case.\n- Consolidation reduces config surface, removes dead code, and eliminates a confusing distinction.\n\n## Changes required\n\n### 1. Remove `## Run BDD Scenarios` from config\n- **`.adw/commands.md`**: Delete the `## Run BDD Scenarios` / `N/A` section (lines 39–40)\n- **`adws/core/projectConfig.ts`**: Remove `runBddScenarios` from `CommandsConfig` interface, `HEADING_TO_KEY` map, and `getDefaultCommandsConfig()`\n\n### 2. Remove `runBddScenarios()` function\n- **`adws/agents/bddScenarioRunner.ts`**: Delete the `runBddScenarios()` function (lines 25–76). Keep `runScenariosByTag()`.\n- **`adws/agents/index.ts`**: Remove `runBddScenarios` from exports\n\n### 3. Refactor retry infrastructure\n- **`adws/agents/testRetry.ts`**: Replace `runBddScenariosWithRetry` to use `runScenariosByTag` internally, passing `adw-{issueNumber}` as the tag and using the `runScenariosByTag` command from config. Remove `BddScenarioRetryOptions.scenarioCommand` and replace with tag-command-based fields (or rename to clarify).\n\n### 4. Update callers to use tag-based approach\n- **`adws/phases/testPhase.ts`** (~line 96): Change from `projectConfig.commands.runBddScenarios` to `projectConfig.commands.runScenariosByTag`, passing `adw-{issueNumber}` as the tag.\n- **`adws/adwTest.tsx`** (~line 148): Same change — use `runScenariosByTag` command with `adw-{issueNumber}`.\n\n### 5. Ensure no testing gaps\n- **Issue-scoped scenarios**: Covered by `## Run Scenarios by Tag` with tag `adw-{issueNumber}` — the scenario writer already creates these tags.\n- **Regression scenarios**: Already covered by `## Run Regression Scenarios` (`cucumber-js --tags \"@regression\"`) — no change needed.\n- **Add unit tests**: `runBddScenarios()` and `runBddScenariosWithRetry()` currently have **no unit tests**. The refactored `runScenariosByTag`-based retry path should have test coverage added.\n\n### 6. Update documentation\n- **`.adw/conditional_docs.md`**: Update the reference to `## Run BDD Scenarios` (line 112) to point to `## Run Scenarios by Tag`\n- **`.claude/commands/adw_init.md`**: Remove `## Run BDD Scenarios` from the ADW init template if present\n- **`.claude/commands/scenario_writer.md`**: Already references `## Run Scenarios by Tag` — verify no stale references\n- **Spec/patch files**: Update references in `specs/` and `app_docs/` where applicable\n\n## Acceptance criteria\n\n- [ ] `## Run BDD Scenarios` section removed from `.adw/commands.md`\n- [ ] `runBddScenarios` field removed from `CommandsConfig` and all config parsing\n- [ ] `runBddScenarios()` function deleted from `bddScenarioRunner.ts`\n- [ ] `testPhase.ts` and `adwTest.tsx` use `runScenariosByTag` with tag `adw-{issueNumber}`\n- [ ] Retry infrastructure updated to use tag-based approach\n- [ ] Unit tests added for the refactored scenario retry path\n- [ ] All documentation references updated\n- [ ] `bun run build` passes\n- [ ] `bun run test` passes"}'