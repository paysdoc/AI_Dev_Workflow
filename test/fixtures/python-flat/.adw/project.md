# Project

## Project Overview

A flat-layout (no `src/`) Python fixture used by ADW behavioral tests to prove the
unit-test phase executes against `testDirectory` and that the skip-as-passed trap is
gone. Not a real project — the tests are minimal stubs.

## Application Type

cli

## Unit Tests

enabled
