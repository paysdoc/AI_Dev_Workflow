# Project

## Project Overview

A minimal fixture CLI tool used by ADW behavioral tests to exercise the review phase
end-to-end with mocked boundaries. Not a real project — all commands are stubs that
return deterministic, cost-free output.

## Application Type

cli

## Unit Tests

disabled
