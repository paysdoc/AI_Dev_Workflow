# Takeover, Coordination & Stage Recovery Policy

## Overview

This module governs how multiple concurrent ADW events safely coordinate around a single issue, and how a workflow recovers when it stalls, crashes, or fails a gated check. It provides the per-issue spawn lock, the takeover decision tree, the per-repo-owned pause queue scanner and resume path, the merge dispatch gate, and the per-repository concurrency guard, together preventing double-spawning and resuming paused workflows after rate-limit recovery. Underneath, an exhaustive stage classifier (`adws/core/stageClassifier.ts`) maps every `WorkflowStage` literal to one of six `StageClass` values that both the takeover decision and cron eligibility dispatch on, a bounded resume-cap policy (`adws/core/resumePolicy.ts`) caps automatic `phase_timeout` recovery before escalating to a human, and a `## Retry` directive handler (`adws/triggers/retryHandler.ts`) lets a human reset a human-gated stage back into the automatic recovery path.

## Responsibilities

- `classifyStage(stage: WorkflowStage): StageClass` (`adws/core/stageClassifier.ts`) — exhaustive `switch` mapping every `WorkflowStage` literal to one of `'active' | 'awaiting_merge' | 'retriable' | 'resumable' | 'terminal' | 'human_gated'`, with a compile-time `never` guard so an unhandled literal fails `tsc`. `classifyStageString(stage: string): StageClass` adapts raw persisted strings, checking the dynamic `${phaseName}_running`/`${phaseName}_completed` values `phaseRunner` emits (which are not `WorkflowStage` literals) before delegating to `classifyStage`.
- `evaluateCandidate` (`takeoverHandler`): the single decision tree for any candidate arriving at an issue. Attempts to acquire the spawn lock; if held by a live process, returns `defer_live_holder`. Otherwise reads state and dispatches on the stage's `StageClass` (see table below), with two raw-stage overrides checked ahead of the class fallthrough: `phase_timeout` (bounded-resume cap gate) and the `review_failed` routing folded into `human_gated`. The `retriable` class and the `phase_timeout` override share a single `recoverViaResumeInPlaceOrReset` seam that probes the worktree via `decideWorktreeReuse` and reuses it in place if healthy, else falls back to `recoverViaResetFromRemote`.
- `resolveAdwId` (inside `buildDefaultTakeoverDeps`): resolves the canonical adwId from issue comments — reads `boundary.providers.issueTracker.fetchComments(issueNumber)`, maps each to `{ body }`, and hands the list to `extractLatestAdwId`, which takes the LATEST matching comment, so chronological comment order is load-bearing. `EvaluateCandidateInput.boundary` (`LaunchBoundary`) is required (#821) — there is no longer a boundary-less fallback to a legacy `fetchIssueCommentBodies`/`repoInfo` route.
- `acquireIssueSpawnLock` (`spawnGate`): atomically creates a JSON lock file under `agents/spawn_locks/` using `O_EXCL` (exclusive create); reclaims stale locks from dead PIDs.
- `releaseIssueSpawnLock`: removes the lock file; tolerates `ENOENT`.
- `readSpawnLockRecord`: returns the stored `{ pid, pidStartedAt }` without modifying the lock.
- `isConcurrencyLimitReached(providers)` (`concurrencyGuard`): counts in-progress issues (ADW comment present + no linked merged/closed PR, via `adws/forge/linkedPrDetector.ts`'s `fetchLinkedPRs`/`hasLinkedMergedOrClosedPR`) and returns true when the count reaches `MAX_CONCURRENT_PER_REPO`. Delegates to the exported `isConcurrencyLimitReachedAt(providers, limit)`, which takes the cap as a parameter (defaulting to the real `MAX_CONCURRENT_PER_REPO` constant) so tests can pin a threshold without depending on the frozen, env-derived constant.
- `shouldDispatchMerge` (`mergeDispatchGate`): reads the spawn lock for an issue and returns true when no lock record exists, the record's PID is dead, or the record's `pidStartedAt` is empty (stale format). Returns false only when a live PID holds the lock.
- `scanPauseQueue(cycleCount, probe, deps)` (`pauseQueueScanner`) is a thin I/O shell over the pure `pauseQueueDecider`: on a `PROBE_INTERVAL_CYCLES`-eligible cycle it reads the queue and filters to the entries `isOwnedByScanningCron(entry, deps.scanningCron)` owns. If nothing is owned, it logs one "none owned" summary line naming the scanning cron's `owner/repo` and returns **without calling the probe at all** — this is also the only trace an orphaned entry (its owning cron not running) leaves; fixing that is out of scope. Otherwise it computes `now` from `deps.now?.() ?? new Date()` and filters the owned subset to those whose `resetsAt` is absent, unparseable, or not later than `now` (`isBeforeReset`). If none of the owned entries are due, it logs the earliest owned `resetsAt` and returns, again without probing — a foreign entry that happens to be due never triggers a probe on a cron that owns nothing due. Otherwise it calls `probe()` exactly once for the whole scan (never once per entry, and never for a cron with nothing owned and due) and feeds **every** entry in the queue — owned or not, due or not — through `decidePauseQueueAction({ entry, scanningCron: deps.scanningCron, probe: classification, now, maxProbeFailures: MAX_UNKNOWN_PROBE_FAILURES })`, then executes the single action returned; a non-owner's entries decide to `skip_not_owner` and the executor no-ops on them, so the "one owning cron" rule is a real production path, not just a probe-gate optimization. `probe` and `deps` are both required (no default): `trigger_cron.ts` passes `probeRateLimit` (`adws/triggers/rateLimitProbe.ts`) and its own `scanningCron` explicitly via the exported `runPauseQueueScanTick`, exactly as it already passes `scanAuthQueue` its boundary. `probeRateLimit` runs a one-turn `claude` ping under `--output-format stream-json --verbose` and classifies via `classifyProbeResult` against the shared `claudeStreamParser` state into a `ProbeClassification` (`{ verdict, rateLimitType?, resetsAt? }`), ranked: `authErrorDetected` ⇒ `failed` (a confirmed non-rate-limit failure, never mistaken for a limit even alongside a rejected event); `rateLimitDetected` / `serverErrorDetected` / `overloadedErrorDetected` ⇒ `limited`, carrying the facts a rejected `rate_limit_event` supplied (the same signals `agentProcessHandler` pauses a live agent run on); exit 0 with a non-errored `result` envelope ⇒ `clear`; everything else, including output with no parseable JSON, ⇒ `unknown`. There is no text fallback — a CLI wording change can never strand a paused workflow again. Both `scanPauseQueue` and `probeRateLimit(exec?)` take an injectable seam so tests never spawn the real CLI; `deps: PauseQueueScanDeps` is `ResumeDeps & { scanningCron: ScanningCronIdentity }` — `scanningCron` is required (there is no sane default owner: "own everything" reinstates the 2026-09-22 incident, "own nothing" strands every entry), and `ResumeDeps` (`{ now?, spawn? }`) is forwarded into `resumeWorkflow` on a `resume` action.
- `decidePauseQueueAction(input)` (`adws/triggers/pauseQueueDecider.ts`) is the pure decision core — no I/O, clock, or environment reads; the clock, the strike budget and the scanning cron's identity are inputs. Decision table (ownership is evaluated before the reset gate, and the reset gate before the verdict, so a probe result taken by a non-owner, or while an entry is still waiting, can neither strike nor resume it):

  | entry owned by scanning cron? | `entry.resetsAt` vs `now` | `probe.verdict` | action |
  |---|---|---|---|
  | no | any | any | `skip_not_owner` |
  | yes | parses and is later than `now` | any | `skip_before_reset` |
  | yes | absent, unparseable, or past | `clear` | `resume` |
  | yes | absent, unparseable, or past | `limited` | `refresh_reset` — carries the probe's `resetsAt`/`rateLimitType` (converted to ISO) when it reports a reset time; a bare `{ kind: 'refresh_reset' }` otherwise, even when the probe names a limit type with no reset time. Never a strike. |
  | yes | absent, unparseable, or past | `failed` / `unknown` | `count_strike` while `probeFailures + 1 < maxProbeFailures`, else `evict` |

  Ownership (`isOwnedByScanningCron`, also exported and used directly by the scanner's probe gate): an entry belongs to the cron whose `ScanningCronIdentity.repoId` matches the target repo recorded on the entry (`--target-repo` in `extraArgs`, read by the module-private, pure, total `parseEntryTargetRepo` — never `parseTargetRepoArgs`, which splices its array and calls `process.exit` on a malformed value and so is unusable inside a total decider a queue entry must never be able to kill), compared case-insensitively via the existing `sameRepoIdentity`; an entry recording no target repo belongs to the self-host cron (`scanningCron.selfHost`). Because `buildCronTargetRepoArgs` always emits `--target-repo`, every orchestrator a cron spawns — the self-host cron's own included — pauses with a target repo naming its spawning cron, so the self-host cron owns both target-less entries and entries recorded for its own repo, and nothing else.

  The executor in `pauseQueueScanner.ts` maps each action to I/O: `skip_not_owner` and `skip_before_reset` touch nothing and log nothing (the scan's one summary line already covers both); `resume` calls `resumeWorkflow` (`pauseQueueResume.ts`); `refresh_reset` updates `lastProbeAt` and, only when carried, `resetsAt`/`rateLimitType` — never `probeFailures`; `count_strike` updates `probeFailures`/`lastProbeAt`; `evict` removes the entry and posts an error comment naming `` `## Retry` `` as the recovery, writing no top-level state (the stage stays `paused`, which is what makes `## Retry` applicable).
- `resumeWorkflow` (`adws/triggers/pauseQueueResume.ts`) resolves the entry's repo via `resolveEntryRepoInfo`, verifies the worktree exists, acquires the spawn lock for verification, checks the canonical adwId claim in the top-level state, releases the lock, opens a per-resume log file, then **removes the entry from the queue before the orchestrator child exists** — so no concurrent reader (another cron's tick, the `## Retry` handler) can ever see an entry whose resume is in flight and spawn a duplicate. It then spawns the orchestrator (detached, through the injectable `deps.spawn ?? child_process.spawn` seam) and waits 2 seconds (`READINESS_WINDOW_MS`) for readiness. On success it posts the `resumed` comment (the entry is already gone). On any failure inside that window — an early exit, an `'error'` event, or the spawn call throwing synchronously — the extracted `requeueAfterFailedSpawn` re-appends the entry via `appendToPauseQueue` (never `updatePauseQueueEntry`, which is a silent no-op once the adwId is no longer in the file) with `probeFailures` incremented and a fresh `lastProbeAt`, so a failed spawn still counts toward the strike budget exactly as a failed probe does; `appendToPauseQueue`'s adwId dedupe rules out a double re-append. The held-lock skip (spawn lock already held by a live process) still returns before the removal, exactly as before — the entry stays queued with no strike until the holder dies and a later scan reclaims the stale lock.
- `resolveEntryRepoInfo` (`pauseQueueResume.ts`): extracts the target repo from a paused entry's `extraArgs` if present; falls back to the local git remote to prevent GH_TOKEN bleed (issue #565). That fallback is now reachable only by the self-host cron, the sole owner of entries recording no target repo, and the value it resolves equals that cron's own `scanningCron.repoId`.
- `resolveEntryBoundary` / `postEntryStageComment` (`pauseQueueResume.ts`, the latter exported so the scanner's own `evict` branch can call it directly): every stage comment the resume path or the scanner posts (worktree-gone, claim-diverged, probe-failure, resumed) routes through one helper. `resolveEntryBoundary(entry)` calls `buildLaunchBoundary(parseTargetRepoArgs(entry.extraArgs ?? []))` — the same `--target-repo` resolution `resolveEntryRepoInfo` already used for the spawn/lock flow — and `postEntryStageComment(entry, stage, ctx)` posts via `postIssueStageComment(boundary.providers, entry.issueNumber, stage, ctx)` inside a try/catch, logging a warning on failure rather than throwing.
- `runPauseQueueScanTick(cycleCount, scan?)` (`adws/triggers/trigger_cron.ts`) is the composition root: it hands `scanPauseQueue` the cron's own `scanningCron` (derived once, at startup, from the same `resolveCronRepo` result `buildLaunchBoundary` is built from — `{ repoId: cronRepoInfo, selfHost: targetRepo === null }`) and `probeRateLimit`. It has no cadence gate of its own (the scanner owns `PROBE_INTERVAL_CYCLES`) and no swallow — a throw still reaches `runGuardedTick` exactly as before this dispatch was factored out. `checkAndTrigger` calls it where it used to call `scanPauseQueue` directly.
- `nextResumeAction(attempts, max?)` (`adws/core/resumePolicy.ts`) — pure cap decision for automatic `phase_timeout` recovery: returns `'resume'` while `attempts < MAX_RESUME_ATTEMPTS` (3), `'escalate'` at or above the bound. `evaluateCandidate`'s `phase_timeout` branch reads `state.resumeAttempts`, calls `nextResumeAction`, and either increments the counter and calls `recoverViaResumeInPlaceOrReset`, or writes `workflowStage: 'human_gated'`, posts `formatHumanGatedComment(adwId, attempts, max)` (`workflowCommentsIssue.ts`), and releases the lock as `escalate_human_gated`.
- `adws/triggers/retryHandler.ts`'s `handleRetryDirective` maps a human's `## Retry` issue comment to one of four recovery paths, decided by the pure `decideRetryAction(stage)`: the three state-only resets — `merge_blocked → awaiting_merge` (resets `mergeRetryCount: 0`), `human_gated → phase_timeout` (re-arms `resumeAttempts: 0`), `review_failed → phase_timeout` (re-arms `resumeAttempts: 0`, so the review re-runs while completed plan/build/step-def phases stay skipped) — plus `paused`, which removes any pause-queue entry for the adwId, respawns the orchestrator `resolveResumeSpawn` resolves from top-level state, and posts the `resumed` comment. `paused_auth` and every active stage (`starting`, `resuming`, any `*_running`) are explicit no-ops, each logging why; any other stage is also a no-op. The handler's required third parameter, `RetryHandlerDeps`, is built by `buildRetryHandlerDeps(boundary, targetRepoArgs)`, binding the per-issue spawn lock and comment poster to the caller's own boundary — `trigger_cron.ts` and `trigger_webhook.ts` each pass their own.
- `review_failed` is classified `human_gated` (never auto-spawned by cron, routed to `spawn_fresh` by takeover): `cronIssueFilter.evaluateIssue` pins its ineligibility with an explicit guard placed before the grace-period and processed-spawn dedup checks (a money-fire guard — a just-failed review must stay ineligible even after a period of inactivity); it is recoverable only via `## Retry`.
- `executeSdlcReviewFailedHandoff` (`adws/phases/sdlcReviewHandoff.ts`) — extracted from `adwSdlc.tsx` so the SDLC review-failure outcome can be driven in isolation: writes `workflowStage: 'review_failed'` via `AgentStateManager.writeTopLevelState`, posts the branch-pointing `review_failed` comment via `postIssueStageComment`/`formatReviewFailedComment` (guarded on `ctx.branchName`, includes `## Retry` instructions), and logs a warning; `adwSdlc.tsx` then persists cost/metadata and returns before the doc/PR phases run.

The table below summarizes the `StageClass` → decision mapping both `evaluateCandidate` (takeover) and `evaluateIssue` (cron) dispatch on:

| `StageClass` | `WorkflowStage` literals | Cron decision | Takeover decision |
|---|---|---|---|
| `active` | `starting`, `resuming`, `build_running`, `test_running`, `review_running`, `document_running`, `install_running` | exclude (in progress) | SIGKILL-if-live → reset → reconcile → `take_over_adwId` |
| `awaiting_merge` | `awaiting_merge` | eligible → `merge` | `spawn_fresh` |
| `retriable` | `abandoned` | eligible → `spawn` | probe → reuse/reset → reconcile → `take_over_adwId` (no kill) |
| `terminal` | `completed`, `discarded`, `paused`, `paused_auth` | exclude (inline checks) | `skip_terminal` (release lock) |
| `human_gated` | `merge_blocked`, `human_gated`, `review_failed` | exclude (inline checks + explicit guard for `review_failed`) | `spawn_fresh` |
| `resumable` | all remaining literals including `phase_timeout`, `error`, `plan_building`, etc. | exclude by default; `phase_timeout` explicitly eligible | `spawn_fresh` by default; `phase_timeout` explicitly cap-gated |

## Contracts & Invariants

- Every `WorkflowStage` literal maps to exactly one `StageClass`; omitting a `case` in `classifyStage` is a `tsc` compile error (the `default` branch assigns `stage` to `never`). The `terminal` class is exactly `{completed, discarded, paused, paused_auth}` — no dynamic string maps to `terminal`.
- `classifyStageString` checks `endsWith('_running')` → `active` and `endsWith('_completed')` → `resumable` **before** delegating to `classifyStage`, so dynamic phaseRunner strings classify correctly even though they are not `WorkflowStage` literals. Unknown strings and `''` fall through to `default` and return `'resumable'` at runtime — the safest defensive class (`spawn_fresh` in takeover, excluded in cron).
- The spawn lock file uses `O_EXCL` (exclusive create) — exactly one writer wins the race; the loser re-reads the file to get the holder's PID and returns `defer_live_holder`. Stale locks (PID is dead according to `isProcessLive`) are reclaimed synchronously before the exclusive-create retry.
- `evaluateCandidate` always either releases the lock (on non-takeover exits) or leaves it held (for `spawn_fresh` and `take_over_adwId`). The caller's spawn releases the lock via `releaseIssueSpawnLock`.
- `paused` and `paused_auth` return `skip_terminal` from the takeover handler — it must never compete with their resumers. `paused_auth`'s sole resumer is the auth queue scanner. `paused` has two: the pause-queue scanner (automatic, on rate-limit clearance) and a human's `## Retry` (`retryHandler.ts`'s `resume_paused` action), which exists because the scanner can evict a queue entry (three `unknown` probes) and strand the workflow with no automatic recovery left.
- A `five_hour` `RateLimitError` with a known reset time no longer reaches this module's pause queue at all: `adws/core/phaseRunner.ts` rides it out in-process via the pure `decideRateLimitWait` (`adws/core/rateLimitWaitPolicy.ts`) and only ever calls `handleRateLimitPause` for the rejections that decision still enqueues. A `## Retry` posted while a workflow waits in-process is the existing `active`-class no-op above (the stage is still the phase's `*_running` stage), so it cannot spawn a second orchestrator beside the sleeping one. If the process dies mid-wait, the state is left in that same `*_running` stage with a dead PID, which the `active` row's existing SIGKILL-if-live → reset → reconcile → `take_over_adwId` path already recovers — no new takeover branch was needed.
- `resumeWorkflow` acquires the spawn lock for verification only and releases it before spawning, so the child's `acquireOrchestratorLock` call takes the lifetime lock. The brief gap is accepted per design. Canonical-claim verification aborts the resume when `topLevelState.adwId !== entry.adwId` — diverged state triggers a removal from the queue and an error comment on the issue.
- `shouldDispatchMerge` only returns false when a live PID holds the lock; a dead PID is treated as "dispatch" and `acquireIssueSpawnLock` handles the stale-lock reclaim when `adwMerge` starts.
- `EvaluateCandidateInput` is `{ issueNumber, boundary }` — `boundary: LaunchBoundary` is required (#821: no more optional/legacy-callers split). Identity, worktree base path (`boundary.gitContext.worktreePathFor(branchName)`), and providers all come from it; every caller (`trigger_cron.ts`, `webhookGatekeeper.ts`, `scanAuthQueue.ts`) passes its own boundary — `cronBoundary` for the cron, the per-event `commentBoundary`/`eventBoundary` for the webhook. `buildDefaultTakeoverDeps(boundary, reconcileDeps = buildDefaultReconcileDeps(boundary))` wires `deriveStageFromRemote` as `(adwId) => deriveStageFromRemote(adwId, reconcileDeps)`; `deriveStageFromRemote` itself dropped its long-unused `issueNumber`/`repoInfo` positional parameters and is now just `(adwId, deps: ReconcileDeps)`. The optional `reconcileDeps` parameter exists so tests can pin `branchExistsOnRemote` without a real git remote.
- `nextResumeAction` is pure (no I/O): `attempts < max → 'resume'`; `attempts >= max → 'escalate'`; identical inputs always produce identical output. `state.resumeAttempts` is cumulative — incremented on each automatic `phase_timeout` resume, cleared only on `## Retry` — and survives cron ticks because `writeTopLevelState` shallow-merges. The `escalate_human_gated` decision never spawns; both `trigger_cron` and `webhookGatekeeper` skip the spawn on this kind.
- `phase_timeout` recovery is via per-consumer raw-stage branches, not reclassification — `phase_timeout` stays `resumable` in `classifyStage`. Both `evaluateIssue` (cron) and `evaluateCandidate` (takeover) add an explicit `if (stage === 'phase_timeout')` branch keyed on the raw string, checked after the `retriable`/class checks.
- `## Retry` on `human_gated` or `review_failed` resets to `{ workflowStage: 'phase_timeout', resumeAttempts: 0 }`; on `merge_blocked` it resets to `{ workflowStage: 'awaiting_merge', mergeRetryCount: 0 }`; on `paused` it removes the pause-queue entry (if any) and respawns, writing no top-level state at all — the child rewrites its own stage at init exactly as it does after a scanner resume; on `paused_auth`, every active stage, and any other stage it is a no-op. The `review_failed` cron exclusion guard is placed **before** the grace-period and processedSpawns checks — placement before is load-bearing, since placing it after would let a just-failed review become eligible after a period of inactivity.
- No PR exists when `review_failed` is written — `adwSdlc.tsx` returns before the PR phase, so the `review_failed` comment points the operator at the (PR-less) branch. No SIGKILL happens on `phase_timeout` — `handlePhaseTimeout` already calls `process.exit(0)`, so the orchestrator is dead before the stage is written and the takeover branch skips straight to the cap gate.
- `cronStageResolver.isActiveStage` (compatibility bridge for `devServerJanitor` and `webhookHandlers`) preserves the historical "in progress" set `{starting, *_running, *_completed}`, which differs from the `active` class: `cls === 'active' && stage !== 'resuming'` OR `cls === 'resumable' && stage.endsWith('_completed')`.

## Configuration

`AGENTS_STATE_DIR` is the base directory for spawn lock files (`agents/spawn_locks/`). `PROBE_INTERVAL_CYCLES` and `MAX_UNKNOWN_PROBE_FAILURES` are core constants controlling the pause probe cadence and retry limit; the scanner passes `MAX_UNKNOWN_PROBE_FAILURES` into `decidePauseQueueAction` as `maxProbeFailures` — the decider itself never reads the environment. `MAX_CONCURRENT_PER_REPO` caps in-progress workflows per repository. `READINESS_WINDOW_MS` = 2000 ms is the window for confirming a resumed child did not immediately crash. `MAX_RESUME_ATTEMPTS = 3` in `adws/core/resumePolicy.ts` is the hard cap on automatic `phase_timeout` resumes — no environment variable; change the constant to adjust the cap. All of this is pure TypeScript with no I/O outside the injected `TakeoverDeps` (`takeoverHandler`) and `RetryHandlerDeps` (`retryHandler`).

## Gotchas

- `evaluateCandidate` caches a single `_defaultDeps` instance for performance; tests must inject their own deps to avoid cross-test contamination.
- `worktreeReset` in the takeover handler is best-effort — the takeover proceeds even if the worktree path does not exist on disk. The remote reconcile (`deriveStageFromRemote`) provides the authoritative next stage.
- `probeRateLimit` uses `--dangerously-skip-permissions` on the `claude` CLI; this is intentional for the lightweight rate-limit probe and does not reflect broader permission policy.
- `failed` and `unknown` are the two probe outcomes that consume `probeFailures` — but only for an entry past its reset time (or with none); an entry still waiting is never struck, whatever the probe says. Any new CLI failure wording (a session-limit rewording, a new outage message) must be classified `limited` structurally via a `claudeStreamParser` flag — there is no text fallback to lean on, and none may be reintroduced — since a genuine multi-hour limit misclassified as `unknown` before its reset time would once again burn the three-strike budget. `limited` never strikes and refreshes `resetsAt` (and `rateLimitType`) whenever the probe reports one, so a limit that extends itself keeps being waited for; a `limited` probe that reports no reset time changes nothing (not even `rateLimitType`) beyond `lastProbeAt`. `failed` exists as a distinct verdict from `unknown` specifically so a confirmed authentication failure is at least a countable strike rather than probed as `limited` forever. Eviction posts a comment naming `` `## Retry` `` as the recovery and writes no top-level state — the stage stays `paused`, which is what makes `## Retry` applicable.
- The `READINESS_WINDOW_MS` guard in `resumeWorkflow` only catches immediate child crash/exit — it does not guarantee the orchestrator completes successfully. The queue entry is removed **before** the child is spawned (not after the window elapses); a crash inside the window re-appends the entry with a strike rather than restoring the pre-spawn state, so a resumed-then-crashed workflow always shows one more `probeFailures` than it did before that resume attempt.
- `isConcurrencyLimitReached` fetches all open issues with comments on every invocation — it does not cache results. Callers should be aware of the API cost at high poll frequencies.
- **Fixed bug — target-repo entries used to lose their error comments**: before `postEntryStageComment` existed, all four pause-queue comment call sites used `createRepoContext({ cwd: process.cwd() })`, which validated the cron host's OWN git remote against the paused entry's repo. Any `--target-repo` entry failed that validation and threw, and the surrounding try/catch silently swallowed the throw — so worktree-gone, claim-diverged, and probe-failure comments were never posted for non-self-hosting repos. `postEntryStageComment` builds its boundary from the entry's own `--target-repo` args (via `resolveEntryBoundary`, mirroring `resolveEntryRepoInfo`), so target-repo entries now correctly receive these comments.
- **Fixed bug — two crons on one host could strike or resume the same entry.** On 2026-09-22 the `paysdoc/devplatform` cron and the `paysdoc/AI_Dev_Workflow` cron each scanned the same shared `agents/paused_queue.json`, and both probed and struck the same entry 1.9 seconds apart — the three-strike budget was spent by whichever crons happened to be running, at their combined cadence, not by one process at one cadence. `decidePauseQueueAction`'s `skip_not_owner` rule (evaluated before the reset gate) now confines every strike, refresh, resume and eviction to the one cron whose identity matches the entry's recorded target repo. The resume path had a second, independent race: it took the per-issue spawn lock only to verify the canonical claim, released it, spawned the child, waited two seconds, and only then removed the entry — a second scan inside that window could see the entry, find the lock free, and spawn a duplicate orchestrator. `resumeWorkflow` now removes the entry before the child is spawned, closing that window; a spawn that then fails re-appends the entry with a strike (see above) rather than leaving it removed or double-queued.
- **`review_failed` is `human_gated`, not `resumable`.** It was previously classified as `resumable`, which would have let cron auto-spawn it — a money-fire risk. The reclassification is a single-line move in the exhaustive switch, enforced at compile time by the `never` guard.
- **`review_failed → phase_timeout` re-arm does NOT consume the resume counter.** Only the automatic `evaluateCandidate` cap gate increments it; a human `## Retry` resets `resumeAttempts: 0`, giving the next automatic recovery a full fresh budget.
- **Intermediate loop comments vs. terminal handoff comment.** `reviewPhase.ts` posts a `review_failed` comment on each failed attempt in the retry loop (per-attempt informational). The terminal operator-facing comment is posted by `executeSdlcReviewFailedHandoff` after the loop exhausts. Multiple branch-pointing comments on the issue are acceptable known behaviour.
- **`executeSdlcReviewFailedHandoff` is extracted for BDD testability**, mirroring the `completePRReviewWorkflow` pattern from `prReviewCompletion.ts` — the inline-in-`main()` form could not be driven by a scenario without spawning a full orchestrator subprocess.
- **`human_gated` stage name equals its class name** (mirroring the `awaiting_merge` precedent). `merge_blocked` and `review_failed` remain distinct siblings in the same `human_gated` class; the three have different `## Retry` reset targets (`awaiting_merge`, `phase_timeout`, `phase_timeout`) so they must stay separate.
- **`resumeAttempts` is cumulative, not per-phase.** A workflow that makes progress but times out at several distinct phases will still escalate after 3 total resumes.
- **Dynamic phaseRunner stages** (`${phaseName}_running`, `${phaseName}_completed`, e.g. `plan_running`, `test_completed`) are not `WorkflowStage` literals. Always use `classifyStageString` (not `classifyStage`) when reading a raw `string` from the state file.
- **`isActiveStage` is NOT the same as `active`.** The compat bridge preserves a different set. Do not replace `isActiveStage` calls in `devServerJanitor.ts` or `webhookHandlers.ts` with `classifyStageString(s) === 'active'` without migrating the grace semantics.
- **`## Retry`'s paused path removes the queue entry before spawning and never rewrites `workflowStage`.** Both choices are load-bearing: removing first stops the scanner from resuming the same adwId a second time later, and writing no state means a failed spawn can't masquerade as a live orchestrator (which a written `resuming` would) or get raced by the very next cron tick (which a written `abandoned` would). The child's per-issue spawn lock — released right after the spawn call so the child can take it as its lifetime lock — is what actually guards against a duplicate, not a state write.
