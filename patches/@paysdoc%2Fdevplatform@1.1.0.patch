diff --git a/dist/providers/github/commands/prCommands.js b/dist/providers/github/commands/prCommands.js
index 2d4a204..32c50a9 100644
--- a/dist/providers/github/commands/prCommands.js
+++ b/dist/providers/github/commands/prCommands.js
@@ -27,7 +27,7 @@ export function fetchPRListCmd(owner, repo) {
     return `gh pr list --repo ${owner}/${repo} --state open --json number,headRefName,updatedAt`;
 }
 export function fetchAllPRsCmd(owner, repo) {
-    return `gh pr list --repo ${owner}/${repo} --state all --json number,body,state,mergedAt --limit 200`;
+    return `gh pr list --repo ${owner}/${repo} --state all --json number,body,state,mergedAt,updatedAt,url --limit 200`;
 }
 export function prChangedFilesCmd(owner, repo, prNumber) {
     return `gh pr view ${prNumber} --repo ${owner}/${repo} --json files`;
diff --git a/dist/providers/github/mappers.js b/dist/providers/github/mappers.js
index f2201bd..3454065 100644
--- a/dist/providers/github/mappers.js
+++ b/dist/providers/github/mappers.js
@@ -28,6 +28,8 @@ export function mapGitHubIssueToIssue(issue) {
         author: issue.author.login,
         labels: issue.labels.map((l) => l.name),
         comments: issue.comments.map(mapGitHubCommentToIssueComment),
+        createdAt: issue.createdAt,
+        url: issue.url,
     };
 }
 /**
diff --git a/dist/providers/jira/jiraIssueTracker.d.ts b/dist/providers/jira/jiraIssueTracker.d.ts
index 25b0fd5..0642f00 100644
--- a/dist/providers/jira/jiraIssueTracker.d.ts
+++ b/dist/providers/jira/jiraIssueTracker.d.ts
@@ -18,9 +18,10 @@ export interface JiraConfig {
 export declare class JiraIssueTracker implements IssueTracker {
     private readonly client;
     private readonly projectKey;
+    private readonly instanceUrl;
     private readonly logger;
     private readonly commentIssueMap;
-    constructor(client: JiraApiClient, projectKey: string, logger?: Logger);
+    constructor(client: JiraApiClient, projectKey: string, instanceUrl: string, logger?: Logger);
     private toJiraKey;
     private mapStatusCategory;
     private toIssueComment;
diff --git a/dist/providers/jira/jiraIssueTracker.js b/dist/providers/jira/jiraIssueTracker.js
index 1224658..6e00bdb 100644
--- a/dist/providers/jira/jiraIssueTracker.js
+++ b/dist/providers/jira/jiraIssueTracker.js
@@ -29,11 +29,13 @@ function matchTransition(targetStatus, transitions) {
 export class JiraIssueTracker {
     client;
     projectKey;
+    instanceUrl;
     logger;
     commentIssueMap = new Map();
-    constructor(client, projectKey, logger = consoleLogger) {
+    constructor(client, projectKey, instanceUrl, logger = consoleLogger) {
         this.client = client;
         this.projectKey = projectKey;
+        this.instanceUrl = instanceUrl.replace(/\/+$/, '');
         this.logger = logger;
     }
     toJiraKey(issueNumber) {
@@ -62,6 +64,8 @@ export class JiraIssueTracker {
             author: jiraIssue.fields.creator.displayName,
             labels: [...jiraIssue.fields.labels],
             comments,
+            createdAt: jiraIssue.fields.created,
+            url: `${this.instanceUrl}/browse/${jiraIssue.key}`,
         };
     }
     async fetchIssue(issueNumber) {
@@ -226,6 +230,6 @@ function validateJiraConfig(config) {
 export function createJiraIssueTracker(config, deps = {}) {
     validateJiraConfig(config);
     const client = new JiraApiClient(config.instanceUrl, config.auth, deps);
-    return new JiraIssueTracker(client, config.projectKey, deps.logger ?? consoleLogger);
+    return new JiraIssueTracker(client, config.projectKey, config.instanceUrl, deps.logger ?? consoleLogger);
 }
 //# sourceMappingURL=jiraIssueTracker.js.map
\ No newline at end of file
diff --git a/dist/providers/jira/jiraTypes.d.ts b/dist/providers/jira/jiraTypes.d.ts
index 59cf735..8f390ee 100644
--- a/dist/providers/jira/jiraTypes.d.ts
+++ b/dist/providers/jira/jiraTypes.d.ts
@@ -35,6 +35,8 @@ export interface JiraIssueFields {
     readonly creator: JiraUser;
     readonly labels: readonly string[];
     readonly comment?: JiraCommentPage;
+    /** ISO 8601 creation timestamp. */
+    readonly created: string;
 }
 export interface JiraIssueResponse {
     readonly id: string;
diff --git a/dist/providers/types.d.ts b/dist/providers/types.d.ts
index 074b9ed..8b477aa 100644
--- a/dist/providers/types.d.ts
+++ b/dist/providers/types.d.ts
@@ -47,6 +47,10 @@ export interface Issue {
     author: string;
     labels: string[];
     comments: IssueComment[];
+    /** ISO 8601 creation timestamp — read by the plan/scenario agent prompts (#844). */
+    createdAt: string;
+    /** The forge-published issue URL — read by the build agent prompt (#844). */
+    url: string;
 }
 /**
  * Named constants for project board status values.
@@ -122,6 +126,10 @@ export interface PullRequestRecord {
     body: string;
     state: string;
     mergedAt: string | null;
+    /** ISO 8601 last-update timestamp — the "newest first" ordering key for preferred-PR selection (#844). */
+    updatedAt: string;
+    /** The forge-published pull-request URL (#844). */
+    url: string;
 }
 /**
  * Interface for issue tracking operations across platforms.
